# Kocotree源文件数据质量检查与建表前规划

> 数据源：`D:\实习\AI客户看板\data\抖店\抖店kocotree服饰配件店\`  
> 目标数据库：`weidian`  
> 目标Schema：`"doudianKocotree"`  
> 检查方式：递归遍历全部XLSX和CSV，统一规范化表头、标识符和空白字符后进行全量只读核查。  
> 当前状态：源文件只读检查已完成；`raw_data`和`customer_id_mapping`已正式建表、上传并校验，其余33张业务表尚未创建。

## 一、数据集与粒度结论

| 项目 | 检查结果 |
|---|---:|
| 文件总数 | 111 |
| XLSX文件 | 19 |
| CSV文件 | 92 |
| 临时锁文件 | 0 |
| 不支持的文件格式 | 0 |
| 无法读取的文件 | 0 |
| 数据记录总数 | 33,743 |
| 不同子订单编号 | 33,743 |
| 不同主订单编号 | 31,765 |
| 空子订单编号 | 0 |
| 空主订单编号 | 0 |
| 含多个子订单的主订单 | 1,715 |
| 单个主订单最多子订单数 | 9 |
| 支付日期范围 | 2026-01-16—2026-06-30 |

数据的正确基础粒度是“每个子订单一行”。共有1,715个主订单包含多个子订单，因此不能把`主订单编号`作为`raw_data`的业务唯一键；`raw_data`仍使用`子订单编号`识别源记录，但客户周期拿货次数已调整为客户ID在`customer_daily_sales`中的出现次数，即有效拿货天数。

## 二、检查项目与结果

### 1. 重复数据

| 检查类型 | 重复组数 | 多余记录数 | 结论 |
|---|---:|---:|---|
| 二进制完全相同文件 | 0 | 0 | 无重复文件 |
| 单文件内部重复子订单 | 0 | 0 | 无文件内重复 |
| 跨文件重复子订单 | 0 | 0 | 无跨文件重复 |
| 全部字段完全相同的重复行 | 0 | 0 | 无整行重复 |

结论：33,743条记录可以按子订单粒度全部保留，不需要在本批数据中删除重复记录。建表时仍应保留子订单编号唯一性检查，以防后续新增文件与历史数据重叠。

### 2. 字段结构完整性

| 检查项 | 结果 |
|---|---:|
| 规范化后的表头版本数 | 1 |
| 表头不一致文件 | 0 |
| 字段数量 | 73 |
| 缺少核心字段的文件 | 0 |
| 每行字段数量异常的文件 | 0 |
| CSV编码读取失败 | 0 |

所有111个文件在去除CSV首字段的UTF-8 BOM后，字段名称、字段数量和字段顺序完全一致。现有设计稿把Kocotree的`raw_data`设计为73个中文原始字段加3个技术字段，共76个字段，是正确的。

### 3. 核心字段内容质量

| 检查项 | 数量 | 占全部记录 | 处理结论 |
|---|---:|---:|---|
| 空支付完成时间 | 1,400 | 4.1490% | 全部为`已关闭`订单，按现有规则不进入销售或退款聚合 |
| 无法解析的非空支付时间 | 0 | 0% | 通过 |
| 空订单应付金额 | 0 | 0% | 通过 |
| 非法订单应付金额 | 0 | 0% | 通过 |
| 负数或零金额 | 0 | 0% | 通过 |
| 空商品数量 | 0 | 0% | 通过 |
| 非法或非正商品数量 | 0 | 0% | 通过 |
| 空白或`-`商家编码 | 1 | 0.0030% | 该记录不进入商品维度表，其余合格维度不受影响 |
| 精选联盟但达人ID为0 | 18 | 精选联盟记录的0.0570% | 不进入客户相关表，其中9条已完成、9条已关闭 |

核心计算字段的类型完整性较高。异常记录均能被当前设计稿的筛选规则确定性处理，不需要为了这些记录改变表结构。

### 4. 业务枚举值

订单状态：

| 值 | 记录数 |
|---|---:|
| `已完成` | 24,952 |
| `已关闭` | 8,791 |

售后状态：

| 值 | 记录数 | 按设计是否退款 |
|---|---:|---|
| `-` | 25,073 | 否 |
| `退款成功` | 8,258 | 是 |
| `换货成功` | 259 | 否 |
| `售后关闭` | 142 | 是 |
| `补寄成功` | 6 | 否 |
| `待收退货` | 3 | 是 |
| `待退货` | 2 | 是 |

流量来源：

| 值 | 记录数 |
|---|---:|
| `精选联盟` | 31,593 |
| `小店自卖` | 2,150 |

当前文件没有出现`已发货`或`待发货`，但保留与儿童店一致的销售允许集合不会改变本批结果，也能兼容后续新增文件。退款规则“除空白、`-`、换货成功、补寄成功外均计入”能够覆盖当前全部售后状态，共识别8,405条退款记录。

### 5. 原始字段稀疏情况

以下9个字段在本批33,743条记录中全部为空：

`预计送达时间`、`车型`、`商品69码`、`发货SN码`、`发货IMEI码1`、`发货IMEI码2`、`物流SN码`、`物流IMEI码1`、`物流IMEI码2`。

以下9个字段在本批数据中全部为`-`：

`所属门店ID`、`预约发货时间`、`仓库ID`、`仓库名称`、`发货主体`、`发货主体明细`、`预约送达时间`、`建议发货时间（起）`、`建议发货时间（止）`。

这些字段属于原始导出结构中的可选字段。为了保持原文件可追溯性，建议继续在`raw_data`中保留，不删除、不做英文映射；派生表不依赖它们。

### 6. 时间覆盖提醒

支付日期范围内共有166个自然日，其中152天存在至少一条非空支付记录，以下14天没有支付记录：

```text
2026-01-17、2026-01-18、2026-01-19、2026-01-22、2026-01-25、2026-01-28、2026-01-29、
2026-01-31、2026-02-01、2026-02-11、2026-02-13、2026-02-16、2026-02-19、2026-02-27
```

业务侧已确认上述14个日期均为正常0成交，不是缺少导出文件。因此，现有源文件对2026-01-16至2026-06-30的业务数据覆盖完整。

## 三、可生成的业务记录规模

以下数量是按设计稿筛选条件进行的源记录级预判，不是派生表最终行数：

| 记录类型 | 符合条件的源记录数 |
|---|---:|
| 有效销售记录 | 24,952 |
| 有效商品销售记录 | 24,951 |
| 有效客户销售记录 | 23,624 |
| 有效客户商品销售记录 | 23,624 |
| 符合退款规则的记录 | 8,405 |

最终派生表行数会因日期、客户、商品和周期分组而减少，需在正式构建Stage表时计算并逐业务键校验。

## 四、问题分级与影响

| 事项 | 严重程度 | 置信度 | 对建表的影响 | 建议 |
|---|---|---|---|---|
| 重复子订单 | 无问题 | 高 | 无 | 保留后续批次唯一性断言 |
| 73字段结构一致 | 无问题 | 高 | 无 | 按76字段`raw_data`建表 |
| 1,400条空支付时间 | 低 | 高 | 不进入派生销售/退款表 | 原样进入`raw_data` |
| 1条空商家编码 | 低 | 高 | 不进入商品表 | 沿用现有过滤规则 |
| 18条精选联盟达人ID为0 | 低 | 高 | 不进入客户表 | 沿用现有过滤规则 |
| 18个完全空或全为`-`的原始字段 | 低 | 高 | 不影响派生表 | 原样保留以维持源结构 |
| 14天没有支付记录 | 无问题 | 高 | 已确认为正常0成交 | 无需补充文件 |
| 1,715个主订单含多个子订单 | 重要口径 | 高 | 必须以子订单为原始粒度 | 保持现有设计，不按主订单去重 |

## 五、设计稿是否需要修改

结论：**35张表的表名、字段结构和主要计算逻辑不需要修改。**现有设计稿已经正确处理了73字段原始表、XLSX/CSV混合输入、CSV BOM、客户筛选、商品筛选、退款规则、自定义季度和半年、拿货次数及健康度。

以下说明已同步到设计审核稿，未改变数据库结构：

1. “抽样检查”已更新为“111个文件全量检查通过”，并写入33,743条记录及重复数为0。
2. 已补充主订单与子订单关系：31,765个主订单对应33,743个子订单，1,715个主订单含多个子订单；`raw_data`唯一性仍以子订单为准，客户周期拿货次数则以客户日销售记录为准。
3. 已补充1,400条空支付时间全部属于已关闭订单，按现有规则正常排除。
4. 已补充1条无效商家编码和18条精选联盟零达人ID的确定性处理方式。
5. 已记录14个无支付记录日期由业务侧确认为正常0成交。

## 六、审核后的实施规划

1. `raw_data`和`customer_id_mapping`已在同一事务中创建、上传并完成提交后审计。
2. 其余33张业务表在正式执行前再次核对设计口径。
3. 其余33张业务表使用Stage并行构建，分别完成字段、业务键、周期、金额、数量、客户映射、拿货次数和健康度校验。
4. 核对原始表快照无变化后，在短事务中一次性发布33张业务表。
5. 任一强制校验失败时整体回滚，不向正式Schema留下半成品。

## 七、待用户确认事项

- [x] 已确认上述14个日期均为正常0成交，不需要补充文件。
- [ ] 确认`raw_data`继续保留全部73个原始字段，包括完全空或全为`-`的字段。
- [ ] 确认1条空商家编码记录只排除出商品维度表，不从`raw_data`及其他符合条件的表中删除。
- [ ] 确认18条精选联盟但达人ID为0的记录不进入客户相关表。
- [x] 确认以`子订单编号`作为原始业务唯一标识；客户周期拿货次数按客户ID在`customer_daily_sales`中的出现次数计算。
- [ ] 确认完成以上事项后，再开始生成原始表上传样例；未经样例确认不执行正式上传。
