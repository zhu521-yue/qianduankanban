# PostgreSQL 全库 385 张业务表功能与信息盘点

> 盘点数据库：`weidian`。本文仅基于 PostgreSQL 元数据及聚合统计生成，不包含客户、订单等敏感明细值，不修改数据库。表的业务解释来自 Schema、表名、字段名/类型、约束、索引、估算行数、容量和时间字段最小/最大值；标注“需结合数据字典或业务确认”的内容不能视为已确认业务口径。

- 表总数：**385**
- Schema 总数：**16**
- 行数：使用 `pg_class.reltuples` 的估算值，非逐行 `COUNT(*)`。
- 时间范围：仅对命名明显表示日期/时间/周期的 `date` 或 `timestamp` 字段执行 `min/max/count` 聚合；无结果表示字段为空或表无该类字段。
- 关联：优先记录数据库外键；未声明外键时，仅根据字段命名给出可能关联，不把推断当作事实。

## Schema 目录

- `alibaba`：35 张表
- `daren`：11 张表
- `doudian`：11 张表
- `doudianChildren`：35 张表
- `doudianKocotree`：35 张表
- `fenxiao`：11 张表
- `jushuitan`：35 张表
- `kuaishouxiaodian`：35 张表
- `kuaituantuan`：35 张表
- `muyinqijian`：35 张表
- `public`：3 张表
- `qijian`：35 张表
- `qudao`：9 张表
- `siyu`：11 张表
- `weidian`：38 张表
- `youzan`：11 张表

## 逐表盘点

### 1. `alibaba.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：816,762 行 / 176,775,168 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 816,762 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 816,762 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_daily_product_sales_business` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON alibaba.customer_daily_product_sales USING btree (id)` |
| `alibaba.uq_customer_daily_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_daily_product_sales_business ON alibaba.customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 2. `alibaba.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：205,076 行 / 40,361,984 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 205,076 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 205,076 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_daily_sales_business` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON alibaba.customer_daily_sales USING btree (id)` |
| `alibaba.uq_customer_daily_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_daily_sales_business ON alibaba.customer_daily_sales USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 3. `alibaba.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：205,076 行 / 45,088,768 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 205,076 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 205,076 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_daily_sales_metrics_business` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON alibaba.customer_daily_sales_metrics USING btree (id)` |
| `alibaba.uq_customer_daily_sales_metrics_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_daily_sales_metrics_business ON alibaba.customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 4. `alibaba.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：575,849 行 / 137,732,096 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 575,849 | 2025-02-01 | 2026-08-01 |
| `period_end` | 575,849 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 575,849 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_half_year_product_sales_business` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON alibaba.customer_half_year_product_sales USING btree (id)` |
| `alibaba.uq_customer_half_year_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_half_year_product_sales_business ON alibaba.customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 5. `alibaba.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：142,687 行 / 32,194,560 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 142,687 | 2025-02-01 | 2026-08-01 |
| `period_end` | 142,687 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 142,687 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_check` | 检查约束 | `CHECK (half_year_purchase_count >= 1 AND half_year_purchase_count <= (period_end - period_start + 1))` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_half_year_sales_business` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON alibaba.customer_half_year_sales USING btree (id)` |
| `alibaba.uq_customer_half_year_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_half_year_sales_business ON alibaba.customer_half_year_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 6. `alibaba.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：6,770,263 行 / 3,094,265,856 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `week_period_start` | `date` | 否 | — |
| `week_period_end` | `date` | 否 | — |
| `month_period_start` | `date` | 否 | — |
| `month_period_end` | `date` | 否 | — |
| `week_purchase_count` | `integer` | 否 | — |
| `week_score` | `numeric(5,2)` | 否 | — |
| `month_purchase_count` | `numeric(10,2)` | 否 | — |
| `month_score` | `numeric(5,2)` | 否 | — |
| `customer_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `character varying(50)` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 6,771,920 | 2025-01-27 | 2026-08-03 |
| `period_end` | 6,771,920 | 2025-02-02 | 2026-08-09 |
| `week_period_start` | 6,771,920 | 2025-01-27 | 2026-08-03 |
| `week_period_end` | 6,771,920 | 2025-02-02 | 2026-08-09 |
| `month_period_start` | 6,771,920 | 2025-01-01 | 2026-08-01 |
| `month_period_end` | 6,771,920 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 6,771,920 | 2026-08-12 16:48:13.743476 | 2026-08-13 23:32:52.680507 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_check` | 检查约束 | `CHECK (period_start = week_period_start)` |
| `customer_health_detail_check1` | 检查约束 | `CHECK (period_end = week_period_end)` |
| `customer_health_detail_check2` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `customer_health_detail_customer_score_check` | 检查约束 | `CHECK (customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_month_purchase_count_check` | 检查约束 | `CHECK (month_purchase_count >= 0::numeric)` |
| `customer_health_detail_month_score_check` | 检查约束 | `CHECK (month_score >= 0::numeric AND month_score <= 100::numeric)` |
| `customer_health_detail_week_purchase_count_check` | 检查约束 | `CHECK (week_purchase_count >= 0 AND week_purchase_count <= 7)` |
| `customer_health_detail_week_score_check` | 检查约束 | `CHECK (week_score >= 0::numeric AND week_score <= 100::numeric)` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_health_detail_business` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.alibaba_customer_health_non_loss_status_idx` | 否 | 否 | `CREATE INDEX alibaba_customer_health_non_loss_status_idx ON alibaba.customer_health_detail USING btree (customer_health_status) WHERE ((customer_health_status)::text <> '流失'::text)` |
| `alibaba.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON alibaba.customer_health_detail USING btree (id)` |
| `alibaba.uq_customer_health_detail_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_health_detail_business ON alibaba.customer_health_detail USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 7. `alibaba.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：128,678 行 / 21,086,208 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `buyer_id` | `character varying(255)` | 否 | — |
| `buyer_nickname` | `character varying(255)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 128,930 | 2026-08-12 11:13:11.590268 | 2026-08-13 23:32:52.680507 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_id_mapping_identity_chk` | 检查约束 | `CHECK (customer_id::text = buyer_id::text)` |
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_id_mapping_buyer_id_uk` | 唯一约束 | `UNIQUE (buyer_id)` |
| `customer_id_mapping_customer_id_uk` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_id_mapping_buyer_id_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_buyer_id_uk ON alibaba.customer_id_mapping USING btree (buyer_id)` |
| `alibaba.customer_id_mapping_customer_id_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_customer_id_uk ON alibaba.customer_id_mapping USING btree (customer_id)` |
| `alibaba.customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON alibaba.customer_id_mapping USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 8. `alibaba.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：627,901 行 / 147,865,600 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 627,901 | 2025-02-01 | 2026-08-01 |
| `period_end` | 627,901 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 627,901 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_monthly_product_sales_business` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON alibaba.customer_monthly_product_sales USING btree (id)` |
| `alibaba.uq_customer_monthly_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_monthly_product_sales_business ON alibaba.customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 9. `alibaba.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：164,408 行 / 36,929,536 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 164,408 | 2025-02-01 | 2026-08-01 |
| `period_end` | 164,408 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 164,408 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_monthly_purchase_count_check` | 检查约束 | `CHECK (monthly_purchase_count >= 1 AND monthly_purchase_count <= 31)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_monthly_sales_business` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON alibaba.customer_monthly_sales USING btree (id)` |
| `alibaba.uq_customer_monthly_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_monthly_sales_business ON alibaba.customer_monthly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 10. `alibaba.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：592,269 行 / 135,274,496 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 592,269 | 2025-02-01 | 2026-08-01 |
| `period_end` | 592,269 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 592,269 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_quarterly_product_sales_business` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON alibaba.customer_quarterly_product_sales USING btree (id)` |
| `alibaba.uq_customer_quarterly_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_quarterly_product_sales_business ON alibaba.customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 11. `alibaba.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：152,968 行 / 35,463,168 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 152,968 | 2025-02-01 | 2026-08-01 |
| `period_end` | 152,968 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 152,968 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_check` | 检查约束 | `CHECK (quarterly_purchase_count >= 1 AND quarterly_purchase_count <= (period_end - period_start + 1))` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_quarterly_sales_business` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON alibaba.customer_quarterly_sales USING btree (id)` |
| `alibaba.uq_customer_quarterly_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_quarterly_sales_business ON alibaba.customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 12. `alibaba.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：180,112 行 / 40,009,728 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 180,112 | 2025-01-27 | 2026-08-03 |
| `period_end` | 180,112 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 180,112 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `customer_weekly_sales_weekly_purchase_count_check` | 检查约束 | `CHECK (weekly_purchase_count >= 1 AND weekly_purchase_count <= 7)` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_customer_weekly_sales_business` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON alibaba.customer_weekly_sales USING btree (id)` |
| `alibaba.uq_customer_weekly_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_customer_weekly_sales_business ON alibaba.customer_weekly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 13. `alibaba.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：205,076 行 / 40,378,368 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 205,076 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 205,076 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_daily_customer_sales_business` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON alibaba.daily_customer_sales USING btree (id)` |
| `alibaba.uq_daily_customer_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_daily_customer_sales_business ON alibaba.daily_customer_sales USING btree (transaction_date, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 14. `alibaba.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：375,472 行 / 85,458,944 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 375,472 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 375,472 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_daily_product_sales_business` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON alibaba.daily_product_sales USING btree (id)` |
| `alibaba.uq_daily_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_daily_product_sales_business ON alibaba.daily_product_sales USING btree (transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 15. `alibaba.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：549 行 / 196,608 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-13 23:11:43.561744 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_daily_sales_business` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON alibaba.daily_sales USING btree (id)` |
| `alibaba.uq_daily_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_daily_sales_business ON alibaba.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 16. `alibaba.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：549 行 / 221,184 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(10,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_daily_sales_metrics_business` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON alibaba.daily_sales_metrics USING btree (id)` |
| `alibaba.uq_daily_sales_metrics_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_daily_sales_metrics_business ON alibaba.daily_sales_metrics USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 17. `alibaba.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：142,687 行 / 30,941,184 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 142,687 | 2025-02-01 | 2026-08-01 |
| `period_end` | 142,687 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 142,687 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_half_year_customer_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON alibaba.half_year_customer_sales USING btree (id)` |
| `alibaba.uq_half_year_customer_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_half_year_customer_sales_business ON alibaba.half_year_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 18. `alibaba.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：14,642 行 / 3,866,624 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 14,642 | 2025-02-01 | 2026-08-01 |
| `period_end` | 14,642 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 14,642 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_half_year_product_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON alibaba.half_year_product_sales USING btree (id)` |
| `alibaba.uq_half_year_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_half_year_product_sales_business ON alibaba.half_year_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 19. `alibaba.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：3 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_check` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_half_year_refunds_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON alibaba.half_year_refunds USING btree (id)` |
| `alibaba.uq_half_year_refunds_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_half_year_refunds_business ON alibaba.half_year_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 20. `alibaba.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-13 23:32:52.680507 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_check` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_half_year_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON alibaba.half_year_sales USING btree (id)` |
| `alibaba.uq_half_year_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_half_year_sales_business ON alibaba.half_year_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 21. `alibaba.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：164,408 行 / 35,586,048 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 164,408 | 2025-02-01 | 2026-08-01 |
| `period_end` | 164,408 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 164,408 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_monthly_customer_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON alibaba.monthly_customer_sales USING btree (id)` |
| `alibaba.uq_monthly_customer_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_monthly_customer_sales_business ON alibaba.monthly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 22. `alibaba.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：47,522 行 / 12,656,640 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 47,522 | 2025-02-01 | 2026-08-01 |
| `period_end` | 47,522 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 47,522 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_monthly_product_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON alibaba.monthly_product_sales USING btree (id)` |
| `alibaba.uq_monthly_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_monthly_product_sales_business ON alibaba.monthly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 23. `alibaba.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：18 行 / 73,728 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_check` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_period_start_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_monthly_refunds_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON alibaba.monthly_refunds USING btree (id)` |
| `alibaba.uq_monthly_refunds_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_monthly_refunds_business ON alibaba.monthly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 24. `alibaba.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：18 行 / 73,728 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-13 23:32:52.680507 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_check` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_period_start_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_monthly_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON alibaba.monthly_sales USING btree (id)` |
| `alibaba.uq_monthly_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_monthly_sales_business ON alibaba.monthly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 25. `alibaba.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：18 行 / 73,728 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(10,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_monthly_sales_metrics_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON alibaba.monthly_sales_metrics USING btree (id)` |
| `alibaba.uq_monthly_sales_metrics_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_monthly_sales_metrics_business ON alibaba.monthly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 26. `alibaba.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：152,968 行 / 33,914,880 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 152,968 | 2025-02-01 | 2026-08-01 |
| `period_end` | 152,968 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 152,968 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_quarterly_customer_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON alibaba.quarterly_customer_sales USING btree (id)` |
| `alibaba.uq_quarterly_customer_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_quarterly_customer_sales_business ON alibaba.quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 27. `alibaba.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：22,322 行 / 5,857,280 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 22,322 | 2025-02-01 | 2026-08-01 |
| `period_end` | 22,322 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 22,322 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_quarterly_product_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON alibaba.quarterly_product_sales USING btree (id)` |
| `alibaba.uq_quarterly_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_quarterly_product_sales_business ON alibaba.quarterly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 28. `alibaba.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：6 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_check` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_quarterly_refunds_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON alibaba.quarterly_refunds USING btree (id)` |
| `alibaba.uq_quarterly_refunds_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_quarterly_refunds_business ON alibaba.quarterly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 29. `alibaba.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：6 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-13 23:32:52.680507 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_check` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_quarterly_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON alibaba.quarterly_sales USING btree (id)` |
| `alibaba.uq_quarterly_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_quarterly_sales_business ON alibaba.quarterly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 30. `alibaba.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：978,308 行 / 675,536,896 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `内部订单号` | `text` | 是 | — |
| `标记多标签` | `text` | 是 | — |
| `售后单号` | `text` | 是 | — |
| `订单类型` | `text` | 是 | — |
| `线上订单号` | `text` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `发货仓` | `text` | 是 | — |
| `虚拟仓` | `text` | 是 | — |
| `分销商` | `text` | 是 | — |
| `店铺` | `text` | 是 | — |
| `小旗` | `text` | 是 | — |
| `买家ID` | `text` | 是 | — |
| `买家账号` | `text` | 是 | — |
| `卖家备注` | `text` | 是 | — |
| `订单日期` | `text` | 是 | — |
| `发货日期` | `text` | 是 | — |
| `付款日期` | `text` | 是 | — |
| `业务员` | `text` | 是 | — |
| `收货人` | `text` | 是 | — |
| `省` | `text` | 是 | — |
| `市` | `text` | 是 | — |
| `区县` | `text` | 是 | — |
| `快递公司` | `text` | 是 | — |
| `快递单号` | `text` | 是 | — |
| `售后登记日期` | `text` | 是 | — |
| `售后确认日期` | `text` | 是 | — |
| `售后分类` | `text` | 是 | — |
| `问题类型` | `text` | 是 | — |
| `商品编码` | `text` | 是 | — |
| `款式编码` | `text` | 是 | — |
| `原始线上订单号` | `text` | 是 | — |
| `产品分类` | `text` | 是 | — |
| `品牌` | `text` | 是 | — |
| `商品简称` | `text` | 是 | — |
| `颜色规格` | `text` | 是 | — |
| `供应商` | `text` | 是 | — |
| `店铺商品编码` | `text` | 是 | — |
| `基本售价` | `text` | 是 | — |
| `成本价` | `text` | 是 | — |
| `成本价来源` | `text` | 是 | — |
| `销售数量` | `text` | 是 | — |
| `实发数量` | `text` | 是 | — |
| `实发金额` | `text` | 是 | — |
| `销售金额` | `text` | 是 | — |
| `销售成本` | `text` | 是 | — |
| `实发成本` | `text` | 是 | — |
| `销售毛利` | `text` | 是 | — |
| `销售毛利率` | `text` | 是 | — |
| `已付金额` | `text` | 是 | — |
| `应付金额` | `text` | 是 | — |
| `售价` | `text` | 是 | — |
| `基本金额` | `text` | 是 | — |
| `退货数量` | `text` | 是 | — |
| `实退数量` | `text` | 是 | — |
| `退货金额` | `text` | 是 | — |
| `退货成本` | `text` | 是 | — |
| `实退成本` | `text` | 是 | — |
| `实退金额` | `text` | 是 | — |
| `运费收入` | `text` | 是 | — |
| `运费收入分摊` | `text` | 是 | — |
| `整单运费支出` | `text` | 是 | — |
| `运费支出` | `text` | 是 | — |
| `优惠金额` | `text` | 是 | — |
| `订单重量` | `text` | 是 | — |
| `供销商` | `text` | 是 | — |
| `币种` | `text` | 是 | — |
| `国际单号` | `text` | 是 | — |
| `剩余发货时间` | `text` | 是 | — |
| `付款方式` | `text` | 是 | — |
| `买家指定物流` | `text` | 是 | — |
| `收货国家地区` | `text` | 是 | — |
| `实发物流渠道` | `text` | 是 | — |
| `境外运费支出分摊` | `text` | 是 | — |
| `境外收入总计分摊` | `text` | 是 | — |
| `境外支出总计分摊` | `text` | 是 | — |
| `汇率` | `text` | 是 | — |
| `货主分销` | `text` | 是 | — |
| `平台补贴金额` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 989,568 | 2026-08-12 10:59:36.651442 | 2026-08-13 23:32:52.680507 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON alibaba.raw_data USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 31. `alibaba.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：180,112 行 / 38,305,792 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 180,112 | 2025-01-27 | 2026-08-03 |
| `period_end` | 180,112 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 180,112 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_weekly_customer_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.uq_weekly_customer_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_weekly_customer_sales_business ON alibaba.weekly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `alibaba.weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON alibaba.weekly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 32. `alibaba.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：129,865 行 / 33,054,720 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 129,865 | 2025-01-27 | 2026-08-03 |
| `period_end` | 129,865 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 129,865 | 2026-08-12 13:38:00.925022 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_weekly_product_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.uq_weekly_product_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_weekly_product_sales_business ON alibaba.weekly_product_sales USING btree (period_start, period_end, product_code)` |
| `alibaba.weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON alibaba.weekly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 33. `alibaba.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 81,920 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_weekly_refunds_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.uq_weekly_refunds_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_weekly_refunds_business ON alibaba.weekly_refunds USING btree (period_start, period_end)` |
| `alibaba.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON alibaba.weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 34. `alibaba.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 81,920 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-13 23:32:52.680507 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_weekly_sales_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.uq_weekly_sales_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_weekly_sales_business ON alibaba.weekly_sales USING btree (period_start, period_end)` |
| `alibaba.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON alibaba.weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 35. `alibaba.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 90,112 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `alibaba.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(10,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_weekly_sales_metrics_business` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `alibaba.uq_weekly_sales_metrics_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_weekly_sales_metrics_business ON alibaba.weekly_sales_metrics USING btree (period_start, period_end)` |
| `alibaba.weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON alibaba.weekly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 36. `daren.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：6,512 行 / 4,628,480 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `health_grain` | `text` | 否 | — |
| `source_platform` | `text` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `week_purchase_count` | `numeric(20,4)` | 是 | — |
| `month_purchase_count` | `numeric(20,4)` | 是 | — |
| `half_year_purchase_count` | `bigint` | 是 | — |
| `half_year_purchase_amount` | `numeric(20,2)` | 是 | — |
| `week_score` | `numeric(10,2)` | 是 | — |
| `month_score` | `numeric(10,2)` | 是 | — |
| `customer_score` | `numeric(10,2)` | 否 | — |
| `customer_health_status` | `text` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 6,610 | 2025-02-01 | 2026-08-01 |
| `period_end` | 6,610 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 6,610 | 2026-08-12 16:48:07.608057+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `customer_health_detail_customer_health_status_check` | 检查约束 | `CHECK (customer_health_status = ANY (ARRAY['高活跃'::text, '活跃'::text, '稳定'::text, '观察'::text, '风险'::text, '流失预警'::text, '流失'::text]))` |
| `customer_health_detail_customer_score_check` | 检查约束 | `CHECK (customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_health_grain_check` | 检查约束 | `CHECK (health_grain = ANY (ARRAY['natural_week'::text, 'business_half_year'::text]))` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daren_customer_health_business_key` | 唯一约束 | `UNIQUE (health_grain, source_platform, customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON daren.customer_health_detail USING btree (id)` |
| `daren.daren_customer_health_business_key` | 是 | 否 | `CREATE UNIQUE INDEX daren_customer_health_business_key ON daren.customer_health_detail USING btree (health_grain, source_platform, customer_id, period_start, period_end)` |
| `daren.daren_customer_health_customer_idx` | 否 | 否 | `CREATE INDEX daren_customer_health_customer_idx ON daren.customer_health_detail USING btree (customer_id)` |
| `daren.daren_customer_health_period_status_idx` | 否 | 否 | `CREATE INDEX daren_customer_health_period_status_idx ON daren.customer_health_detail USING btree (period_start, period_end, customer_health_status)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 37. `daren.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：549 行 / 147,456 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(20,2)` | 否 | — |
| `year_over_year_rate` | `numeric(20,6)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(20,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 556 | 2025-02-01 | 2026-08-11 |
| `updated_at` | 556 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_rolling_30_day_transaction_amount_check` | 检查约束 | `CHECK (rolling_30_day_transaction_amount >= 0::numeric)` |
| `daily_sales_rolling_7_day_transaction_amount_check` | 检查约束 | `CHECK (rolling_7_day_transaction_amount >= 0::numeric)` |
| `daily_sales_transaction_amount_check` | 检查约束 | `CHECK (transaction_amount >= 0::numeric)` |
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_transaction_date_key` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON daren.daily_sales USING btree (id)` |
| `daren.daily_sales_transaction_date_key` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_transaction_date_key ON daren.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 38. `daren.half_year_high_frequency_products`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：半年商品频次分析表。
- **表的作用**：按商品周期保存购买数量、金额及频次排名/分位数等分析结果。
- **可以提供的信息**：可提供商品周期交易金额、商品数量、数量排名和频次分位数；排名范围和分位数算法需结合刷新逻辑确认。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_product_quantity` | `numeric(20,4)` | 否 | — |
| `half_year_transaction_amount` | `numeric(20,2)` | 否 | — |
| `quantity_rank` | `bigint` | 否 | — |
| `amount_rank` | `bigint` | 否 | — |
| `selection_type` | `text` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 29 | 2025-02-01 | 2026-08-01 |
| `period_end` | 29 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 29 | 2026-08-12 12:56:09.99743+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_high_frequency_pro_half_year_transaction_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_high_frequency_produ_half_year_product_quantity_check` | 检查约束 | `CHECK (half_year_product_quantity >= 0::numeric)` |
| `half_year_high_frequency_products_amount_rank_check` | 检查约束 | `CHECK (amount_rank > 0)` |
| `half_year_high_frequency_products_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_high_frequency_products_quantity_rank_check` | 检查约束 | `CHECK (quantity_rank > 0)` |
| `half_year_high_frequency_products_selection_type_check` | 检查约束 | `CHECK (selection_type = ANY (ARRAY['quantity_top5'::text, 'amount_top5'::text, 'both'::text]))` |
| `half_year_high_frequency_products_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_high_frequency_prod_period_start_period_end_produ_key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.half_year_high_frequency_prod_period_start_period_end_produ_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_high_frequency_prod_period_start_period_end_produ_key ON daren.half_year_high_frequency_products USING btree (period_start, period_end, product_code)` |
| `daren.half_year_high_frequency_products_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_high_frequency_products_pkey ON daren.half_year_high_frequency_products USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 39. `daren.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_refunds_half_year_refund_amount_check` | 检查约束 | `CHECK (half_year_refund_amount >= 0::numeric)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.half_year_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_period_start_period_end_key ON daren.half_year_refunds USING btree (period_start, period_end)` |
| `daren.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON daren.half_year_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 40. `daren.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_sales_half_year_transaction_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.half_year_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_period_start_period_end_key ON daren.half_year_sales USING btree (period_start, period_end)` |
| `daren.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON daren.half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 41. `daren.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `monthly_refunds_monthly_refund_amount_check` | 检查约束 | `CHECK (monthly_refund_amount >= 0::numeric)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.monthly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_period_start_period_end_key ON daren.monthly_refunds USING btree (period_start, period_end)` |
| `daren.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON daren.monthly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 42. `daren.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `month_over_month_rate` | `numeric(20,6)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `monthly_sales_monthly_transaction_amount_check` | 检查约束 | `CHECK (monthly_transaction_amount >= 0::numeric)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.monthly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_period_start_period_end_key ON daren.monthly_sales USING btree (period_start, period_end)` |
| `daren.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON daren.monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 43. `daren.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `quarterly_refunds_quarterly_refund_amount_check` | 检查约束 | `CHECK (quarterly_refund_amount >= 0::numeric)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.quarterly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_period_start_period_end_key ON daren.quarterly_refunds USING btree (period_start, period_end)` |
| `daren.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON daren.quarterly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 44. `daren.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `quarterly_sales_quarterly_transaction_amount_check` | 检查约束 | `CHECK (quarterly_transaction_amount >= 0::numeric)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.quarterly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_period_start_period_end_key ON daren.quarterly_sales USING btree (period_start, period_end)` |
| `daren.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON daren.quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 45. `daren.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 81 | 2025-01-27 | 2026-08-10 |
| `period_end` | 81 | 2025-02-02 | 2026-08-16 |
| `updated_at` | 81 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `weekly_refunds_weekly_refund_amount_check` | 检查约束 | `CHECK (weekly_refund_amount >= 0::numeric)` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.weekly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_period_start_period_end_key ON daren.weekly_refunds USING btree (period_start, period_end)` |
| `daren.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON daren.weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 46. `daren.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `week_over_week_rate` | `numeric(20,6)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 81 | 2025-01-27 | 2026-08-10 |
| `period_end` | 81 | 2025-02-02 | 2026-08-16 |
| `updated_at` | 81 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_weekly_transaction_amount_check` | 检查约束 | `CHECK (weekly_transaction_amount >= 0::numeric)` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `daren.weekly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_period_start_period_end_key ON daren.weekly_sales USING btree (period_start, period_end)` |
| `daren.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON daren.weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 47. `doudian.daily_sales_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：543 行 / 147,456 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-11 09:36:12.039112+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_daily_sales_summary_date` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.daily_sales_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_summary_pkey ON doudian.daily_sales_summary USING btree (id)` |
| `doudian.uq_daily_sales_summary_date` | 是 | 否 | `CREATE UNIQUE INDEX uq_daily_sales_summary_date ON doudian.daily_sales_summary USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 48. `doudian.half_year_customer_health`

- **表类型**：普通表
- **估算行数 / 存储大小**：5,247 行 / 2,523,136 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `half_year_purchase_amount` | `numeric(18,2)` | 否 | — |
| `customer_health_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `text` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 5,306 | 2025-02-01 | 2026-08-01 |
| `period_end` | 5,306 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 5,306 | 2026-08-12 16:48:07.608057+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_half_year_customer_health_status` | 检查约束 | `CHECK (customer_health_status = ANY (ARRAY['高活跃'::text, '活跃'::text, '稳定'::text, '观察'::text, '风险'::text, '流失预警'::text, '流失'::text]))` |
| `half_year_customer_health_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_half_year_customer_health_business` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.half_year_customer_health_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_health_pkey ON doudian.half_year_customer_health USING btree (id)` |
| `doudian.uq_half_year_customer_health_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_half_year_customer_health_business ON doudian.half_year_customer_health USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 49. `doudian.half_year_high_frequency_products`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：半年商品频次分析表。
- **表的作用**：按商品周期保存购买数量、金额及频次排名/分位数等分析结果。
- **可以提供的信息**：可提供商品周期交易金额、商品数量、数量排名和频次分位数；排名范围和分位数算法需结合刷新逻辑确认。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quantity_rank` | `bigint` | 否 | — |
| `amount_rank` | `bigint` | 否 | — |
| `selection_type` | `text` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 28 | 2025-02-01 | 2026-08-01 |
| `period_end` | 28 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 28 | 2026-08-11 09:36:12.039112+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_half_year_high_frequency_products_selection` | 检查约束 | `CHECK (selection_type = ANY (ARRAY['quantity_top5'::text, 'amount_top5'::text, 'both'::text]))` |
| `half_year_high_frequency_products_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_half_year_high_frequency_products_business` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.half_year_high_frequency_products_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_high_frequency_products_pkey ON doudian.half_year_high_frequency_products USING btree (id)` |
| `doudian.uq_half_year_high_frequency_products_business` | 是 | 否 | `CREATE UNIQUE INDEX uq_half_year_high_frequency_products_business ON doudian.half_year_high_frequency_products USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 50. `doudian.half_year_refunds_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。
- **数据库注释**：抖店两家店按业务半年合并的退款金额

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-12 17:04:23.635673+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_half_year_refunds_summary_amount` | 检查约束 | `CHECK (half_year_refund_amount >= 0::numeric)` |
| `ck_half_year_refunds_summary_boundary` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_half_year_refunds_summary_period` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.half_year_refunds_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_summary_pkey ON doudian.half_year_refunds_summary USING btree (id)` |
| `doudian.uq_half_year_refunds_summary_period` | 是 | 否 | `CREATE UNIQUE INDEX uq_half_year_refunds_summary_period ON doudian.half_year_refunds_summary USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 51. `doudian.half_year_sales_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-11 09:36:12.039112+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_half_year_sales_summary_period` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.half_year_sales_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_summary_pkey ON doudian.half_year_sales_summary USING btree (id)` |
| `doudian.uq_half_year_sales_summary_period` | 是 | 否 | `CREATE UNIQUE INDEX uq_half_year_sales_summary_period ON doudian.half_year_sales_summary USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 52. `doudian.monthly_refunds_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。
- **数据库注释**：抖店两家店按自然月合并的退款金额

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-12 17:04:23.635673+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_monthly_refunds_summary_amount` | 检查约束 | `CHECK (monthly_refund_amount >= 0::numeric)` |
| `ck_monthly_refunds_summary_boundary` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_monthly_refunds_summary_period` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.monthly_refunds_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_summary_pkey ON doudian.monthly_refunds_summary USING btree (id)` |
| `doudian.uq_monthly_refunds_summary_period` | 是 | 否 | `CREATE UNIQUE INDEX uq_monthly_refunds_summary_period ON doudian.monthly_refunds_summary USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 53. `doudian.monthly_sales_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 09:36:12.039112+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_monthly_sales_summary_boundary` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_monthly_sales_summary_period` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.monthly_sales_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_summary_pkey ON doudian.monthly_sales_summary USING btree (id)` |
| `doudian.uq_monthly_sales_summary_period` | 是 | 否 | `CREATE UNIQUE INDEX uq_monthly_sales_summary_period ON doudian.monthly_sales_summary USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 54. `doudian.quarterly_refunds_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。
- **数据库注释**：抖店两家店按业务季度合并的退款金额

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-12 17:04:23.635673+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_quarterly_refunds_summary_amount` | 检查约束 | `CHECK (quarterly_refund_amount >= 0::numeric)` |
| `ck_quarterly_refunds_summary_boundary` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_quarterly_refunds_summary_period` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.quarterly_refunds_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_summary_pkey ON doudian.quarterly_refunds_summary USING btree (id)` |
| `doudian.uq_quarterly_refunds_summary_period` | 是 | 否 | `CREATE UNIQUE INDEX uq_quarterly_refunds_summary_period ON doudian.quarterly_refunds_summary USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 55. `doudian.quarterly_sales_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-11 09:36:12.039112+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_quarterly_sales_summary_period` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.quarterly_sales_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_summary_pkey ON doudian.quarterly_sales_summary USING btree (id)` |
| `doudian.uq_quarterly_sales_summary_period` | 是 | 否 | `CREATE UNIQUE INDEX uq_quarterly_sales_summary_period ON doudian.quarterly_sales_summary USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 56. `doudian.weekly_refunds_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。
- **数据库注释**：抖店两家店按自然周合并的退款金额

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-12 17:04:23.635673+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_weekly_refunds_summary_amount` | 检查约束 | `CHECK (weekly_refund_amount >= 0::numeric)` |
| `ck_weekly_refunds_summary_boundary` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_refunds_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_weekly_refunds_summary_period` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.uq_weekly_refunds_summary_period` | 是 | 否 | `CREATE UNIQUE INDEX uq_weekly_refunds_summary_period ON doudian.weekly_refunds_summary USING btree (period_start, period_end)` |
| `doudian.weekly_refunds_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_summary_pkey ON doudian.weekly_refunds_summary USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 57. `doudian.weekly_sales_summary`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 09:36:12.039112+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_weekly_sales_summary_boundary` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_summary_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_weekly_sales_summary_period` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `doudian.uq_weekly_sales_summary_period` | 是 | 否 | `CREATE UNIQUE INDEX uq_weekly_sales_summary_period ON doudian.weekly_sales_summary USING btree (period_start, period_end)` |
| `doudian.weekly_sales_summary_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_summary_pkey ON doudian.weekly_sales_summary USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 58. `doudianChildren.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：101,513 行 / 18,948,096 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 102,436 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 102,436 | 2026-08-10 20:29:03.071489+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_daily_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ALL (ARRAY[''::text, '-'::text]))` |
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_daily_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_product_sales_business_uk ON "doudianChildren".customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |
| `"doudianChildren".customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON "doudianChildren".customer_daily_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 59. `doudianChildren.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：31,324 行 / 4,481,024 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 31,498 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 31,498 | 2026-08-10 20:34:32.37666+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_daily_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_business_uk ON "doudianChildren".customer_daily_sales USING btree (customer_id, transaction_date)` |
| `"doudianChildren".customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON "doudianChildren".customer_daily_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 60. `doudianChildren.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：31,324 行 / 4,997,120 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 31,498 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 31,498 | 2026-08-10 20:34:32.37666+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_daily_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_business_uk ON "doudianChildren".customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |
| `"doudianChildren".customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON "doudianChildren".customer_daily_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 61. `doudianChildren.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：40,585 行 / 8,134,656 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 40,899 | 2025-02-01 | 2026-08-01 |
| `period_end` | 40,899 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 40,899 | 2026-08-10 20:29:03.071489+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_half_year_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_half_year_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ALL (ARRAY[''::text, '-'::text]))` |
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_half_year_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_product_sales_business_uk ON "doudianChildren".customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `"doudianChildren".customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON "doudianChildren".customer_half_year_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 62. `doudianChildren.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：5,237 行 / 942,080 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 5,296 | 2025-02-01 | 2026-08-01 |
| `period_end` | 5,296 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 5,296 | 2026-08-11 13:59:44.09254+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_half_year_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_half_year_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_sales_business_uk ON "doudianChildren".customer_half_year_sales USING btree (customer_id, period_start, period_end)` |
| `"doudianChildren".customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON "doudianChildren".customer_half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 63. `doudianChildren.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：5,237 行 / 2,613,248 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `half_year_purchase_amount` | `numeric(18,2)` | 否 | — |
| `customer_health_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `text` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 5,296 | 2025-02-01 | 2026-08-01 |
| `period_end` | 5,296 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 5,296 | 2026-08-12 16:48:07.608057+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_health_detail_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_health_detail_score_check` | 检查约束 | `CHECK (customer_health_score >= 0.00 AND customer_health_score <= 100.00)` |
| `customer_health_detail_status_check` | 检查约束 | `CHECK (customer_health_status = ANY (ARRAY['高活跃'::text, '活跃'::text, '稳定'::text, '观察'::text, '风险'::text, '流失预警'::text, '流失'::text]))` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_health_detail_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_business_uk ON "doudianChildren".customer_health_detail USING btree (customer_id, period_start, period_end)` |
| `"doudianChildren".customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON "doudianChildren".customer_health_detail USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 64. `doudianChildren.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：5,250 行 / 851,968 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `customer_nickname` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 5,255 | 2026-08-10 17:28:25.273644+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_id_mapping_customer_check` | 检查约束 | `CHECK (btrim(customer_id, ' 	  '::text) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_id_mapping_business_uk` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_id_mapping_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_business_uk ON "doudianChildren".customer_id_mapping USING btree (customer_id)` |
| `"doudianChildren".customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON "doudianChildren".customer_id_mapping USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 65. `doudianChildren.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：53,395 行 / 10,600,448 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 53,726 | 2025-02-01 | 2026-08-01 |
| `period_end` | 53,726 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 53,726 | 2026-08-10 20:29:03.071489+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_monthly_product_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `customer_monthly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ALL (ARRAY[''::text, '-'::text]))` |
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_monthly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_product_sales_business_uk ON "doudianChildren".customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `"doudianChildren".customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON "doudianChildren".customer_monthly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 66. `doudianChildren.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：8,617 行 / 1,474,560 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 8,680 | 2025-02-01 | 2026-08-01 |
| `period_end` | 8,680 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 8,680 | 2026-08-11 13:59:44.09254+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_monthly_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_monthly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_sales_business_uk ON "doudianChildren".customer_monthly_sales USING btree (customer_id, period_start, period_end)` |
| `"doudianChildren".customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON "doudianChildren".customer_monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 67. `doudianChildren.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：44,825 行 / 8,945,664 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 45,146 | 2025-02-01 | 2026-08-01 |
| `period_end` | 45,146 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 45,146 | 2026-08-10 20:29:03.071489+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_quarterly_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `customer_quarterly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ALL (ARRAY[''::text, '-'::text]))` |
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_quarterly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_business_uk ON "doudianChildren".customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `"doudianChildren".customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON "doudianChildren".customer_quarterly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 68. `doudianChildren.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：6,256 行 / 1,105,920 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 6,318 | 2025-02-01 | 2026-08-01 |
| `period_end` | 6,318 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 6,318 | 2026-08-11 13:59:44.09254+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_quarterly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_quarterly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_sales_business_uk ON "doudianChildren".customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |
| `"doudianChildren".customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON "doudianChildren".customer_quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 69. `doudianChildren.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：14,879 行 / 2,473,984 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 14,944 | 2025-01-27 | 2026-08-03 |
| `period_end` | 14,944 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 14,944 | 2026-08-11 13:59:44.09254+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_weekly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_weekly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".customer_weekly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_weekly_sales_business_uk ON "doudianChildren".customer_weekly_sales USING btree (customer_id, period_start, period_end)` |
| `"doudianChildren".customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON "doudianChildren".customer_weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 70. `doudianChildren.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：31,324 行 / 4,505,600 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 31,498 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 31,498 | 2026-08-10 20:34:32.37666+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_customer_sales_business_uk` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".daily_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_customer_sales_business_uk ON "doudianChildren".daily_customer_sales USING btree (transaction_date, customer_id)` |
| `"doudianChildren".daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON "doudianChildren".daily_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 71. `doudianChildren.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：104,275 行 / 16,392,192 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 105,278 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 105,278 | 2026-08-10 20:35:41.086165+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_product_sales_business_uk` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".daily_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_product_sales_business_uk ON "doudianChildren".daily_product_sales USING btree (transaction_date, product_code)` |
| `"doudianChildren".daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON "doudianChildren".daily_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 72. `doudianChildren.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：543 行 / 131,072 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_business_uk` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".daily_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_business_uk ON "doudianChildren".daily_sales USING btree (transaction_date)` |
| `"doudianChildren".daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON "doudianChildren".daily_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 73. `doudianChildren.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：543 行 / 147,456 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".daily_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_metrics_business_uk ON "doudianChildren".daily_sales_metrics USING btree (transaction_date)` |
| `"doudianChildren".daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON "doudianChildren".daily_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 74. `doudianChildren.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：5,237 行 / 901,120 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 5,296 | 2025-02-01 | 2026-08-01 |
| `period_end` | 5,296 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 5,296 | 2026-08-10 20:34:32.37666+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `half_year_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".half_year_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_customer_sales_business_uk ON "doudianChildren".half_year_customer_sales USING btree (period_start, period_end, customer_id)` |
| `"doudianChildren".half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON "doudianChildren".half_year_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 75. `doudianChildren.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：8,619 行 / 1,581,056 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 8,908 | 2025-02-01 | 2026-08-01 |
| `period_end` | 8,908 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 8,908 | 2026-08-10 20:35:41.086165+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".half_year_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_sales_business_uk ON "doudianChildren".half_year_product_sales USING btree (period_start, period_end, product_code)` |
| `"doudianChildren".half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON "doudianChildren".half_year_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 76. `doudianChildren.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".half_year_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_business_uk ON "doudianChildren".half_year_refunds USING btree (period_start, period_end)` |
| `"doudianChildren".half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON "doudianChildren".half_year_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 77. `doudianChildren.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".half_year_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_business_uk ON "doudianChildren".half_year_sales USING btree (period_start, period_end)` |
| `"doudianChildren".half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON "doudianChildren".half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 78. `doudianChildren.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：8,617 行 / 1,400,832 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 8,680 | 2025-02-01 | 2026-08-01 |
| `period_end` | 8,680 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 8,680 | 2026-08-10 20:34:32.37666+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `monthly_customer_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".monthly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_customer_sales_business_uk ON "doudianChildren".monthly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `"doudianChildren".monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON "doudianChildren".monthly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 79. `doudianChildren.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：21,811 行 / 3,629,056 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 22,123 | 2025-02-01 | 2026-08-01 |
| `period_end` | 22,123 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 22,123 | 2026-08-10 20:35:41.086165+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".monthly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_sales_business_uk ON "doudianChildren".monthly_product_sales USING btree (period_start, period_end, product_code)` |
| `"doudianChildren".monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON "doudianChildren".monthly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 80. `doudianChildren.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".monthly_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_business_uk ON "doudianChildren".monthly_refunds USING btree (period_start, period_end)` |
| `"doudianChildren".monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON "doudianChildren".monthly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 81. `doudianChildren.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".monthly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_business_uk ON "doudianChildren".monthly_sales USING btree (period_start, period_end)` |
| `"doudianChildren".monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON "doudianChildren".monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 82. `doudianChildren.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".monthly_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_metrics_business_uk ON "doudianChildren".monthly_sales_metrics USING btree (period_start, period_end)` |
| `"doudianChildren".monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON "doudianChildren".monthly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 83. `doudianChildren.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：6,256 行 / 1,048,576 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 6,318 | 2025-02-01 | 2026-08-01 |
| `period_end` | 6,318 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 6,318 | 2026-08-10 20:34:32.37666+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `quarterly_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".quarterly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_customer_sales_business_uk ON "doudianChildren".quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `"doudianChildren".quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON "doudianChildren".quarterly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 84. `doudianChildren.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：12,396 行 / 2,162,688 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 12,690 | 2025-02-01 | 2026-08-01 |
| `period_end` | 12,690 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 12,690 | 2026-08-10 20:35:41.086165+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".quarterly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_sales_business_uk ON "doudianChildren".quarterly_product_sales USING btree (period_start, period_end, product_code)` |
| `"doudianChildren".quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON "doudianChildren".quarterly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 85. `doudianChildren.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".quarterly_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_business_uk ON "doudianChildren".quarterly_refunds USING btree (period_start, period_end)` |
| `"doudianChildren".quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON "doudianChildren".quarterly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 86. `doudianChildren.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".quarterly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_business_uk ON "doudianChildren".quarterly_sales USING btree (period_start, period_end)` |
| `"doudianChildren".quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON "doudianChildren".quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 87. `doudianChildren.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：879,791 行 / 771,047,424 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：由表内主键及业务字段共同确定。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `主订单编号` | `text` | 是 | — |
| `子订单编号` | `text` | 是 | — |
| `选购商品` | `text` | 是 | — |
| `商品规格` | `text` | 是 | — |
| `商品数量` | `text` | 是 | — |
| `商品ID` | `text` | 是 | — |
| `商家编码` | `text` | 是 | — |
| `商品单价` | `text` | 是 | — |
| `订单应付金额` | `text` | 是 | — |
| `运费` | `text` | 是 | — |
| `优惠总金额` | `text` | 是 | — |
| `平台优惠` | `text` | 是 | — |
| `商家优惠` | `text` | 是 | — |
| `达人优惠` | `text` | 是 | — |
| `商家改价` | `text` | 是 | — |
| `支付优惠` | `text` | 是 | — |
| `红包抵扣` | `text` | 是 | — |
| `支付方式` | `text` | 是 | — |
| `手续费` | `text` | 是 | — |
| `收件人` | `text` | 是 | — |
| `收件人手机号` | `text` | 是 | — |
| `省` | `text` | 是 | — |
| `市` | `text` | 是 | — |
| `区` | `text` | 是 | — |
| `街道` | `text` | 是 | — |
| `详细地址` | `text` | 是 | — |
| `是否修改过地址` | `text` | 是 | — |
| `地址修改时段` | `text` | 是 | — |
| `买家留言` | `text` | 是 | — |
| `订单提交时间` | `text` | 是 | — |
| `旗帜颜色` | `text` | 是 | — |
| `商家备注` | `text` | 是 | — |
| `订单完成时间` | `text` | 是 | — |
| `支付完成时间` | `text` | 是 | — |
| `APP渠道` | `text` | 是 | — |
| `流量来源` | `text` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `承诺发货时间` | `text` | 是 | — |
| `订单类型` | `text` | 是 | — |
| `鲁班落地页ID` | `text` | 是 | — |
| `达人ID` | `text` | 是 | — |
| `达人昵称` | `text` | 是 | — |
| `所属门店ID` | `text` | 是 | — |
| `售后状态` | `text` | 是 | — |
| `取消原因` | `text` | 是 | — |
| `预约发货时间` | `text` | 是 | — |
| `仓库ID` | `text` | 是 | — |
| `仓库名称` | `text` | 是 | — |
| `序列号` | `text` | 是 | — |
| `是否安心购` | `text` | 是 | — |
| `广告渠道` | `text` | 是 | — |
| `流量类型` | `text` | 是 | — |
| `流量体裁` | `text` | 是 | — |
| `流量渠道` | `text` | 是 | — |
| `发货主体` | `text` | 是 | — |
| `发货主体明细` | `text` | 是 | — |
| `发货时间` | `text` | 是 | — |
| `降价类优惠` | `text` | 是 | — |
| `平台实际承担优惠金额` | `text` | 是 | — |
| `商家实际承担优惠金额` | `text` | 是 | — |
| `达人实际承担优惠金额` | `text` | 是 | — |
| `预计送达时间` | `text` | 是 | — |
| `是否平台仓自流转` | `text` | 是 | — |
| `车型` | `text` | 是 | — |
| `商品69码` | `text` | 是 | — |
| `发货SN码` | `text` | 是 | — |
| `发货IMEI码1` | `text` | 是 | — |
| `发货IMEI码2` | `text` | 是 | — |
| `是否福袋订单` | `text` | 是 | — |
| `福袋采购订单号` | `text` | 是 | — |
| `福袋采购订单状态` | `text` | 是 | — |
| `福袋采购订单采购方ID` | `text` | 是 | — |
| `福袋采购订单采购方名称` | `text` | 是 | — |
| `福袋采购订单创建时间` | `text` | 是 | — |
| `福袋采购订单支付时间` | `text` | 是 | — |
| `福袋采购订单订单应付金额` | `text` | 是 | — |
| `福袋采购订单订单实际付款金额` | `text` | 是 | — |
| `福袋采购订单订单优惠金额` | `text` | 是 | — |
| `福袋采购订单商品ID` | `text` | 是 | — |
| `福袋采购订单商品名称` | `text` | 是 | — |
| `福袋采购订单商品单价` | `text` | 是 | — |
| `福袋采购订单商品数量` | `text` | 是 | — |
| `福袋采购订单核销数量` | `text` | 是 | — |
| `福袋采购订单结算数量` | `text` | 是 | — |
| `预约送达时间` | `text` | 是 | — |
| `建议发货时间（起）` | `text` | 是 | — |
| `建议发货时间（止）` | `text` | 是 | — |
| `物流SN码` | `text` | 是 | — |
| `物流IMEI码1` | `text` | 是 | — |
| `物流IMEI码2` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 892,634 | 2026-08-10 17:22:54.072635+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".raw_data_aftersale_status_idx` | 否 | 否 | `CREATE INDEX raw_data_aftersale_status_idx ON "doudianChildren".raw_data USING btree ("售后状态")` |
| `"doudianChildren".raw_data_influencer_id_idx` | 否 | 否 | `CREATE INDEX raw_data_influencer_id_idx ON "doudianChildren".raw_data USING btree ("达人ID")` |
| `"doudianChildren".raw_data_merchant_code_idx` | 否 | 否 | `CREATE INDEX raw_data_merchant_code_idx ON "doudianChildren".raw_data USING btree ("商家编码")` |
| `"doudianChildren".raw_data_order_status_idx` | 否 | 否 | `CREATE INDEX raw_data_order_status_idx ON "doudianChildren".raw_data USING btree ("订单状态")` |
| `"doudianChildren".raw_data_payment_completed_at_idx` | 否 | 否 | `CREATE INDEX raw_data_payment_completed_at_idx ON "doudianChildren".raw_data USING btree ("支付完成时间")` |
| `"doudianChildren".raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON "doudianChildren".raw_data USING btree (id)` |
| `"doudianChildren".raw_data_sub_order_number_idx` | 否 | 否 | `CREATE INDEX raw_data_sub_order_number_idx ON "doudianChildren".raw_data USING btree ("子订单编号")` |
| `"doudianChildren".raw_data_traffic_source_idx` | 否 | 否 | `CREATE INDEX raw_data_traffic_source_idx ON "doudianChildren".raw_data USING btree ("流量来源")` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 88. `doudianChildren.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：14,879 行 / 2,351,104 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 14,944 | 2025-01-27 | 2026-08-03 |
| `period_end` | 14,944 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 14,944 | 2026-08-10 20:34:32.37666+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `weekly_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".weekly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_customer_sales_business_uk ON "doudianChildren".weekly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `"doudianChildren".weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON "doudianChildren".weekly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 89. `doudianChildren.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：46,623 行 / 7,512,064 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 46,962 | 2025-01-27 | 2026-08-03 |
| `period_end` | 46,962 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 46,962 | 2026-08-10 20:35:41.086165+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".weekly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_product_sales_business_uk ON "doudianChildren".weekly_product_sales USING btree (period_start, period_end, product_code)` |
| `"doudianChildren".weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON "doudianChildren".weekly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 90. `doudianChildren.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".weekly_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_business_uk ON "doudianChildren".weekly_refunds USING btree (period_start, period_end)` |
| `"doudianChildren".weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON "doudianChildren".weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 91. `doudianChildren.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".weekly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_business_uk ON "doudianChildren".weekly_sales USING btree (period_start, period_end)` |
| `"doudianChildren".weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON "doudianChildren".weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 92. `doudianChildren.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianChildren.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-10 20:37:58.090379+08 | 2026-08-13 14:03:16.80945+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianChildren".weekly_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_metrics_business_uk ON "doudianChildren".weekly_sales_metrics USING btree (period_start, period_end)` |
| `"doudianChildren".weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON "doudianChildren".weekly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 93. `doudianKocotree.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,519 行 / 1,056,768 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 3,519 | 2026-01-21 | 2026-07-29 |
| `updated_at` | 3,519 | 2026-08-10 21:59:31.088828+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_daily_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ALL (ARRAY[''::text, '-'::text]))` |
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_daily_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_product_sales_business_uk ON "doudianKocotree".customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |
| `"doudianKocotree".customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON "doudianKocotree".customer_daily_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 94. `doudianKocotree.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：272 行 / 147,456 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 329 | 2026-01-21 | 2026-07-29 |
| `updated_at` | 329 | 2026-08-10 21:59:31.335822+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_daily_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_business_uk ON "doudianKocotree".customer_daily_sales USING btree (customer_id, transaction_date)` |
| `"doudianKocotree".customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON "doudianKocotree".customer_daily_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 95. `doudianKocotree.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：272 行 / 155,648 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 329 | 2026-01-21 | 2026-07-29 |
| `updated_at` | 329 | 2026-08-10 21:59:31.335822+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_daily_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_business_uk ON "doudianKocotree".customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |
| `"doudianKocotree".customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON "doudianKocotree".customer_daily_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 96. `doudianKocotree.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：525 行 / 294,912 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 525 | 2025-08-01 | 2026-02-01 |
| `period_end` | 525 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 525 | 2026-08-10 21:59:31.088828+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_half_year_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_half_year_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ALL (ARRAY[''::text, '-'::text]))` |
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_half_year_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_product_sales_business_uk ON "doudianKocotree".customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `"doudianKocotree".customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON "doudianKocotree".customer_half_year_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 97. `doudianKocotree.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-08-01 | 2026-02-01 |
| `period_end` | 19 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 19 | 2026-08-11 13:59:44.09254+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_half_year_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_half_year_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_sales_business_uk ON "doudianKocotree".customer_half_year_sales USING btree (customer_id, period_start, period_end)` |
| `"doudianKocotree".customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON "doudianKocotree".customer_half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 98. `doudianKocotree.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：19 行 / 81,920 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `half_year_purchase_amount` | `numeric(18,2)` | 否 | — |
| `customer_health_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `text` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-08-01 | 2026-02-01 |
| `period_end` | 19 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 19 | 2026-08-12 16:48:07.608057+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_health_detail_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_health_detail_score_check` | 检查约束 | `CHECK (customer_health_score >= 0.00 AND customer_health_score <= 100.00)` |
| `customer_health_detail_status_check` | 检查约束 | `CHECK (customer_health_status = ANY (ARRAY['高活跃'::text, '活跃'::text, '稳定'::text, '观察'::text, '风险'::text, '流失预警'::text, '流失'::text]))` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_health_detail_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_business_uk ON "doudianKocotree".customer_health_detail USING btree (customer_id, period_start, period_end)` |
| `"doudianKocotree".customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON "doudianKocotree".customer_health_detail USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 99. `doudianKocotree.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `customer_nickname` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 21 | 2026-08-10 21:47:34.447075+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `ck_dk_customer_id_mapping_customer_id` | 检查约束 | `CHECK (btrim(customer_id, ' 	  '::text) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `uq_dk_customer_id_mapping_customer_id` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON "doudianKocotree".customer_id_mapping USING btree (id)` |
| `"doudianKocotree".uq_dk_customer_id_mapping_customer_id` | 是 | 否 | `CREATE UNIQUE INDEX uq_dk_customer_id_mapping_customer_id ON "doudianKocotree".customer_id_mapping USING btree (customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 100. `doudianKocotree.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,004 行 / 385,024 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,004 | 2026-01-01 | 2026-07-01 |
| `period_end` | 1,004 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 1,004 | 2026-08-10 21:59:31.088828+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_monthly_product_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `customer_monthly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ALL (ARRAY[''::text, '-'::text]))` |
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_monthly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_product_sales_business_uk ON "doudianKocotree".customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `"doudianKocotree".customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON "doudianKocotree".customer_monthly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 101. `doudianKocotree.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：33 行 / 49,152 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 35 | 2026-01-01 | 2026-07-01 |
| `period_end` | 35 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 35 | 2026-08-11 13:59:44.09254+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_monthly_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_monthly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_sales_business_uk ON "doudianKocotree".customer_monthly_sales USING btree (customer_id, period_start, period_end)` |
| `"doudianKocotree".customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON "doudianKocotree".customer_monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 102. `doudianKocotree.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：672 行 / 376,832 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 672 | 2025-11-01 | 2026-05-01 |
| `period_end` | 672 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 672 | 2026-08-10 21:59:31.088828+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_quarterly_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `customer_quarterly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ALL (ARRAY[''::text, '-'::text]))` |
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_quarterly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_business_uk ON "doudianKocotree".customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `"doudianKocotree".customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON "doudianKocotree".customer_quarterly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 103. `doudianKocotree.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 24 | 2025-11-01 | 2026-05-01 |
| `period_end` | 24 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 24 | 2026-08-11 13:59:44.09254+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_quarterly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_quarterly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_sales_business_uk ON "doudianKocotree".customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |
| `"doudianKocotree".customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON "doudianKocotree".customer_quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 104. `doudianKocotree.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：77 行 / 90,112 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 85 | 2026-01-19 | 2026-07-27 |
| `period_end` | 85 | 2026-01-25 | 2026-08-02 |
| `updated_at` | 85 | 2026-08-11 13:59:44.09254+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `customer_weekly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_weekly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".customer_weekly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_weekly_sales_business_uk ON "doudianKocotree".customer_weekly_sales USING btree (customer_id, period_start, period_end)` |
| `"doudianKocotree".customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON "doudianKocotree".customer_weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 105. `doudianKocotree.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：272 行 / 147,456 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 329 | 2026-01-21 | 2026-07-29 |
| `updated_at` | 329 | 2026-08-10 21:59:31.335822+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_customer_sales_business_uk` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".daily_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_customer_sales_business_uk ON "doudianKocotree".daily_customer_sales USING btree (transaction_date, customer_id)` |
| `"doudianKocotree".daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON "doudianKocotree".daily_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 106. `doudianKocotree.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：4,634 行 / 1,048,576 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 4,634 | 2026-01-21 | 2026-07-29 |
| `updated_at` | 4,634 | 2026-08-10 21:59:31.332861+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_product_sales_business_uk` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".daily_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_product_sales_business_uk ON "doudianKocotree".daily_product_sales USING btree (transaction_date, product_code)` |
| `"doudianKocotree".daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON "doudianKocotree".daily_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 107. `doudianKocotree.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：145 行 / 73,728 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 174 | 2026-01-21 | 2026-07-29 |
| `updated_at` | 174 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_business_uk` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".daily_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_business_uk ON "doudianKocotree".daily_sales USING btree (transaction_date)` |
| `"doudianKocotree".daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON "doudianKocotree".daily_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 108. `doudianKocotree.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：145 行 / 73,728 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 174 | 2026-01-21 | 2026-07-29 |
| `updated_at` | 174 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".daily_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_metrics_business_uk ON "doudianKocotree".daily_sales_metrics USING btree (transaction_date)` |
| `"doudianKocotree".daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON "doudianKocotree".daily_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 109. `doudianKocotree.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-08-01 | 2026-02-01 |
| `period_end` | 19 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 19 | 2026-08-10 21:59:31.335822+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `half_year_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".half_year_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_customer_sales_business_uk ON "doudianKocotree".half_year_customer_sales USING btree (period_start, period_end, customer_id)` |
| `"doudianKocotree".half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON "doudianKocotree".half_year_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 110. `doudianKocotree.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：673 行 / 286,720 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 673 | 2025-08-01 | 2026-02-01 |
| `period_end` | 673 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 673 | 2026-08-10 21:59:31.332861+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".half_year_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_sales_business_uk ON "doudianKocotree".half_year_product_sales USING btree (period_start, period_end, product_code)` |
| `"doudianKocotree".half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON "doudianKocotree".half_year_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 111. `doudianKocotree.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2 | 2025-08-01 | 2026-02-01 |
| `period_end` | 2 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 2 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".half_year_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_business_uk ON "doudianKocotree".half_year_refunds USING btree (period_start, period_end)` |
| `"doudianKocotree".half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON "doudianKocotree".half_year_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 112. `doudianKocotree.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2 | 2025-08-01 | 2026-02-01 |
| `period_end` | 2 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 2 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".half_year_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_business_uk ON "doudianKocotree".half_year_sales USING btree (period_start, period_end)` |
| `"doudianKocotree".half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON "doudianKocotree".half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 113. `doudianKocotree.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 35 | 2026-01-01 | 2026-07-01 |
| `period_end` | 35 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 35 | 2026-08-10 21:59:31.335822+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `monthly_customer_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".monthly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_customer_sales_business_uk ON "doudianKocotree".monthly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `"doudianKocotree".monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON "doudianKocotree".monthly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 114. `doudianKocotree.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,284 行 / 360,448 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,284 | 2026-01-01 | 2026-07-01 |
| `period_end` | 1,284 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 1,284 | 2026-08-10 21:59:31.332861+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".monthly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_sales_business_uk ON "doudianKocotree".monthly_product_sales USING btree (period_start, period_end, product_code)` |
| `"doudianKocotree".monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON "doudianKocotree".monthly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 115. `doudianKocotree.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2026-01-01 | 2026-07-01 |
| `period_end` | 7 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 7 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".monthly_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_business_uk ON "doudianKocotree".monthly_refunds USING btree (period_start, period_end)` |
| `"doudianKocotree".monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON "doudianKocotree".monthly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 116. `doudianKocotree.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2026-01-01 | 2026-07-01 |
| `period_end` | 7 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 7 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".monthly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_business_uk ON "doudianKocotree".monthly_sales USING btree (period_start, period_end)` |
| `"doudianKocotree".monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON "doudianKocotree".monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 117. `doudianKocotree.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2026-01-01 | 2026-07-01 |
| `period_end` | 7 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 7 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".monthly_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_metrics_business_uk ON "doudianKocotree".monthly_sales_metrics USING btree (period_start, period_end)` |
| `"doudianKocotree".monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON "doudianKocotree".monthly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 118. `doudianKocotree.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 24 | 2025-11-01 | 2026-05-01 |
| `period_end` | 24 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 24 | 2026-08-10 21:59:31.335822+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `quarterly_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".quarterly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_customer_sales_business_uk ON "doudianKocotree".quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `"doudianKocotree".quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON "doudianKocotree".quarterly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 119. `doudianKocotree.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：860 行 / 327,680 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 860 | 2025-11-01 | 2026-05-01 |
| `period_end` | 860 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 860 | 2026-08-10 21:59:31.332861+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".quarterly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_sales_business_uk ON "doudianKocotree".quarterly_product_sales USING btree (period_start, period_end, product_code)` |
| `"doudianKocotree".quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON "doudianKocotree".quarterly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 120. `doudianKocotree.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3 | 2025-11-01 | 2026-05-01 |
| `period_end` | 3 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 3 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".quarterly_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_business_uk ON "doudianKocotree".quarterly_refunds USING btree (period_start, period_end)` |
| `"doudianKocotree".quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON "doudianKocotree".quarterly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 121. `doudianKocotree.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3 | 2025-11-01 | 2026-05-01 |
| `period_end` | 3 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 3 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".quarterly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_business_uk ON "doudianKocotree".quarterly_sales USING btree (period_start, period_end)` |
| `"doudianKocotree".quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON "doudianKocotree".quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 122. `doudianKocotree.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：43,679 行 / 37,183,488 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：由表内主键及业务字段共同确定。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `主订单编号` | `text` | 是 | — |
| `子订单编号` | `text` | 是 | — |
| `选购商品` | `text` | 是 | — |
| `商品规格` | `text` | 是 | — |
| `商品数量` | `text` | 是 | — |
| `商品ID` | `text` | 是 | — |
| `商家编码` | `text` | 是 | — |
| `商品单价` | `text` | 是 | — |
| `订单应付金额` | `text` | 是 | — |
| `运费` | `text` | 是 | — |
| `优惠总金额` | `text` | 是 | — |
| `平台优惠` | `text` | 是 | — |
| `商家优惠` | `text` | 是 | — |
| `达人优惠` | `text` | 是 | — |
| `商家改价` | `text` | 是 | — |
| `支付优惠` | `text` | 是 | — |
| `红包抵扣` | `text` | 是 | — |
| `支付方式` | `text` | 是 | — |
| `手续费` | `text` | 是 | — |
| `收件人` | `text` | 是 | — |
| `收件人手机号` | `text` | 是 | — |
| `省` | `text` | 是 | — |
| `市` | `text` | 是 | — |
| `区` | `text` | 是 | — |
| `街道` | `text` | 是 | — |
| `详细地址` | `text` | 是 | — |
| `是否修改过地址` | `text` | 是 | — |
| `地址修改时段` | `text` | 是 | — |
| `买家留言` | `text` | 是 | — |
| `订单提交时间` | `text` | 是 | — |
| `旗帜颜色` | `text` | 是 | — |
| `商家备注` | `text` | 是 | — |
| `订单完成时间` | `text` | 是 | — |
| `支付完成时间` | `text` | 是 | — |
| `APP渠道` | `text` | 是 | — |
| `流量来源` | `text` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `承诺发货时间` | `text` | 是 | — |
| `订单类型` | `text` | 是 | — |
| `鲁班落地页ID` | `text` | 是 | — |
| `达人ID` | `text` | 是 | — |
| `达人昵称` | `text` | 是 | — |
| `所属门店ID` | `text` | 是 | — |
| `售后状态` | `text` | 是 | — |
| `取消原因` | `text` | 是 | — |
| `预约发货时间` | `text` | 是 | — |
| `仓库ID` | `text` | 是 | — |
| `仓库名称` | `text` | 是 | — |
| `是否安心购` | `text` | 是 | — |
| `广告渠道` | `text` | 是 | — |
| `流量类型` | `text` | 是 | — |
| `流量体裁` | `text` | 是 | — |
| `流量渠道` | `text` | 是 | — |
| `发货主体` | `text` | 是 | — |
| `发货主体明细` | `text` | 是 | — |
| `发货时间` | `text` | 是 | — |
| `降价类优惠` | `text` | 是 | — |
| `平台实际承担优惠金额` | `text` | 是 | — |
| `商家实际承担优惠金额` | `text` | 是 | — |
| `达人实际承担优惠金额` | `text` | 是 | — |
| `预计送达时间` | `text` | 是 | — |
| `是否平台仓自流转` | `text` | 是 | — |
| `车型` | `text` | 是 | — |
| `商品69码` | `text` | 是 | — |
| `发货SN码` | `text` | 是 | — |
| `发货IMEI码1` | `text` | 是 | — |
| `发货IMEI码2` | `text` | 是 | — |
| `预约送达时间` | `text` | 是 | — |
| `建议发货时间（起）` | `text` | 是 | — |
| `建议发货时间（止）` | `text` | 是 | — |
| `物流SN码` | `text` | 是 | — |
| `物流IMEI码1` | `text` | 是 | — |
| `物流IMEI码2` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 43,679 | 2026-08-10 21:47:34.447075+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".ix_dk_raw_aftersale_status` | 否 | 否 | `CREATE INDEX ix_dk_raw_aftersale_status ON "doudianKocotree".raw_data USING btree ("售后状态")` |
| `"doudianKocotree".ix_dk_raw_main_order` | 否 | 否 | `CREATE INDEX ix_dk_raw_main_order ON "doudianKocotree".raw_data USING btree ("主订单编号")` |
| `"doudianKocotree".ix_dk_raw_order_status` | 否 | 否 | `CREATE INDEX ix_dk_raw_order_status ON "doudianKocotree".raw_data USING btree ("订单状态")` |
| `"doudianKocotree".ix_dk_raw_payment_time` | 否 | 否 | `CREATE INDEX ix_dk_raw_payment_time ON "doudianKocotree".raw_data USING btree ("支付完成时间")` |
| `"doudianKocotree".ix_dk_raw_product_code` | 否 | 否 | `CREATE INDEX ix_dk_raw_product_code ON "doudianKocotree".raw_data USING btree ("商家编码")` |
| `"doudianKocotree".ix_dk_raw_talent_id` | 否 | 否 | `CREATE INDEX ix_dk_raw_talent_id ON "doudianKocotree".raw_data USING btree ("达人ID")` |
| `"doudianKocotree".ix_dk_raw_traffic_source` | 否 | 否 | `CREATE INDEX ix_dk_raw_traffic_source ON "doudianKocotree".raw_data USING btree ("流量来源")` |
| `"doudianKocotree".raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON "doudianKocotree".raw_data USING btree (id)` |
| `"doudianKocotree".uq_dk_raw_suborder_normalized` | 是 | 否 | `CREATE UNIQUE INDEX uq_dk_raw_suborder_normalized ON "doudianKocotree".raw_data USING btree (btrim("子订单编号", ' 	  '::text)) WHERE (btrim(COALESCE("子订单编号", ''::text), ' 	  '::text) <> ALL (ARRAY[''::text, '-'::text]))` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 123. `doudianKocotree.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：77 行 / 81,920 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 85 | 2026-01-19 | 2026-07-27 |
| `period_end` | 85 | 2026-01-25 | 2026-08-02 |
| `updated_at` | 85 | 2026-08-10 21:59:31.335822+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_customer_check` | 检查约束 | `CHECK (btrim(customer_id) <> ALL (ARRAY[''::text, '-'::text, '0'::text, '0.0'::text]))` |
| `weekly_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".weekly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_customer_sales_business_uk ON "doudianKocotree".weekly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `"doudianKocotree".weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON "doudianKocotree".weekly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 124. `doudianKocotree.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,368 行 / 581,632 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2,368 | 2026-01-19 | 2026-07-27 |
| `period_end` | 2,368 | 2026-01-25 | 2026-08-02 |
| `updated_at` | 2,368 | 2026-08-10 21:59:31.332861+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_product_sales_product_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text AND btrim(product_code) <> '-'::text)` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".weekly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_product_sales_business_uk ON "doudianKocotree".weekly_product_sales USING btree (period_start, period_end, product_code)` |
| `"doudianKocotree".weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON "doudianKocotree".weekly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 125. `doudianKocotree.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 29 | 2026-01-12 | 2026-07-27 |
| `period_end` | 29 | 2026-01-18 | 2026-08-02 |
| `updated_at` | 29 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".weekly_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_business_uk ON "doudianKocotree".weekly_refunds USING btree (period_start, period_end)` |
| `"doudianKocotree".weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON "doudianKocotree".weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 126. `doudianKocotree.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 28 | 2026-01-19 | 2026-07-27 |
| `period_end` | 28 | 2026-01-25 | 2026-08-02 |
| `updated_at` | 28 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".weekly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_business_uk ON "doudianKocotree".weekly_sales USING btree (period_start, period_end)` |
| `"doudianKocotree".weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON "doudianKocotree".weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 127. `doudianKocotree.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `doudianKocotree.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 28 | 2026-01-19 | 2026-07-27 |
| `period_end` | 28 | 2026-01-25 | 2026-08-02 |
| `updated_at` | 28 | 2026-08-10 21:59:31.09224+08 | 2026-08-13 13:13:31.059906+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `"doudianKocotree".weekly_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_metrics_business_uk ON "doudianKocotree".weekly_sales_metrics USING btree (period_start, period_end)` |
| `"doudianKocotree".weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON "doudianKocotree".weekly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 128. `fenxiao.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：6,779,287 行 / 3,674,374,144 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `health_grain` | `text` | 否 | — |
| `source_platform` | `text` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `week_purchase_count` | `numeric(20,4)` | 是 | — |
| `month_purchase_count` | `numeric(20,4)` | 是 | — |
| `half_year_purchase_count` | `bigint` | 是 | — |
| `half_year_purchase_amount` | `numeric(20,2)` | 是 | — |
| `week_score` | `numeric(10,2)` | 是 | — |
| `month_score` | `numeric(10,2)` | 是 | — |
| `customer_score` | `numeric(10,2)` | 否 | — |
| `customer_health_status` | `text` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 6,781,022 | 2025-01-27 | 2026-08-03 |
| `period_end` | 6,781,022 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 6,781,022 | 2026-08-12 16:48:13.743476+08 | 2026-08-14 09:30:14.143058+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `customer_health_detail_customer_health_status_check` | 检查约束 | `CHECK (customer_health_status = ANY (ARRAY['高活跃'::text, '活跃'::text, '稳定'::text, '观察'::text, '风险'::text, '流失预警'::text, '流失'::text]))` |
| `customer_health_detail_customer_score_check` | 检查约束 | `CHECK (customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_health_grain_check` | 检查约束 | `CHECK (health_grain = ANY (ARRAY['natural_week'::text, 'business_half_year'::text]))` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `fenxiao_customer_health_business_key` | 唯一约束 | `UNIQUE (health_grain, source_platform, customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON fenxiao.customer_health_detail USING btree (id)` |
| `fenxiao.fenxiao_customer_health_business_key` | 是 | 否 | `CREATE UNIQUE INDEX fenxiao_customer_health_business_key ON fenxiao.customer_health_detail USING btree (health_grain, source_platform, customer_id, period_start, period_end)` |
| `fenxiao.fenxiao_customer_health_customer_idx` | 否 | 否 | `CREATE INDEX fenxiao_customer_health_customer_idx ON fenxiao.customer_health_detail USING btree (customer_id)` |
| `fenxiao.fenxiao_customer_health_non_loss_status_idx` | 否 | 否 | `CREATE INDEX fenxiao_customer_health_non_loss_status_idx ON fenxiao.customer_health_detail USING btree (customer_health_status) WHERE (customer_health_status <> '流失'::text)` |
| `fenxiao.fenxiao_customer_health_period_status_idx` | 否 | 否 | `CREATE INDEX fenxiao_customer_health_period_status_idx ON fenxiao.customer_health_detail USING btree (period_start, period_end, customer_health_status)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 129. `fenxiao.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：549 行 / 221,184 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(20,2)` | 否 | — |
| `year_over_year_rate` | `numeric(20,6)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(20,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_rolling_30_day_transaction_amount_check` | 检查约束 | `CHECK (rolling_30_day_transaction_amount >= 0::numeric)` |
| `daily_sales_rolling_7_day_transaction_amount_check` | 检查约束 | `CHECK (rolling_7_day_transaction_amount >= 0::numeric)` |
| `daily_sales_transaction_amount_check` | 检查约束 | `CHECK (transaction_amount >= 0::numeric)` |
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_transaction_date_key` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON fenxiao.daily_sales USING btree (id)` |
| `fenxiao.daily_sales_transaction_date_key` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_transaction_date_key ON fenxiao.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 130. `fenxiao.half_year_high_frequency_products`

- **表类型**：普通表
- **估算行数 / 存储大小**：35 行 / 90,112 bytes
- **推断功能**：半年商品频次分析表。
- **表的作用**：按商品周期保存购买数量、金额及频次排名/分位数等分析结果。
- **可以提供的信息**：可提供商品周期交易金额、商品数量、数量排名和频次分位数；排名范围和分位数算法需结合刷新逻辑确认。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_product_quantity` | `numeric(20,4)` | 否 | — |
| `half_year_transaction_amount` | `numeric(20,2)` | 否 | — |
| `quantity_rank` | `bigint` | 否 | — |
| `amount_rank` | `bigint` | 否 | — |
| `selection_type` | `text` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 35 | 2025-02-01 | 2026-08-01 |
| `period_end` | 35 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 35 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_high_frequency_pro_half_year_transaction_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_high_frequency_produ_half_year_product_quantity_check` | 检查约束 | `CHECK (half_year_product_quantity >= 0::numeric)` |
| `half_year_high_frequency_products_amount_rank_check` | 检查约束 | `CHECK (amount_rank > 0)` |
| `half_year_high_frequency_products_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_high_frequency_products_quantity_rank_check` | 检查约束 | `CHECK (quantity_rank > 0)` |
| `half_year_high_frequency_products_selection_type_check` | 检查约束 | `CHECK (selection_type = ANY (ARRAY['quantity_top5'::text, 'amount_top5'::text, 'both'::text]))` |
| `half_year_high_frequency_products_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_high_frequency_prod_period_start_period_end_produ_key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.half_year_high_frequency_prod_period_start_period_end_produ_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_high_frequency_prod_period_start_period_end_produ_key ON fenxiao.half_year_high_frequency_products USING btree (period_start, period_end, product_code)` |
| `fenxiao.half_year_high_frequency_products_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_high_frequency_products_pkey ON fenxiao.half_year_high_frequency_products USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 131. `fenxiao.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：3 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_refunds_half_year_refund_amount_check` | 检查约束 | `CHECK (half_year_refund_amount >= 0::numeric)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.half_year_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_period_start_period_end_key ON fenxiao.half_year_refunds USING btree (period_start, period_end)` |
| `fenxiao.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON fenxiao.half_year_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 132. `fenxiao.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_sales_half_year_transaction_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.half_year_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_period_start_period_end_key ON fenxiao.half_year_sales USING btree (period_start, period_end)` |
| `fenxiao.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON fenxiao.half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 133. `fenxiao.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：18 行 / 73,728 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `monthly_refunds_monthly_refund_amount_check` | 检查约束 | `CHECK (monthly_refund_amount >= 0::numeric)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.monthly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_period_start_period_end_key ON fenxiao.monthly_refunds USING btree (period_start, period_end)` |
| `fenxiao.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON fenxiao.monthly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 134. `fenxiao.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：18 行 / 73,728 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `month_over_month_rate` | `numeric(20,6)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `monthly_sales_monthly_transaction_amount_check` | 检查约束 | `CHECK (monthly_transaction_amount >= 0::numeric)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.monthly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_period_start_period_end_key ON fenxiao.monthly_sales USING btree (period_start, period_end)` |
| `fenxiao.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON fenxiao.monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 135. `fenxiao.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：7 行 / 73,728 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `quarterly_refunds_quarterly_refund_amount_check` | 检查约束 | `CHECK (quarterly_refund_amount >= 0::numeric)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.quarterly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_period_start_period_end_key ON fenxiao.quarterly_refunds USING btree (period_start, period_end)` |
| `fenxiao.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON fenxiao.quarterly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 136. `fenxiao.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：7 行 / 73,728 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `quarterly_sales_quarterly_transaction_amount_check` | 检查约束 | `CHECK (quarterly_transaction_amount >= 0::numeric)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.quarterly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_period_start_period_end_key ON fenxiao.quarterly_sales USING btree (period_start, period_end)` |
| `fenxiao.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON fenxiao.quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 137. `fenxiao.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 81,920 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `weekly_refunds_weekly_refund_amount_check` | 检查约束 | `CHECK (weekly_refund_amount >= 0::numeric)` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.weekly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_period_start_period_end_key ON fenxiao.weekly_refunds USING btree (period_start, period_end)` |
| `fenxiao.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON fenxiao.weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 138. `fenxiao.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 81,920 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `week_over_week_rate` | `numeric(20,6)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_weekly_transaction_amount_check` | 检查约束 | `CHECK (weekly_transaction_amount >= 0::numeric)` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `fenxiao.weekly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_period_start_period_end_key ON fenxiao.weekly_sales USING btree (period_start, period_end)` |
| `fenxiao.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON fenxiao.weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 139. `jushuitan.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：342,473 行 / 71,475,200 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 342,473 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 342,473 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON jushuitan.customer_daily_product_sales USING btree (id)` |
| `jushuitan.customer_daily_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_product_sales_uq ON jushuitan.customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 140. `jushuitan.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：13,730 行 / 2,973,696 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 13,730 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 13,730 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON jushuitan.customer_daily_sales USING btree (id)` |
| `jushuitan.customer_daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_uq ON jushuitan.customer_daily_sales USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 141. `jushuitan.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：13,730 行 / 4,374,528 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 13,730 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 13,730 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_metrics_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON jushuitan.customer_daily_sales_metrics USING btree (id)` |
| `jushuitan.customer_daily_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_uq ON jushuitan.customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 142. `jushuitan.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：66,022 行 / 16,334,848 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 66,022 | 2025-02-01 | 2026-08-01 |
| `period_end` | 66,022 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 66,022 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON jushuitan.customer_half_year_product_sales USING btree (id)` |
| `jushuitan.customer_half_year_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_product_sales_uq ON jushuitan.customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 143. `jushuitan.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：327 行 / 155,648 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 327 | 2025-02-01 | 2026-08-01 |
| `period_end` | 327 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 327 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_count_ck` | 检查约束 | `CHECK (half_year_purchase_count >= 0)` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON jushuitan.customer_half_year_sales USING btree (id)` |
| `jushuitan.customer_half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_sales_uq ON jushuitan.customer_half_year_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 144. `jushuitan.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：9,024 行 / 4,612,096 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `week_period_start` | `date` | 否 | — |
| `week_period_end` | `date` | 否 | — |
| `month_period_start` | `date` | 否 | — |
| `month_period_end` | `date` | 否 | — |
| `week_purchase_count` | `integer` | 否 | — |
| `week_score` | `numeric(5,2)` | 否 | — |
| `month_purchase_count` | `numeric(10,2)` | 否 | — |
| `month_score` | `numeric(5,2)` | 否 | — |
| `customer_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `character varying(50)` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 9,102 | 2025-01-27 | 2026-08-03 |
| `period_end` | 9,102 | 2025-02-02 | 2026-08-09 |
| `week_period_start` | 9,102 | 2025-01-27 | 2026-08-03 |
| `week_period_end` | 9,102 | 2025-02-02 | 2026-08-09 |
| `month_period_start` | 9,102 | 2025-01-01 | 2026-08-01 |
| `month_period_end` | 9,102 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 9,102 | 2026-08-12 16:48:13.743476 | 2026-08-14 09:30:14.143058 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_month_count_ck` | 检查约束 | `CHECK (month_purchase_count >= 0::numeric)` |
| `customer_health_detail_period_match_ck` | 检查约束 | `CHECK (period_start = week_period_start AND period_end = week_period_end AND (period_end - period_start) = 6 AND EXTRACT(isodow FROM period_start) = 1::numeric AND EXTRACT(isodow FROM period_end) = 7::numeric)` |
| `customer_health_detail_score_ck` | 检查约束 | `CHECK (week_score >= 0::numeric AND week_score <= 100::numeric AND month_score >= 0::numeric AND month_score <= 100::numeric AND customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_status_ck` | 检查约束 | `CHECK (customer_health_status::text = CASE     WHEN customer_score >= 90::numeric THEN '高活跃'::text     WHEN customer_score >= 80::numeric THEN '活跃'::text     WHEN customer_score >= 70::numeric THEN '稳定'::text     WHEN customer_score >= 60::numeric THEN '观察'::text     WHEN customer_score >= 50::numeric THEN '风险'::text     WHEN customer_score >= 40::numeric THEN '流失预警'::text     ELSE '流失'::text END)` |
| `customer_health_detail_week_count_ck` | 检查约束 | `CHECK (week_purchase_count >= 0)` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON jushuitan.customer_health_detail USING btree (id)` |
| `jushuitan.customer_health_detail_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_uq ON jushuitan.customer_health_detail USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 145. `jushuitan.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：158 行 / 73,728 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 163 | 2026-08-12 10:51:03.90219 | 2026-08-14 09:30:14.143058 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_id_mapping_customer_id_trimmed_chk` | 检查约束 | `CHECK (customer_id::text = btrim(customer_id::text) AND customer_id::text <> ''::text)` |
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_id_mapping_customer_id_key` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_id_mapping_customer_id_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_customer_id_key ON jushuitan.customer_id_mapping USING btree (customer_id)` |
| `jushuitan.customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON jushuitan.customer_id_mapping USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 146. `jushuitan.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：116,641 行 / 28,811,264 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 116,641 | 2025-02-01 | 2026-08-01 |
| `period_end` | 116,641 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 116,641 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON jushuitan.customer_monthly_product_sales USING btree (id)` |
| `jushuitan.customer_monthly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_product_sales_uq ON jushuitan.customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 147. `jushuitan.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,005 行 / 344,064 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,005 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,005 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 1,005 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_count_ck` | 检查约束 | `CHECK (monthly_purchase_count >= 0)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON jushuitan.customer_monthly_sales USING btree (id)` |
| `jushuitan.customer_monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_sales_uq ON jushuitan.customer_monthly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 148. `jushuitan.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：81,352 行 / 19,472,384 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 81,352 | 2025-02-01 | 2026-08-01 |
| `period_end` | 81,352 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 81,352 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON jushuitan.customer_quarterly_product_sales USING btree (id)` |
| `jushuitan.customer_quarterly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_uq ON jushuitan.customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 149. `jushuitan.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：484 行 / 204,800 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 484 | 2025-02-01 | 2026-08-01 |
| `period_end` | 484 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 484 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_count_ck` | 检查约束 | `CHECK (quarterly_purchase_count >= 0)` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON jushuitan.customer_quarterly_sales USING btree (id)` |
| `jushuitan.customer_quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_sales_uq ON jushuitan.customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 150. `jushuitan.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,196 行 / 909,312 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,196 | 2025-01-27 | 2026-08-03 |
| `period_end` | 3,196 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 3,196 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_count_ck` | 检查约束 | `CHECK (weekly_purchase_count >= 0)` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_weekly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON jushuitan.customer_weekly_sales USING btree (id)` |
| `jushuitan.customer_weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_weekly_sales_uq ON jushuitan.customer_weekly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 151. `jushuitan.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：13,730 行 / 3,923,968 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 13,730 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 13,730 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_customer_sales_uq` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.daily_customer_sales_customer_date_idx` | 否 | 否 | `CREATE INDEX daily_customer_sales_customer_date_idx ON jushuitan.daily_customer_sales USING btree (customer_id, transaction_date)` |
| `jushuitan.daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON jushuitan.daily_customer_sales USING btree (id)` |
| `jushuitan.daily_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_customer_sales_uq ON jushuitan.daily_customer_sales USING btree (transaction_date, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 152. `jushuitan.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：236,070 行 / 38,215,680 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 236,070 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 236,070 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_product_sales_uq` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON jushuitan.daily_product_sales USING btree (id)` |
| `jushuitan.daily_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_product_sales_uq ON jushuitan.daily_product_sales USING btree (transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 153. `jushuitan.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：549 行 / 196,608 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON jushuitan.daily_sales USING btree (id)` |
| `jushuitan.daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_uq ON jushuitan.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 154. `jushuitan.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：549 行 / 221,184 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_metrics_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON jushuitan.daily_sales_metrics USING btree (id)` |
| `jushuitan.daily_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_metrics_uq ON jushuitan.daily_sales_metrics USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 155. `jushuitan.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：327 行 / 163,840 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 327 | 2025-02-01 | 2026-08-01 |
| `period_end` | 327 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 327 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON jushuitan.half_year_customer_sales USING btree (id)` |
| `jushuitan.half_year_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_customer_sales_uq ON jushuitan.half_year_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 156. `jushuitan.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：13,639 行 / 3,325,952 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 13,639 | 2025-02-01 | 2026-08-01 |
| `period_end` | 13,639 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 13,639 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON jushuitan.half_year_product_sales USING btree (id)` |
| `jushuitan.half_year_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_sales_uq ON jushuitan.half_year_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 157. `jushuitan.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON jushuitan.half_year_refunds USING btree (id)` |
| `jushuitan.half_year_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_uq ON jushuitan.half_year_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 158. `jushuitan.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON jushuitan.half_year_sales USING btree (id)` |
| `jushuitan.half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_uq ON jushuitan.half_year_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 159. `jushuitan.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,005 行 / 352,256 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,005 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,005 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 1,005 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON jushuitan.monthly_customer_sales USING btree (id)` |
| `jushuitan.monthly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_customer_sales_uq ON jushuitan.monthly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 160. `jushuitan.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：38,444 行 / 8,372,224 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 38,444 | 2025-02-01 | 2026-08-01 |
| `period_end` | 38,444 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 38,444 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON jushuitan.monthly_product_sales USING btree (id)` |
| `jushuitan.monthly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_sales_uq ON jushuitan.monthly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 161. `jushuitan.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON jushuitan.monthly_refunds USING btree (id)` |
| `jushuitan.monthly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_uq ON jushuitan.monthly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 162. `jushuitan.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON jushuitan.monthly_sales USING btree (id)` |
| `jushuitan.monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_uq ON jushuitan.monthly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 163. `jushuitan.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_metrics_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON jushuitan.monthly_sales_metrics USING btree (id)` |
| `jushuitan.monthly_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_metrics_uq ON jushuitan.monthly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 164. `jushuitan.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：484 行 / 212,992 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 484 | 2025-02-01 | 2026-08-01 |
| `period_end` | 484 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 484 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON jushuitan.quarterly_customer_sales USING btree (id)` |
| `jushuitan.quarterly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_customer_sales_uq ON jushuitan.quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 165. `jushuitan.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：20,213 行 / 4,841,472 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 20,213 | 2025-02-01 | 2026-08-01 |
| `period_end` | 20,213 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 20,213 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON jushuitan.quarterly_product_sales USING btree (id)` |
| `jushuitan.quarterly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_sales_uq ON jushuitan.quarterly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 166. `jushuitan.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON jushuitan.quarterly_refunds USING btree (id)` |
| `jushuitan.quarterly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_uq ON jushuitan.quarterly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 167. `jushuitan.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON jushuitan.quarterly_sales USING btree (id)` |
| `jushuitan.quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_uq ON jushuitan.quarterly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 168. `jushuitan.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：721,212 行 / 648,896,512 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `内部订单号` | `text` | 是 | — |
| `标记多标签` | `text` | 是 | — |
| `售后单号` | `text` | 是 | — |
| `订单类型` | `text` | 是 | — |
| `线上订单号` | `text` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `发货仓` | `text` | 是 | — |
| `虚拟仓` | `text` | 是 | — |
| `分销商` | `text` | 是 | — |
| `店铺` | `text` | 是 | — |
| `小旗` | `text` | 是 | — |
| `买家账号` | `text` | 是 | — |
| `卖家备注` | `text` | 是 | — |
| `订单日期` | `text` | 是 | — |
| `发货日期` | `text` | 是 | — |
| `付款日期` | `text` | 是 | — |
| `业务员` | `text` | 是 | — |
| `收货人` | `text` | 是 | — |
| `省` | `text` | 是 | — |
| `市` | `text` | 是 | — |
| `区县` | `text` | 是 | — |
| `快递公司` | `text` | 是 | — |
| `快递单号` | `text` | 是 | — |
| `售后登记日期` | `text` | 是 | — |
| `售后确认日期` | `text` | 是 | — |
| `售后分类` | `text` | 是 | — |
| `问题类型` | `text` | 是 | — |
| `商品编码` | `text` | 是 | — |
| `款式编码` | `text` | 是 | — |
| `原始线上订单号` | `text` | 是 | — |
| `产品分类` | `text` | 是 | — |
| `品牌` | `text` | 是 | — |
| `商品简称` | `text` | 是 | — |
| `颜色规格` | `text` | 是 | — |
| `供应商` | `text` | 是 | — |
| `店铺商品编码` | `text` | 是 | — |
| `基本售价` | `text` | 是 | — |
| `成本价` | `text` | 是 | — |
| `成本价来源` | `text` | 是 | — |
| `销售数量` | `text` | 是 | — |
| `实发数量` | `text` | 是 | — |
| `实发金额` | `text` | 是 | — |
| `销售金额` | `text` | 是 | — |
| `销售成本` | `text` | 是 | — |
| `实发成本` | `text` | 是 | — |
| `销售毛利` | `text` | 是 | — |
| `销售毛利率` | `text` | 是 | — |
| `已付金额` | `text` | 是 | — |
| `应付金额` | `text` | 是 | — |
| `售价` | `text` | 是 | — |
| `基本金额` | `text` | 是 | — |
| `退货数量` | `text` | 是 | — |
| `实退数量` | `text` | 是 | — |
| `退货金额` | `text` | 是 | — |
| `退货成本` | `text` | 是 | — |
| `实退成本` | `text` | 是 | — |
| `实退金额` | `text` | 是 | — |
| `运费收入` | `text` | 是 | — |
| `运费收入分摊` | `text` | 是 | — |
| `整单运费支出` | `text` | 是 | — |
| `运费支出` | `text` | 是 | — |
| `优惠金额` | `text` | 是 | — |
| `订单重量` | `text` | 是 | — |
| `供销商` | `text` | 是 | — |
| `币种` | `text` | 是 | — |
| `国际单号` | `text` | 是 | — |
| `剩余发货时间` | `text` | 是 | — |
| `付款方式` | `text` | 是 | — |
| `买家指定物流` | `text` | 是 | — |
| `收货国家地区` | `text` | 是 | — |
| `实发物流渠道` | `text` | 是 | — |
| `境外运费支出分摊` | `text` | 是 | — |
| `境外收入总计分摊` | `text` | 是 | — |
| `境外支出总计分摊` | `text` | 是 | — |
| `汇率` | `text` | 是 | — |
| `货主分销` | `text` | 是 | — |
| `平台补贴金额` | `text` | 是 | — |
| `买家ID` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 779,419 | 2026-08-12 10:42:40.778829 | 2026-08-14 09:30:14.143058 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.raw_data_distributor_idx` | 否 | 否 | `CREATE INDEX raw_data_distributor_idx ON jushuitan.raw_data USING btree ("分销商")` |
| `jushuitan.raw_data_order_status_idx` | 否 | 否 | `CREATE INDEX raw_data_order_status_idx ON jushuitan.raw_data USING btree ("订单状态")` |
| `jushuitan.raw_data_payment_date_idx` | 否 | 否 | `CREATE INDEX raw_data_payment_date_idx ON jushuitan.raw_data USING btree ("付款日期")` |
| `jushuitan.raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON jushuitan.raw_data USING btree (id)` |
| `jushuitan.raw_data_product_code_idx` | 否 | 否 | `CREATE INDEX raw_data_product_code_idx ON jushuitan.raw_data USING btree ("商品编码")` |
| `jushuitan.raw_data_store_idx` | 否 | 否 | `CREATE INDEX raw_data_store_idx ON jushuitan.raw_data USING btree ("店铺")` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 169. `jushuitan.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,196 行 / 884,736 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,196 | 2025-01-27 | 2026-08-03 |
| `period_end` | 3,196 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 3,196 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON jushuitan.weekly_customer_sales USING btree (id)` |
| `jushuitan.weekly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_customer_sales_uq ON jushuitan.weekly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 170. `jushuitan.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：93,154 行 / 17,850,368 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 93,154 | 2025-01-27 | 2026-08-03 |
| `period_end` | 93,154 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 93,154 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON jushuitan.weekly_product_sales USING btree (id)` |
| `jushuitan.weekly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_product_sales_uq ON jushuitan.weekly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 171. `jushuitan.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 73,728 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-12 11:07:22.183329 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON jushuitan.weekly_refunds USING btree (id)` |
| `jushuitan.weekly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_uq ON jushuitan.weekly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 172. `jushuitan.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 73,728 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON jushuitan.weekly_sales USING btree (id)` |
| `jushuitan.weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_uq ON jushuitan.weekly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 173. `jushuitan.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 81,920 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `jushuitan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-14 20:47:45.277471 | 2026-08-14 20:47:45.277471 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_metrics_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `jushuitan.weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON jushuitan.weekly_sales_metrics USING btree (id)` |
| `jushuitan.weekly_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_metrics_uq ON jushuitan.weekly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 174. `kuaishouxiaodian.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：33,869 行 / 5,996,544 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 34,110 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 34,110 | 2026-08-10 14:05:28.21605+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_daily_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_product_sales_business_uk ON kuaishouxiaodian.customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |
| `kuaishouxiaodian.customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON kuaishouxiaodian.customer_daily_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 175. `kuaishouxiaodian.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：8,941 行 / 1,261,568 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 9,029 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 9,029 | 2026-08-10 13:20:23.299439+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_customer_date_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_daily_sales_customer_date_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_customer_date_uk ON kuaishouxiaodian.customer_daily_sales USING btree (customer_id, transaction_date)` |
| `kuaishouxiaodian.customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON kuaishouxiaodian.customer_daily_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 176. `kuaishouxiaodian.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：8,941 行 / 1,409,024 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 9,029 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 9,029 | 2026-08-10 13:28:07.443667+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_metrics_customer_date_uk` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_daily_sales_metrics_customer_date_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_customer_date_uk ON kuaishouxiaodian.customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |
| `kuaishouxiaodian.customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON kuaishouxiaodian.customer_daily_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 177. `kuaishouxiaodian.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：11,138 行 / 2,146,304 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 11,250 | 2025-02-01 | 2026-08-01 |
| `period_end` | 11,250 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 11,250 | 2026-08-10 14:05:28.21605+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons'::interval - '1 day'::interval)::date)` |
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_half_year_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_product_sales_business_uk ON kuaishouxiaodian.customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `kuaishouxiaodian.customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON kuaishouxiaodian.customer_half_year_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 178. `kuaishouxiaodian.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,139 行 / 270,336 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,169 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,169 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 1,169 | 2026-08-10 14:05:28.21605+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons'::interval - '1 day'::interval)::date)` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_half_year_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_sales_business_uk ON kuaishouxiaodian.customer_half_year_sales USING btree (customer_id, period_start, period_end)` |
| `kuaishouxiaodian.customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON kuaishouxiaodian.customer_half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 179. `kuaishouxiaodian.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,139 行 / 614,400 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `half_year_purchase_amount` | `numeric(18,2)` | 否 | — |
| `customer_health_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `text` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,169 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,169 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 1,169 | 2026-08-12 16:48:07.608057+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons'::interval - '1 day'::interval)::date)` |
| `customer_health_detail_score_check` | 检查约束 | `CHECK (customer_health_score >= 0::numeric AND customer_health_score <= 100::numeric)` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_health_detail_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_business_uk ON kuaishouxiaodian.customer_health_detail USING btree (customer_id, period_start, period_end)` |
| `kuaishouxiaodian.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON kuaishouxiaodian.customer_health_detail USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 180. `kuaishouxiaodian.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,252 行 / 253,952 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `customer_nickname` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 1,259 | 2026-08-10 09:07:57.235148+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_id_mapping_customer_id_uk` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_id_mapping_customer_id_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_customer_id_uk ON kuaishouxiaodian.customer_id_mapping USING btree (customer_id)` |
| `kuaishouxiaodian.customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON kuaishouxiaodian.customer_id_mapping USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 181. `kuaishouxiaodian.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：16,224 行 / 3,063,808 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 16,354 | 2025-02-01 | 2026-08-01 |
| `period_end` | 16,354 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 16,354 | 2026-08-10 14:05:28.21605+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon'::interval - '1 day'::interval)::date)` |
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_monthly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_product_sales_business_uk ON kuaishouxiaodian.customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `kuaishouxiaodian.customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON kuaishouxiaodian.customer_monthly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 182. `kuaishouxiaodian.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,926 行 / 393,216 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,956 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,956 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 1,956 | 2026-08-10 14:05:28.21605+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon'::interval - '1 day'::interval)::date)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_monthly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_sales_business_uk ON kuaishouxiaodian.customer_monthly_sales USING btree (customer_id, period_start, period_end)` |
| `kuaishouxiaodian.customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON kuaishouxiaodian.customer_monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 183. `kuaishouxiaodian.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：13,135 行 / 2,506,752 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 13,256 | 2025-02-01 | 2026-08-01 |
| `period_end` | 13,256 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 13,256 | 2026-08-10 14:05:28.21605+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons'::interval - '1 day'::interval)::date)` |
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_product_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_quarterly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_business_uk ON kuaishouxiaodian.customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `kuaishouxiaodian.customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON kuaishouxiaodian.customer_quarterly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 184. `kuaishouxiaodian.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,371 行 / 303,104 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,401 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,401 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 1,401 | 2026-08-10 14:05:28.21605+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons'::interval - '1 day'::interval)::date)` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_quarterly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_sales_business_uk ON kuaishouxiaodian.customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |
| `kuaishouxiaodian.customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON kuaishouxiaodian.customer_quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 185. `kuaishouxiaodian.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,650 行 / 647,168 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,679 | 2025-01-27 | 2026-08-03 |
| `period_end` | 3,679 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 3,679 | 2026-08-10 14:05:28.21605+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_weekly_sales_business_uk` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.customer_weekly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX customer_weekly_sales_business_uk ON kuaishouxiaodian.customer_weekly_sales USING btree (customer_id, period_start, period_end)` |
| `kuaishouxiaodian.customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON kuaishouxiaodian.customer_weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 186. `kuaishouxiaodian.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：8,941 行 / 1,261,568 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 9,029 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 9,029 | 2026-08-10 09:59:42.903533+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_customer_sales_date_customer_uk` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.daily_customer_sales_date_customer_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_customer_sales_date_customer_uk ON kuaishouxiaodian.daily_customer_sales USING btree (transaction_date, customer_id)` |
| `kuaishouxiaodian.daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON kuaishouxiaodian.daily_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 187. `kuaishouxiaodian.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：32,550 行 / 5,160,960 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 32,794 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 32,794 | 2026-08-10 09:36:11.387499+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_product_sales_date_product_uk` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.daily_product_sales_date_product_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_product_sales_date_product_uk ON kuaishouxiaodian.daily_product_sales USING btree (transaction_date, product_code)` |
| `kuaishouxiaodian.daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON kuaishouxiaodian.daily_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 188. `kuaishouxiaodian.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：543 行 / 131,072 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-10 09:21:50.247939+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_transaction_date_uk` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON kuaishouxiaodian.daily_sales USING btree (id)` |
| `kuaishouxiaodian.daily_sales_transaction_date_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_transaction_date_uk ON kuaishouxiaodian.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 189. `kuaishouxiaodian.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：543 行 / 139,264 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,2)` | 是 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-10 12:53:10.766005+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_metrics_transaction_date_uk` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON kuaishouxiaodian.daily_sales_metrics USING btree (id)` |
| `kuaishouxiaodian.daily_sales_metrics_transaction_date_uk` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_metrics_transaction_date_uk ON kuaishouxiaodian.daily_sales_metrics USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 190. `kuaishouxiaodian.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,139 行 / 262,144 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,169 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,169 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 1,169 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount > 0::numeric)` |
| `half_year_customer_sales_customer_id_check` | 检查约束 | `CHECK (btrim(customer_id) <> ''::text)` |
| `half_year_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.half_year_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_customer_sales_business_uk ON kuaishouxiaodian.half_year_customer_sales USING btree (period_start, period_end, customer_id)` |
| `kuaishouxiaodian.half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON kuaishouxiaodian.half_year_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 191. `kuaishouxiaodian.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：4,237 行 / 778,240 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4,331 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4,331 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4,331 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount > 0::numeric)` |
| `half_year_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_sales_product_code_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text)` |
| `half_year_product_sales_quantity_check` | 检查约束 | `CHECK (half_year_product_quantity > 0)` |
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.half_year_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_sales_business_uk ON kuaishouxiaodian.half_year_product_sales USING btree (period_start, period_end, product_code)` |
| `kuaishouxiaodian.half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON kuaishouxiaodian.half_year_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 192. `kuaishouxiaodian.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_amount_check` | 检查约束 | `CHECK (half_year_refund_amount > 0::numeric)` |
| `half_year_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.half_year_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_business_uk ON kuaishouxiaodian.half_year_refunds USING btree (period_start, period_end)` |
| `kuaishouxiaodian.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON kuaishouxiaodian.half_year_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 193. `kuaishouxiaodian.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount > 0::numeric)` |
| `half_year_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.half_year_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_business_uk ON kuaishouxiaodian.half_year_sales USING btree (period_start, period_end)` |
| `kuaishouxiaodian.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON kuaishouxiaodian.half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 194. `kuaishouxiaodian.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,926 行 / 376,832 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,956 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,956 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 1,956 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_amount_check` | 检查约束 | `CHECK (monthly_transaction_amount > 0::numeric)` |
| `monthly_customer_sales_customer_id_check` | 检查约束 | `CHECK (btrim(customer_id) <> ''::text)` |
| `monthly_customer_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.monthly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_customer_sales_business_uk ON kuaishouxiaodian.monthly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `kuaishouxiaodian.monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON kuaishouxiaodian.monthly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 195. `kuaishouxiaodian.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：9,044 行 / 1,515,520 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 9,150 | 2025-02-01 | 2026-08-01 |
| `period_end` | 9,150 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 9,150 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_amount_check` | 检查约束 | `CHECK (monthly_transaction_amount > 0::numeric)` |
| `monthly_product_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_product_sales_product_code_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text)` |
| `monthly_product_sales_quantity_check` | 检查约束 | `CHECK (monthly_product_quantity > 0)` |
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.monthly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_sales_business_uk ON kuaishouxiaodian.monthly_product_sales USING btree (period_start, period_end, product_code)` |
| `kuaishouxiaodian.monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON kuaishouxiaodian.monthly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 196. `kuaishouxiaodian.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_amount_check` | 检查约束 | `CHECK (monthly_refund_amount > 0::numeric)` |
| `monthly_refunds_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.monthly_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_business_uk ON kuaishouxiaodian.monthly_refunds USING btree (period_start, period_end)` |
| `kuaishouxiaodian.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON kuaishouxiaodian.monthly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 197. `kuaishouxiaodian.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-10 11:09:52.579086+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_amount_check` | 检查约束 | `CHECK (monthly_transaction_amount > 0::numeric)` |
| `monthly_sales_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (date_trunc('month'::text, period_start::timestamp with time zone) + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.monthly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_business_uk ON kuaishouxiaodian.monthly_sales USING btree (period_start, period_end)` |
| `kuaishouxiaodian.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON kuaishouxiaodian.monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 198. `kuaishouxiaodian.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,2)` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-10 13:13:37.57043+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_period_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon'::interval - '1 day'::interval)::date)` |
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.monthly_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_metrics_business_uk ON kuaishouxiaodian.monthly_sales_metrics USING btree (period_start, period_end)` |
| `kuaishouxiaodian.monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON kuaishouxiaodian.monthly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 199. `kuaishouxiaodian.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,371 行 / 286,720 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,401 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,401 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 1,401 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_amount_check` | 检查约束 | `CHECK (quarterly_transaction_amount > 0::numeric)` |
| `quarterly_customer_sales_customer_id_check` | 检查约束 | `CHECK (btrim(customer_id) <> ''::text)` |
| `quarterly_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.quarterly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_customer_sales_business_uk ON kuaishouxiaodian.quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `kuaishouxiaodian.quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON kuaishouxiaodian.quarterly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 200. `kuaishouxiaodian.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：5,859 行 / 1,032,192 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 5,959 | 2025-02-01 | 2026-08-01 |
| `period_end` | 5,959 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 5,959 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_amount_check` | 检查约束 | `CHECK (quarterly_transaction_amount > 0::numeric)` |
| `quarterly_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_sales_product_code_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text)` |
| `quarterly_product_sales_quantity_check` | 检查约束 | `CHECK (quarterly_product_quantity > 0)` |
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.quarterly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_sales_business_uk ON kuaishouxiaodian.quarterly_product_sales USING btree (period_start, period_end, product_code)` |
| `kuaishouxiaodian.quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON kuaishouxiaodian.quarterly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 201. `kuaishouxiaodian.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_amount_check` | 检查约束 | `CHECK (quarterly_refund_amount > 0::numeric)` |
| `quarterly_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.quarterly_refunds_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_business_uk ON kuaishouxiaodian.quarterly_refunds USING btree (period_start, period_end)` |
| `kuaishouxiaodian.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON kuaishouxiaodian.quarterly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 202. `kuaishouxiaodian.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-10 11:33:08.180113+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_amount_check` | 检查约束 | `CHECK (quarterly_transaction_amount > 0::numeric)` |
| `quarterly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(day FROM period_start) = 1::numeric AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.quarterly_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_business_uk ON kuaishouxiaodian.quarterly_sales USING btree (period_start, period_end)` |
| `kuaishouxiaodian.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON kuaishouxiaodian.quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 203. `kuaishouxiaodian.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：213,351 行 / 128,229,376 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：订单或订单商品记录（以具体订单号/商品编码组合为准）。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `订单号` | `text` | 否 | — |
| `赠品订单号` | `text` | 是 | — |
| `活动订单编号` | `text` | 是 | — |
| `订单创建时间` | `timestamp with time zone` | 否 | — |
| `订单支付时间` | `timestamp with time zone` | 是 | — |
| `预售定金支付时间` | `timestamp with time zone` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `实付款` | `numeric(18,2)` | 是 | — |
| `快递费` | `numeric(18,2)` | 是 | — |
| `店铺优惠` | `numeric(18,2)` | 是 | — |
| `平台补贴` | `numeric(18,2)` | 是 | — |
| `主播补贴` | `numeric(18,2)` | 是 | — |
| `混资活动优惠` | `numeric(18,2)` | 是 | — |
| `支付优惠` | `numeric(18,2)` | 是 | — |
| `支付方式` | `text` | 是 | — |
| `成交数量` | `integer` | 是 | — |
| `买家留言` | `text` | 是 | — |
| `账号类型` | `text` | 是 | — |
| `账号明细` | `text` | 是 | — |
| `订单备注` | `text` | 是 | — |
| `旗帜颜色` | `text` | 是 | — |
| `售后状态` | `text` | 是 | — |
| `活动订单` | `text` | 是 | — |
| `预售/承诺发货时间` | `timestamp with time zone` | 是 | — |
| `订单载体` | `text` | 是 | — |
| `国补类型` | `text` | 是 | — |
| `商品名称` | `text` | 是 | — |
| `商品ID` | `text` | 是 | — |
| `商品规格` | `text` | 是 | — |
| `SKU编码` | `text` | 是 | — |
| `商品单价` | `numeric(18,2)` | 是 | — |
| `渠道` | `text` | 是 | — |
| `CPS达人ID` | `text` | 是 | — |
| `CPS达人昵称` | `text` | 是 | — |
| `预估推广佣金` | `numeric(18,2)` | 是 | — |
| `预估推广者分佣比例` | `numeric(9,6)` | 是 | — |
| `团长ID` | `text` | 是 | — |
| `团长昵称` | `text` | 是 | — |
| `快赚客ID` | `text` | 是 | — |
| `快赚客昵称` | `text` | 是 | — |
| `授权推广者ID` | `text` | 是 | — |
| `授权推广者昵称` | `text` | 是 | — |
| `收货人姓名` | `text` | 是 | — |
| `收货人电话` | `text` | 是 | — |
| `收货地址-省` | `text` | 是 | — |
| `收货地址-市` | `text` | 是 | — |
| `收货地址-区` | `text` | 是 | — |
| `收货地址-街道` | `text` | 是 | — |
| `收货地址` | `text` | 是 | — |
| `发货时间` | `timestamp with time zone` | 是 | — |
| `快递公司` | `text` | 是 | — |
| `快递单号` | `text` | 是 | — |
| `物流信息` | `text` | 是 | — |
| `集运类型` | `text` | 是 | — |
| `直邮类型` | `text` | 是 | — |
| `仓库名称` | `text` | 是 | — |
| `仓库地址` | `text` | 是 | — |
| `发货类型` | `text` | 是 | — |
| `实名姓名` | `text` | 是 | — |
| `服务门店ID` | `text` | 是 | — |
| `服务门店名称` | `text` | 是 | — |
| `服务门店地址` | `text` | 是 | — |
| `国补/类国补/消费券` | `numeric(18,2)` | 是 | — |
| `订单销售主体` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `订单创建时间` | 214,493 | 2025-02-01 12:37:23+08 | 2026-08-03 23:40:22+08 |
| `订单支付时间` | 191,770 | 2025-02-01 12:37:28+08 | 2026-08-03 23:40:23+08 |
| `预售定金支付时间` | 0 | — | — |
| `预售/承诺发货时间` | 191,770 | 2025-02-09 05:58:32+08 | 2026-08-15 11:27:44+08 |
| `发货时间` | 155,833 | 2025-02-01 15:39:28+08 | 2026-08-04 13:56:21+08 |
| `updated_at` | 214,493 | 2026-08-10 08:48:43.118566+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.raw_data_aftersale_status_idx` | 否 | 否 | `CREATE INDEX raw_data_aftersale_status_idx ON kuaishouxiaodian.raw_data USING btree ("售后状态")` |
| `kuaishouxiaodian.raw_data_cps_id_idx` | 否 | 否 | `CREATE INDEX raw_data_cps_id_idx ON kuaishouxiaodian.raw_data USING btree ("CPS达人ID")` |
| `kuaishouxiaodian.raw_data_leader_id_idx` | 否 | 否 | `CREATE INDEX raw_data_leader_id_idx ON kuaishouxiaodian.raw_data USING btree ("团长ID")` |
| `kuaishouxiaodian.raw_data_order_created_at_idx` | 否 | 否 | `CREATE INDEX raw_data_order_created_at_idx ON kuaishouxiaodian.raw_data USING btree ("订单创建时间")` |
| `kuaishouxiaodian.raw_data_order_status_idx` | 否 | 否 | `CREATE INDEX raw_data_order_status_idx ON kuaishouxiaodian.raw_data USING btree ("订单状态")` |
| `kuaishouxiaodian.raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON kuaishouxiaodian.raw_data USING btree (id)` |
| `kuaishouxiaodian.raw_data_sku_code_idx` | 否 | 否 | `CREATE INDEX raw_data_sku_code_idx ON kuaishouxiaodian.raw_data USING btree ("SKU编码")` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 204. `kuaishouxiaodian.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,650 行 / 614,400 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,679 | 2025-01-27 | 2026-08-03 |
| `period_end` | 3,679 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 3,679 | 2026-08-10 11:03:06.348516+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_amount_check` | 检查约束 | `CHECK (weekly_transaction_amount > 0::numeric)` |
| `weekly_customer_sales_customer_id_check` | 检查约束 | `CHECK (btrim(customer_id) <> ''::text)` |
| `weekly_customer_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_customer_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.weekly_customer_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_customer_sales_business_uk ON kuaishouxiaodian.weekly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `kuaishouxiaodian.weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON kuaishouxiaodian.weekly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 205. `kuaishouxiaodian.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：16,932 行 / 2,744,320 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 17,048 | 2025-01-27 | 2026-08-03 |
| `period_end` | 17,048 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 17,048 | 2026-08-10 10:54:23.456794+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_amount_check` | 检查约束 | `CHECK (weekly_transaction_amount > 0::numeric)` |
| `weekly_product_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_product_sales_product_code_check` | 检查约束 | `CHECK (btrim(product_code) <> ''::text)` |
| `weekly_product_sales_quantity_check` | 检查约束 | `CHECK (weekly_product_quantity > 0)` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_product_sales_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.weekly_product_sales_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_product_sales_business_uk ON kuaishouxiaodian.weekly_product_sales USING btree (period_start, period_end, product_code)` |
| `kuaishouxiaodian.weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON kuaishouxiaodian.weekly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 206. `kuaishouxiaodian.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-10 10:45:23.283616+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_refunds_positive_amount_check` | 检查约束 | `CHECK (weekly_refund_amount > 0::numeric)` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_period_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.weekly_refunds_period_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_period_uk ON kuaishouxiaodian.weekly_refunds USING btree (period_start, period_end)` |
| `kuaishouxiaodian.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON kuaishouxiaodian.weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 207. `kuaishouxiaodian.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-10 10:09:40.75555+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_period_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.weekly_sales_period_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_period_uk ON kuaishouxiaodian.weekly_sales USING btree (period_start, period_end)` |
| `kuaishouxiaodian.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON kuaishouxiaodian.weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 208. `kuaishouxiaodian.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：79 行 / 73,728 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaishouxiaodian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,2)` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-10 13:01:58.756631+08 | 2026-08-13 16:42:01.577337+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_period_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_metrics_business_uk` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaishouxiaodian.weekly_sales_metrics_business_uk` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_metrics_business_uk ON kuaishouxiaodian.weekly_sales_metrics USING btree (period_start, period_end)` |
| `kuaishouxiaodian.weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON kuaishouxiaodian.weekly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 209. `kuaituantuan.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：147,949 行 / 26,353,664 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 151,092 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 151,092 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON kuaituantuan.customer_daily_product_sales USING btree (id)` |
| `kuaituantuan.customer_daily_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_product_sales_uq ON kuaituantuan.customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 210. `kuaituantuan.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：10,227 行 / 1,531,904 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 10,382 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 10,382 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON kuaituantuan.customer_daily_sales USING btree (id)` |
| `kuaituantuan.customer_daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_uq ON kuaituantuan.customer_daily_sales USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 211. `kuaituantuan.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：10,227 行 / 1,687,552 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 10,382 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 10,382 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_metrics_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON kuaituantuan.customer_daily_sales_metrics USING btree (id)` |
| `kuaituantuan.customer_daily_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_uq ON kuaituantuan.customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 212. `kuaituantuan.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：47,451 行 / 9,232,384 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 48,584 | 2025-02-01 | 2026-08-01 |
| `period_end` | 48,584 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 48,584 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON kuaituantuan.customer_half_year_product_sales USING btree (id)` |
| `kuaituantuan.customer_half_year_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_product_sales_uq ON kuaituantuan.customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 213. `kuaituantuan.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：362 行 / 114,688 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 362 | 2025-02-01 | 2026-08-01 |
| `period_end` | 362 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 362 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_count_ck` | 检查约束 | `CHECK (half_year_purchase_count >= 0)` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON kuaituantuan.customer_half_year_sales USING btree (id)` |
| `kuaituantuan.customer_half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_sales_uq ON kuaituantuan.customer_half_year_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 214. `kuaituantuan.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：14,352 行 / 7,847,936 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `week_period_start` | `date` | 否 | — |
| `week_period_end` | `date` | 否 | — |
| `month_period_start` | `date` | 否 | — |
| `month_period_end` | `date` | 否 | — |
| `week_purchase_count` | `integer` | 否 | — |
| `week_score` | `numeric(5,2)` | 否 | — |
| `month_purchase_count` | `numeric(10,2)` | 否 | — |
| `month_score` | `numeric(5,2)` | 否 | — |
| `customer_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `character varying(50)` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `week_period_start` | 14,744 | 2025-01-27 | 2026-08-03 |
| `week_period_end` | 14,744 | 2025-02-02 | 2026-08-09 |
| `month_period_start` | 14,744 | 2025-01-01 | 2026-08-01 |
| `month_period_end` | 14,744 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 14,744 | 2026-08-12 16:48:08.037873 | 2026-08-14 11:14:58.217903 |
| `period_start` | 14,744 | 2025-01-27 | 2026-08-03 |
| `period_end` | 14,744 | 2025-02-02 | 2026-08-09 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_month_count_ck` | 检查约束 | `CHECK (month_purchase_count >= 0::numeric)` |
| `customer_health_detail_period_ck` | 检查约束 | `CHECK (period_start = week_period_start AND period_end = week_period_end AND period_end = (period_start + 6))` |
| `customer_health_detail_score_ck` | 检查约束 | `CHECK (week_score >= 0::numeric AND week_score <= 100::numeric AND month_score >= 0::numeric AND month_score <= 100::numeric AND customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_status_ck` | 检查约束 | `CHECK (customer_health_status::text = CASE     WHEN customer_score >= 90::numeric THEN '高活跃'::text     WHEN customer_score >= 80::numeric THEN '活跃'::text     WHEN customer_score >= 70::numeric THEN '稳定'::text     WHEN customer_score >= 60::numeric THEN '观察'::text     WHEN customer_score >= 50::numeric THEN '风险'::text     WHEN customer_score >= 40::numeric THEN '流失预警'::text     ELSE '流失'::text END)` |
| `customer_health_detail_week_count_ck` | 检查约束 | `CHECK (week_purchase_count >= 0)` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_uq` | 唯一约束 | `UNIQUE (customer_id, week_period_start, week_period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON kuaituantuan.customer_health_detail USING btree (id)` |
| `kuaituantuan.customer_health_detail_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_uq ON kuaituantuan.customer_health_detail USING btree (customer_id, week_period_start, week_period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 215. `kuaituantuan.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：184 行 / 73,728 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 188 | 2026-08-11 14:41:53.014125 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_id_mapping_customer_id_key` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_id_mapping_customer_id_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_customer_id_key ON kuaituantuan.customer_id_mapping USING btree (customer_id)` |
| `kuaituantuan.customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON kuaituantuan.customer_id_mapping USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 216. `kuaituantuan.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：76,008 行 / 14,581,760 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 77,464 | 2025-02-01 | 2026-08-01 |
| `period_end` | 77,464 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 77,464 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON kuaituantuan.customer_monthly_product_sales USING btree (id)` |
| `kuaituantuan.customer_monthly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_product_sales_uq ON kuaituantuan.customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 217. `kuaituantuan.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,068 行 / 245,760 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,093 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,093 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 1,093 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_count_ck` | 检查约束 | `CHECK (monthly_purchase_count >= 0)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON kuaituantuan.customer_monthly_sales USING btree (id)` |
| `kuaituantuan.customer_monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_sales_uq ON kuaituantuan.customer_monthly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 218. `kuaituantuan.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：56,382 行 / 10,878,976 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 57,570 | 2025-02-01 | 2026-08-01 |
| `period_end` | 57,570 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 57,570 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON kuaituantuan.customer_quarterly_product_sales USING btree (id)` |
| `kuaituantuan.customer_quarterly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_uq ON kuaituantuan.customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 219. `kuaituantuan.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：535 行 / 155,648 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 560 | 2025-02-01 | 2026-08-01 |
| `period_end` | 560 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 560 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_count_ck` | 检查约束 | `CHECK (quarterly_purchase_count >= 0)` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON kuaituantuan.customer_quarterly_sales USING btree (id)` |
| `kuaituantuan.customer_quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_sales_uq ON kuaituantuan.customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 220. `kuaituantuan.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,838 行 / 532,480 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2,886 | 2025-01-27 | 2026-08-03 |
| `period_end` | 2,886 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 2,886 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_count_ck` | 检查约束 | `CHECK (weekly_purchase_count >= 0)` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_weekly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON kuaituantuan.customer_weekly_sales USING btree (id)` |
| `kuaituantuan.customer_weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_weekly_sales_uq ON kuaituantuan.customer_weekly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 221. `kuaituantuan.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：10,227 行 / 1,949,696 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 10,382 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 10,382 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_customer_sales_uq` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.daily_customer_sales_customer_date_idx` | 否 | 否 | `CREATE INDEX daily_customer_sales_customer_date_idx ON kuaituantuan.daily_customer_sales USING btree (customer_id, transaction_date)` |
| `kuaituantuan.daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON kuaituantuan.daily_customer_sales USING btree (id)` |
| `kuaituantuan.daily_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_customer_sales_uq ON kuaituantuan.daily_customer_sales USING btree (transaction_date, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 222. `kuaituantuan.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：106,183 行 / 16,179,200 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 108,760 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 108,760 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_product_sales_uq` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON kuaituantuan.daily_product_sales USING btree (id)` |
| `kuaituantuan.daily_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_product_sales_uq ON kuaituantuan.daily_product_sales USING btree (transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 223. `kuaituantuan.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：541 行 / 131,072 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON kuaituantuan.daily_sales USING btree (id)` |
| `kuaituantuan.daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_uq ON kuaituantuan.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 224. `kuaituantuan.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：541 行 / 147,456 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_metrics_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON kuaituantuan.daily_sales_metrics USING btree (id)` |
| `kuaituantuan.daily_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_metrics_uq ON kuaituantuan.daily_sales_metrics USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 225. `kuaituantuan.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：362 行 / 114,688 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 362 | 2025-02-01 | 2026-08-01 |
| `period_end` | 362 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 362 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON kuaituantuan.half_year_customer_sales USING btree (id)` |
| `kuaituantuan.half_year_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_customer_sales_uq ON kuaituantuan.half_year_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 226. `kuaituantuan.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：9,601 行 / 1,712,128 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 9,601 | 2025-02-01 | 2026-08-01 |
| `period_end` | 9,601 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 9,601 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON kuaituantuan.half_year_product_sales USING btree (id)` |
| `kuaituantuan.half_year_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_sales_uq ON kuaituantuan.half_year_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 227. `kuaituantuan.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON kuaituantuan.half_year_refunds USING btree (id)` |
| `kuaituantuan.half_year_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_uq ON kuaituantuan.half_year_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 228. `kuaituantuan.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON kuaituantuan.half_year_sales USING btree (id)` |
| `kuaituantuan.half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_uq ON kuaituantuan.half_year_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 229. `kuaituantuan.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,068 行 / 245,760 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,093 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,093 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 1,093 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON kuaituantuan.monthly_customer_sales USING btree (id)` |
| `kuaituantuan.monthly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_customer_sales_uq ON kuaituantuan.monthly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 230. `kuaituantuan.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：24,519 行 / 4,104,192 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 25,176 | 2025-02-01 | 2026-08-01 |
| `period_end` | 25,176 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 25,176 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON kuaituantuan.monthly_product_sales USING btree (id)` |
| `kuaituantuan.monthly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_sales_uq ON kuaituantuan.monthly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 231. `kuaituantuan.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON kuaituantuan.monthly_refunds USING btree (id)` |
| `kuaituantuan.monthly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_uq ON kuaituantuan.monthly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 232. `kuaituantuan.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON kuaituantuan.monthly_sales USING btree (id)` |
| `kuaituantuan.monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_uq ON kuaituantuan.monthly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 233. `kuaituantuan.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_metrics_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON kuaituantuan.monthly_sales_metrics USING btree (id)` |
| `kuaituantuan.monthly_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_metrics_uq ON kuaituantuan.monthly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 234. `kuaituantuan.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：535 行 / 163,840 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 560 | 2025-02-01 | 2026-08-01 |
| `period_end` | 560 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 560 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON kuaituantuan.quarterly_customer_sales USING btree (id)` |
| `kuaituantuan.quarterly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_customer_sales_uq ON kuaituantuan.quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 235. `kuaituantuan.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：13,570 行 / 2,342,912 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 13,570 | 2025-02-01 | 2026-08-01 |
| `period_end` | 13,570 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 13,570 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON kuaituantuan.quarterly_product_sales USING btree (id)` |
| `kuaituantuan.quarterly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_sales_uq ON kuaituantuan.quarterly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 236. `kuaituantuan.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON kuaituantuan.quarterly_refunds USING btree (id)` |
| `kuaituantuan.quarterly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_uq ON kuaituantuan.quarterly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 237. `kuaituantuan.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON kuaituantuan.quarterly_sales USING btree (id)` |
| `kuaituantuan.quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_uq ON kuaituantuan.quarterly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 238. `kuaituantuan.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：243,427 行 / 342,376,448 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：订单或订单商品记录（以具体订单号/商品编码组合为准）。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `跟团号` | `text` | 是 | — |
| `订单号` | `text` | 是 | — |
| `子订单号` | `text` | 是 | — |
| `创单时间` | `text` | 是 | — |
| `支付时间` | `text` | 是 | — |
| `团长` | `text` | 是 | — |
| `团购标题` | `text` | 是 | — |
| `商品名称` | `text` | 是 | — |
| `规格` | `text` | 是 | — |
| `数量` | `text` | 是 | — |
| `商品编码` | `text` | 是 | — |
| `商品金额` | `text` | 是 | — |
| `运费` | `text` | 是 | — |
| `已退款+退款中` | `text` | 是 | — |
| `退款时间` | `text` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `支付状态` | `text` | 是 | — |
| `支付单号` | `text` | 是 | — |
| `服务消费单号` | `text` | 是 | — |
| `物流单号（必填）` | `text` | 是 | — |
| `物流公司（必填）` | `text` | 是 | — |
| `发出物流时间` | `text` | 是 | — |
| `最后更新时间` | `text` | 是 | — |
| `最后更新物流` | `text` | 是 | — |
| `团员备注` | `text` | 是 | — |
| `团长备注` | `text` | 是 | — |
| `供货商备注` | `text` | 是 | — |
| `下单人` | `text` | 是 | — |
| `收货人` | `text` | 是 | — |
| `联系电话` | `text` | 是 | — |
| `打码手机号` | `text` | 是 | — |
| `省` | `text` | 是 | — |
| `市` | `text` | 是 | — |
| `区` | `text` | 是 | — |
| `详细地址` | `text` | 是 | — |
| `选择如遇缺货无法发出是否直接退` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 257,718 | 2026-08-11 14:34:00.495215 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON kuaituantuan.raw_data USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 239. `kuaituantuan.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,838 行 / 524,288 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2,886 | 2025-01-27 | 2026-08-03 |
| `period_end` | 2,886 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 2,886 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON kuaituantuan.weekly_customer_sales USING btree (id)` |
| `kuaituantuan.weekly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_customer_sales_uq ON kuaituantuan.weekly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 240. `kuaituantuan.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：51,381 行 / 8,200,192 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 52,638 | 2025-01-27 | 2026-08-03 |
| `period_end` | 52,638 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 52,638 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON kuaituantuan.weekly_product_sales USING btree (id)` |
| `kuaituantuan.weekly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_product_sales_uq ON kuaituantuan.weekly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 241. `kuaituantuan.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON kuaituantuan.weekly_refunds USING btree (id)` |
| `kuaituantuan.weekly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_uq ON kuaituantuan.weekly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 242. `kuaituantuan.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON kuaituantuan.weekly_sales USING btree (id)` |
| `kuaituantuan.weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_uq ON kuaituantuan.weekly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 243. `kuaituantuan.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `kuaituantuan.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 15:02:56.848372 | 2026-08-14 11:14:58.217903 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_metrics_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `kuaituantuan.weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON kuaituantuan.weekly_sales_metrics USING btree (id)` |
| `kuaituantuan.weekly_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_metrics_uq ON kuaituantuan.weekly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 244. `muyinqijian.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：11,260 行 / 2,482,176 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 11,314 | 2025-11-01 | 2026-08-03 |
| `updated_at` | 11,314 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON muyinqijian.customer_daily_product_sales USING btree (id)` |
| `muyinqijian.customer_daily_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_product_sales_uq ON muyinqijian.customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 245. `muyinqijian.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：766 行 / 196,608 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 843 | 2025-11-01 | 2026-08-03 |
| `updated_at` | 843 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON muyinqijian.customer_daily_sales USING btree (id)` |
| `muyinqijian.customer_daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_uq ON muyinqijian.customer_daily_sales USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 246. `muyinqijian.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：843 行 / 221,184 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 843 | 2025-11-01 | 2026-08-03 |
| `updated_at` | 843 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_metrics_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON muyinqijian.customer_daily_sales_metrics USING btree (id)` |
| `muyinqijian.customer_daily_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_uq ON muyinqijian.customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 247. `muyinqijian.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,274 行 / 950,272 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,274 | 2025-08-01 | 2026-08-01 |
| `period_end` | 3,274 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 3,274 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON muyinqijian.customer_half_year_product_sales USING btree (id)` |
| `muyinqijian.customer_half_year_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_product_sales_uq ON muyinqijian.customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 248. `muyinqijian.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：57 行 / 73,728 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 57 | 2025-08-01 | 2026-08-01 |
| `period_end` | 57 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 57 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_count_ck` | 检查约束 | `CHECK (half_year_purchase_count >= 0)` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON muyinqijian.customer_half_year_sales USING btree (id)` |
| `muyinqijian.customer_half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_sales_uq ON muyinqijian.customer_half_year_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 249. `muyinqijian.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：464 行 / 294,912 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `week_period_start` | `date` | 否 | — |
| `week_period_end` | `date` | 否 | — |
| `month_period_start` | `date` | 否 | — |
| `month_period_end` | `date` | 否 | — |
| `week_purchase_count` | `integer` | 否 | — |
| `week_score` | `numeric(5,2)` | 否 | — |
| `month_purchase_count` | `numeric(10,2)` | 否 | — |
| `month_score` | `numeric(5,2)` | 否 | — |
| `customer_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `character varying(50)` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `week_period_start` | 468 | 2025-10-27 | 2026-08-03 |
| `week_period_end` | 468 | 2025-11-02 | 2026-08-09 |
| `month_period_start` | 468 | 2025-10-01 | 2026-08-01 |
| `month_period_end` | 468 | 2025-11-30 | 2026-08-31 |
| `updated_at` | 468 | 2026-08-12 16:48:08.037873 | 2026-08-14 12:47:23.685472 |
| `period_start` | 468 | 2025-10-27 | 2026-08-03 |
| `period_end` | 468 | 2025-11-02 | 2026-08-09 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_month_count_ck` | 检查约束 | `CHECK (month_purchase_count >= 0::numeric)` |
| `customer_health_detail_period_match_ck` | 检查约束 | `CHECK (period_start = week_period_start AND period_end = week_period_end AND (period_end - period_start) = 6 AND EXTRACT(isodow FROM period_start) = 1::numeric AND EXTRACT(isodow FROM period_end) = 7::numeric)` |
| `customer_health_detail_score_ck` | 检查约束 | `CHECK (week_score >= 0::numeric AND week_score <= 100::numeric AND month_score >= 0::numeric AND month_score <= 100::numeric AND customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_status_ck` | 检查约束 | `CHECK (customer_health_status::text = CASE     WHEN customer_score >= 90::numeric THEN '高活跃'::text     WHEN customer_score >= 80::numeric THEN '活跃'::text     WHEN customer_score >= 70::numeric THEN '稳定'::text     WHEN customer_score >= 60::numeric THEN '观察'::text     WHEN customer_score >= 50::numeric THEN '风险'::text     WHEN customer_score >= 40::numeric THEN '流失预警'::text     ELSE '流失'::text END)` |
| `customer_health_detail_week_count_ck` | 检查约束 | `CHECK (week_purchase_count >= 0)` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON muyinqijian.customer_health_detail USING btree (id)` |
| `muyinqijian.customer_health_detail_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_uq ON muyinqijian.customer_health_detail USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 250. `muyinqijian.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 42 | 2026-08-11 16:20:07.297966 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_id_mapping_customer_id_uq` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_id_mapping_customer_id_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_customer_id_uq ON muyinqijian.customer_id_mapping USING btree (customer_id)` |
| `muyinqijian.customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON muyinqijian.customer_id_mapping USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 251. `muyinqijian.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：4,846 行 / 1,236,992 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4,793 | 2025-11-01 | 2026-08-01 |
| `period_end` | 4,793 | 2025-11-30 | 2026-08-31 |
| `updated_at` | 4,793 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON muyinqijian.customer_monthly_product_sales USING btree (id)` |
| `muyinqijian.customer_monthly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_product_sales_uq ON muyinqijian.customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 252. `muyinqijian.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：94 行 / 73,728 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 94 | 2025-11-01 | 2026-08-01 |
| `period_end` | 94 | 2025-11-30 | 2026-08-31 |
| `updated_at` | 94 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_count_ck` | 检查约束 | `CHECK (monthly_purchase_count >= 0)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON muyinqijian.customer_monthly_sales USING btree (id)` |
| `muyinqijian.customer_monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_sales_uq ON muyinqijian.customer_monthly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 253. `muyinqijian.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,852 行 / 1,007,616 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,799 | 2025-11-01 | 2026-08-01 |
| `period_end` | 3,799 | 2026-01-31 | 2026-10-31 |
| `updated_at` | 3,799 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON muyinqijian.customer_quarterly_product_sales USING btree (id)` |
| `muyinqijian.customer_quarterly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_uq ON muyinqijian.customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 254. `muyinqijian.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：67 行 / 40,960 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 67 | 2025-11-01 | 2026-08-01 |
| `period_end` | 67 | 2026-01-31 | 2026-10-31 |
| `updated_at` | 67 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_count_ck` | 检查约束 | `CHECK (quarterly_purchase_count >= 0)` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON muyinqijian.customer_quarterly_sales USING btree (id)` |
| `muyinqijian.customer_quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_sales_uq ON muyinqijian.customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 255. `muyinqijian.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：214 行 / 106,496 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 214 | 2025-10-27 | 2026-08-03 |
| `period_end` | 214 | 2025-11-02 | 2026-08-09 |
| `updated_at` | 214 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_count_ck` | 检查约束 | `CHECK (weekly_purchase_count >= 0)` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_weekly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON muyinqijian.customer_weekly_sales USING btree (id)` |
| `muyinqijian.customer_weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_weekly_sales_uq ON muyinqijian.customer_weekly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 256. `muyinqijian.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：766 行 / 245,760 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 843 | 2025-11-01 | 2026-08-03 |
| `updated_at` | 843 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_customer_sales_uq` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.daily_customer_sales_customer_date_idx` | 否 | 否 | `CREATE INDEX daily_customer_sales_customer_date_idx ON muyinqijian.daily_customer_sales USING btree (customer_id, transaction_date)` |
| `muyinqijian.daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON muyinqijian.daily_customer_sales USING btree (id)` |
| `muyinqijian.daily_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_customer_sales_uq ON muyinqijian.daily_customer_sales USING btree (transaction_date, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 257. `muyinqijian.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：10,586 行 / 1,933,312 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 10,724 | 2025-11-01 | 2026-08-03 |
| `updated_at` | 10,724 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_product_sales_uq` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON muyinqijian.daily_product_sales USING btree (id)` |
| `muyinqijian.daily_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_product_sales_uq ON muyinqijian.daily_product_sales USING btree (transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 258. `muyinqijian.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：216 行 / 73,728 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 224 | 2025-11-01 | 2026-08-03 |
| `updated_at` | 224 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON muyinqijian.daily_sales USING btree (id)` |
| `muyinqijian.daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_uq ON muyinqijian.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 259. `muyinqijian.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：216 行 / 81,920 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 224 | 2025-11-01 | 2026-08-03 |
| `updated_at` | 224 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_metrics_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON muyinqijian.daily_sales_metrics USING btree (id)` |
| `muyinqijian.daily_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_metrics_uq ON muyinqijian.daily_sales_metrics USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 260. `muyinqijian.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：57 行 / 73,728 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 57 | 2025-08-01 | 2026-08-01 |
| `period_end` | 57 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 57 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON muyinqijian.half_year_customer_sales USING btree (id)` |
| `muyinqijian.half_year_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_customer_sales_uq ON muyinqijian.half_year_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 261. `muyinqijian.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,064 行 / 540,672 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,994 | 2025-08-01 | 2026-08-01 |
| `period_end` | 1,994 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 1,994 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON muyinqijian.half_year_product_sales USING btree (id)` |
| `muyinqijian.half_year_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_sales_uq ON muyinqijian.half_year_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 262. `muyinqijian.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3 | 2025-08-01 | 2026-08-01 |
| `period_end` | 3 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 3 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON muyinqijian.half_year_refunds USING btree (id)` |
| `muyinqijian.half_year_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_uq ON muyinqijian.half_year_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 263. `muyinqijian.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3 | 2025-08-01 | 2026-08-01 |
| `period_end` | 3 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 3 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON muyinqijian.half_year_sales USING btree (id)` |
| `muyinqijian.half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_uq ON muyinqijian.half_year_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 264. `muyinqijian.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：94 行 / 73,728 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 94 | 2025-11-01 | 2026-08-01 |
| `period_end` | 94 | 2025-11-30 | 2026-08-31 |
| `updated_at` | 94 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON muyinqijian.monthly_customer_sales USING btree (id)` |
| `muyinqijian.monthly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_customer_sales_uq ON muyinqijian.monthly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 265. `muyinqijian.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,616 行 / 811,008 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,616 | 2025-11-01 | 2026-08-01 |
| `period_end` | 3,616 | 2025-11-30 | 2026-08-31 |
| `updated_at` | 3,616 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON muyinqijian.monthly_product_sales USING btree (id)` |
| `muyinqijian.monthly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_sales_uq ON muyinqijian.monthly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 266. `muyinqijian.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 9 | 2025-12-01 | 2026-08-01 |
| `period_end` | 9 | 2025-12-31 | 2026-08-31 |
| `updated_at` | 9 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON muyinqijian.monthly_refunds USING btree (id)` |
| `muyinqijian.monthly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_uq ON muyinqijian.monthly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 267. `muyinqijian.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 10 | 2025-11-01 | 2026-08-01 |
| `period_end` | 10 | 2025-11-30 | 2026-08-31 |
| `updated_at` | 10 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON muyinqijian.monthly_sales USING btree (id)` |
| `muyinqijian.monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_uq ON muyinqijian.monthly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 268. `muyinqijian.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 10 | 2025-11-01 | 2026-08-01 |
| `period_end` | 10 | 2025-11-30 | 2026-08-31 |
| `updated_at` | 10 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_metrics_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON muyinqijian.monthly_sales_metrics USING btree (id)` |
| `muyinqijian.monthly_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_metrics_uq ON muyinqijian.monthly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 269. `muyinqijian.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：67 行 / 40,960 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 67 | 2025-11-01 | 2026-08-01 |
| `period_end` | 67 | 2026-01-31 | 2026-10-31 |
| `updated_at` | 67 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON muyinqijian.quarterly_customer_sales USING btree (id)` |
| `muyinqijian.quarterly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_customer_sales_uq ON muyinqijian.quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 270. `muyinqijian.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,490 行 / 622,592 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2,522 | 2025-11-01 | 2026-08-01 |
| `period_end` | 2,522 | 2026-01-31 | 2026-10-31 |
| `updated_at` | 2,522 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON muyinqijian.quarterly_product_sales USING btree (id)` |
| `muyinqijian.quarterly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_sales_uq ON muyinqijian.quarterly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 271. `muyinqijian.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-11-01 | 2026-08-01 |
| `period_end` | 4 | 2026-01-31 | 2026-10-31 |
| `updated_at` | 4 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON muyinqijian.quarterly_refunds USING btree (id)` |
| `muyinqijian.quarterly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_uq ON muyinqijian.quarterly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 272. `muyinqijian.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-11-01 | 2026-08-01 |
| `period_end` | 4 | 2026-01-31 | 2026-10-31 |
| `updated_at` | 4 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON muyinqijian.quarterly_sales USING btree (id)` |
| `muyinqijian.quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_uq ON muyinqijian.quarterly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 273. `muyinqijian.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：16,327 行 / 8,544,256 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：订单或订单商品记录（以具体订单号/商品编码组合为准）。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `订单号` | `text` | 是 | — |
| `商品名称` | `text` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `销售渠道` | `text` | 是 | — |
| `交易成功时间` | `text` | 是 | — |
| `订单来源` | `text` | 是 | — |
| `订单实付金额` | `text` | 是 | — |
| `订单类型` | `text` | 是 | — |
| `商品ID` | `text` | 是 | — |
| `商品编码` | `text` | 是 | — |
| `商品数量` | `text` | 是 | — |
| `商品规格ID` | `text` | 是 | — |
| `商品单价` | `text` | 是 | — |
| `商品规格` | `text` | 是 | — |
| `规格编码` | `text` | 是 | — |
| `商品推广佣金` | `text` | 是 | — |
| `商品退款状态` | `text` | 是 | — |
| `商品已退款金额` | `text` | 是 | — |
| `分销推广补差` | `text` | 是 | — |
| `分销推广佣金` | `text` | 是 | — |
| `订单创建时间` | `text` | 是 | — |
| `买家昵称` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 16,478 | 2026-08-11 16:17:46.368895 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON muyinqijian.raw_data USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 274. `muyinqijian.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：179 行 / 98,304 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 214 | 2025-10-27 | 2026-08-03 |
| `period_end` | 214 | 2025-11-02 | 2026-08-09 |
| `updated_at` | 214 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON muyinqijian.weekly_customer_sales USING btree (id)` |
| `muyinqijian.weekly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_customer_sales_uq ON muyinqijian.weekly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 275. `muyinqijian.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：6,253 行 / 1,277,952 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 6,284 | 2025-10-27 | 2026-08-03 |
| `period_end` | 6,284 | 2025-11-02 | 2026-08-09 |
| `updated_at` | 6,284 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON muyinqijian.weekly_product_sales USING btree (id)` |
| `muyinqijian.weekly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_product_sales_uq ON muyinqijian.weekly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 276. `muyinqijian.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 32 | 2025-12-15 | 2026-08-03 |
| `period_end` | 32 | 2025-12-21 | 2026-08-09 |
| `updated_at` | 32 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON muyinqijian.weekly_refunds USING btree (id)` |
| `muyinqijian.weekly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_uq ON muyinqijian.weekly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 277. `muyinqijian.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 35 | 2025-10-27 | 2026-08-03 |
| `period_end` | 35 | 2025-11-02 | 2026-08-09 |
| `updated_at` | 35 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON muyinqijian.weekly_sales USING btree (id)` |
| `muyinqijian.weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_uq ON muyinqijian.weekly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 278. `muyinqijian.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `muyinqijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 35 | 2025-10-27 | 2026-08-03 |
| `period_end` | 35 | 2025-11-02 | 2026-08-09 |
| `updated_at` | 35 | 2026-08-11 16:29:09.342239 | 2026-08-14 12:47:23.685472 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_metrics_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `muyinqijian.weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON muyinqijian.weekly_sales_metrics USING btree (id)` |
| `muyinqijian.weekly_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_metrics_uq ON muyinqijian.weekly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 279. `public.distribution_customer_status_action`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：由表内主键及业务字段共同确定。
- **可能的上下游关联**：通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_health_status` | `character varying(32)` | 否 | — |
| `state_instructions` | `text` | 否 | — |
| `follow_up_action` | `text` | 否 | — |
| `created_time` | `timestamp with time zone` | 否 | — |
| `updated_time` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `created_time` | 7 | 2026-08-12 16:24:28.323884+08 | 2026-08-12 16:24:28.323884+08 |
| `updated_time` | 7 | 2026-08-12 16:48:13.743476+08 | 2026-08-12 16:48:13.743476+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `distribution_customer_status_action_status_check` | 检查约束 | `CHECK (customer_health_status::text = ANY (ARRAY['高活跃'::character varying, '活跃'::character varying, '稳定'::character varying, '观察'::character varying, '风险'::character varying, '流失预警'::character varying, '流失'::character varying]::text[]))` |
| `distribution_customer_status_action_pkey` | 主键 | `PRIMARY KEY (id)` |
| `distribution_customer_status_action_customer_health_status_key` | 唯一约束 | `UNIQUE (customer_health_status)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `distribution_customer_status_action_customer_health_status_key` | 是 | 否 | `CREATE UNIQUE INDEX distribution_customer_status_action_customer_health_status_key ON public.distribution_customer_status_action USING btree (customer_health_status)` |
| `distribution_customer_status_action_pkey` | 是 | 是 | `CREATE UNIQUE INDEX distribution_customer_status_action_pkey ON public.distribution_customer_status_action USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 280. `public.private_customer_status_action`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：由表内主键及业务字段共同确定。
- **可能的上下游关联**：通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_health_status` | `character varying(32)` | 否 | — |
| `state_instructions` | `text` | 否 | — |
| `follow_up_action` | `text` | 否 | — |
| `created_time` | `timestamp with time zone` | 否 | — |
| `updated_time` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `created_time` | 7 | 2026-08-12 16:24:28.323884+08 | 2026-08-12 16:24:28.323884+08 |
| `updated_time` | 7 | 2026-08-12 16:48:08.037873+08 | 2026-08-12 16:48:08.037873+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `private_customer_status_action_status_check` | 检查约束 | `CHECK (customer_health_status::text = ANY (ARRAY['高活跃'::character varying, '活跃'::character varying, '稳定'::character varying, '观察'::character varying, '风险'::character varying, '流失预警'::character varying, '流失'::character varying]::text[]))` |
| `private_customer_status_action_pkey` | 主键 | `PRIMARY KEY (id)` |
| `private_customer_status_action_customer_health_status_key` | 唯一约束 | `UNIQUE (customer_health_status)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `private_customer_status_action_customer_health_status_key` | 是 | 否 | `CREATE UNIQUE INDEX private_customer_status_action_customer_health_status_key ON public.private_customer_status_action USING btree (customer_health_status)` |
| `private_customer_status_action_pkey` | 是 | 是 | `CREATE UNIQUE INDEX private_customer_status_action_pkey ON public.private_customer_status_action USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 281. `public.talent_customer_status_action`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：由表内主键及业务字段共同确定。
- **可能的上下游关联**：通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_health_status` | `character varying(32)` | 否 | — |
| `state_instructions` | `text` | 否 | — |
| `follow_up_action` | `text` | 否 | — |
| `created_time` | `timestamp with time zone` | 否 | — |
| `updated_time` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `created_time` | 7 | 2026-08-12 16:24:28.323884+08 | 2026-08-12 16:24:28.323884+08 |
| `updated_time` | 7 | 2026-08-12 16:54:48.707804+08 | 2026-08-12 16:54:48.707804+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `talent_customer_status_action_status_check` | 检查约束 | `CHECK (customer_health_status::text = ANY (ARRAY['高活跃'::character varying, '活跃'::character varying, '稳定'::character varying, '观察'::character varying, '风险'::character varying, '流失预警'::character varying, '流失'::character varying]::text[]))` |
| `talent_customer_status_action_pkey` | 主键 | `PRIMARY KEY (id)` |
| `talent_customer_status_action_customer_health_status_key` | 唯一约束 | `UNIQUE (customer_health_status)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `talent_customer_status_action_customer_health_status_key` | 是 | 否 | `CREATE UNIQUE INDEX talent_customer_status_action_customer_health_status_key ON public.talent_customer_status_action USING btree (customer_health_status)` |
| `talent_customer_status_action_pkey` | 是 | 是 | `CREATE UNIQUE INDEX talent_customer_status_action_pkey ON public.talent_customer_status_action USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 282. `qijian.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：263,140 行 / 49,356,800 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 263,841 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 263,841 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON qijian.customer_daily_product_sales USING btree (id)` |
| `qijian.customer_daily_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_product_sales_uq ON qijian.customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 283. `qijian.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：12,949 行 / 2,048,000 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 12,984 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 12,984 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON qijian.customer_daily_sales USING btree (id)` |
| `qijian.customer_daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_uq ON qijian.customer_daily_sales USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 284. `qijian.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：12,949 行 / 2,244,608 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 12,984 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 12,984 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_metrics_uq` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON qijian.customer_daily_sales_metrics USING btree (id)` |
| `qijian.customer_daily_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_uq ON qijian.customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 285. `qijian.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：38,101 行 / 7,790,592 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 38,602 | 2025-02-01 | 2026-08-01 |
| `period_end` | 38,602 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 38,602 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON qijian.customer_half_year_product_sales USING btree (id)` |
| `qijian.customer_half_year_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_product_sales_uq ON qijian.customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 286. `qijian.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,483 行 / 344,064 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,487 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,487 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 1,487 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_count_ck` | 检查约束 | `CHECK (half_year_purchase_count >= 0)` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON qijian.customer_half_year_sales USING btree (id)` |
| `qijian.customer_half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_sales_uq ON qijian.customer_half_year_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 287. `qijian.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：58,615 行 / 33,177,600 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `week_period_start` | `date` | 否 | — |
| `week_period_end` | `date` | 否 | — |
| `month_period_start` | `date` | 否 | — |
| `month_period_end` | `date` | 否 | — |
| `week_purchase_count` | `integer` | 否 | — |
| `week_score` | `numeric(5,2)` | 否 | — |
| `month_purchase_count` | `numeric(10,2)` | 否 | — |
| `month_score` | `numeric(5,2)` | 否 | — |
| `customer_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `character varying(50)` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 60,742 | 2025-01-27 | 2026-08-03 |
| `period_end` | 60,742 | 2025-02-02 | 2026-08-09 |
| `week_period_start` | 60,742 | 2025-01-27 | 2026-08-03 |
| `week_period_end` | 60,742 | 2025-02-02 | 2026-08-09 |
| `month_period_start` | 60,742 | 2025-01-01 | 2026-08-01 |
| `month_period_end` | 60,742 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 60,742 | 2026-08-12 16:48:08.037873 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_month_count_ck` | 检查约束 | `CHECK (month_purchase_count >= 0::numeric)` |
| `customer_health_detail_period_match_ck` | 检查约束 | `CHECK (period_start = week_period_start AND period_end = week_period_end AND (period_end - period_start) = 6 AND EXTRACT(isodow FROM period_start) = 1::numeric AND EXTRACT(isodow FROM period_end) = 7::numeric)` |
| `customer_health_detail_score_ck` | 检查约束 | `CHECK (week_score >= 0::numeric AND week_score <= 100::numeric AND month_score >= 0::numeric AND month_score <= 100::numeric AND customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_status_ck` | 检查约束 | `CHECK (customer_health_status::text = CASE     WHEN customer_score >= 90::numeric THEN '高活跃'::text     WHEN customer_score >= 80::numeric THEN '活跃'::text     WHEN customer_score >= 70::numeric THEN '稳定'::text     WHEN customer_score >= 60::numeric THEN '观察'::text     WHEN customer_score >= 50::numeric THEN '风险'::text     WHEN customer_score >= 40::numeric THEN '流失预警'::text     ELSE '流失'::text END)` |
| `customer_health_detail_week_count_ck` | 检查约束 | `CHECK (week_purchase_count >= 0)` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON qijian.customer_health_detail USING btree (id)` |
| `qijian.customer_health_detail_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_uq ON qijian.customer_health_detail USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 288. `qijian.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,064 行 / 229,376 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 1,064 | 2026-08-11 17:18:05.981253 | 2026-08-11 17:18:05.981253 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_id_mapping_customer_id_uq` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_id_mapping_customer_id_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_customer_id_uq ON qijian.customer_id_mapping USING btree (customer_id)` |
| `qijian.customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON qijian.customer_id_mapping USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 289. `qijian.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：70,398 行 / 14,049,280 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 70,898 | 2025-02-01 | 2026-08-01 |
| `period_end` | 70,898 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 70,898 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON qijian.customer_monthly_product_sales USING btree (id)` |
| `qijian.customer_monthly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_product_sales_uq ON qijian.customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 290. `qijian.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,822 行 / 557,056 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2,827 | 2025-02-01 | 2026-08-01 |
| `period_end` | 2,827 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 2,827 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_count_ck` | 检查约束 | `CHECK (monthly_purchase_count >= 0)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON qijian.customer_monthly_sales USING btree (id)` |
| `qijian.customer_monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_sales_uq ON qijian.customer_monthly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 291. `qijian.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：46,777 行 / 9,453,568 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 47,278 | 2025-02-01 | 2026-08-01 |
| `period_end` | 47,278 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 47,278 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_product_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON qijian.customer_quarterly_product_sales USING btree (id)` |
| `qijian.customer_quarterly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_uq ON qijian.customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 292. `qijian.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,895 行 / 409,600 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,899 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,899 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 1,899 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_count_ck` | 检查约束 | `CHECK (quarterly_purchase_count >= 0)` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON qijian.customer_quarterly_sales USING btree (id)` |
| `qijian.customer_quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_sales_uq ON qijian.customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 293. `qijian.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：5,080 行 / 933,888 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `integer` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 5,090 | 2025-01-27 | 2026-08-03 |
| `period_end` | 5,090 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 5,090 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_count_ck` | 检查约束 | `CHECK (weekly_purchase_count >= 0)` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_weekly_sales_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON qijian.customer_weekly_sales USING btree (id)` |
| `qijian.customer_weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_weekly_sales_uq ON qijian.customer_weekly_sales USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 294. `qijian.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：12,949 行 / 2,637,824 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 12,984 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 12,984 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_customer_sales_uq` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.daily_customer_sales_customer_date_idx` | 否 | 否 | `CREATE INDEX daily_customer_sales_customer_date_idx ON qijian.daily_customer_sales USING btree (customer_id, transaction_date)` |
| `qijian.daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON qijian.daily_customer_sales USING btree (id)` |
| `qijian.daily_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_customer_sales_uq ON qijian.daily_customer_sales USING btree (transaction_date, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 295. `qijian.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：179,519 行 / 26,632,192 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 180,185 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 180,185 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_product_sales_uq` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON qijian.daily_product_sales USING btree (id)` |
| `qijian.daily_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_product_sales_uq ON qijian.daily_product_sales USING btree (transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 296. `qijian.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：541 行 / 131,072 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON qijian.daily_sales USING btree (id)` |
| `qijian.daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_uq ON qijian.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 297. `qijian.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：541 行 / 147,456 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_metrics_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON qijian.daily_sales_metrics USING btree (id)` |
| `qijian.daily_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_metrics_uq ON qijian.daily_sales_metrics USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 298. `qijian.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,483 行 / 327,680 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,487 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,487 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 1,487 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON qijian.half_year_customer_sales USING btree (id)` |
| `qijian.half_year_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_customer_sales_uq ON qijian.half_year_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 299. `qijian.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：8,325 行 / 1,490,944 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 8,786 | 2025-02-01 | 2026-08-01 |
| `period_end` | 8,786 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 8,786 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON qijian.half_year_product_sales USING btree (id)` |
| `qijian.half_year_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_sales_uq ON qijian.half_year_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 300. `qijian.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON qijian.half_year_refunds USING btree (id)` |
| `qijian.half_year_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_uq ON qijian.half_year_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 301. `qijian.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON qijian.half_year_sales USING btree (id)` |
| `qijian.half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_uq ON qijian.half_year_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 302. `qijian.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,822 行 / 532,480 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2,827 | 2025-02-01 | 2026-08-01 |
| `period_end` | 2,827 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 2,827 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON qijian.monthly_customer_sales USING btree (id)` |
| `qijian.monthly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_customer_sales_uq ON qijian.monthly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 303. `qijian.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：24,912 行 / 4,055,040 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 25,372 | 2025-02-01 | 2026-08-01 |
| `period_end` | 25,372 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 25,372 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON qijian.monthly_product_sales USING btree (id)` |
| `qijian.monthly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_sales_uq ON qijian.monthly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 304. `qijian.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON qijian.monthly_refunds USING btree (id)` |
| `qijian.monthly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_uq ON qijian.monthly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 305. `qijian.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON qijian.monthly_sales USING btree (id)` |
| `qijian.monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_uq ON qijian.monthly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 306. `qijian.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_metrics_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON qijian.monthly_sales_metrics USING btree (id)` |
| `qijian.monthly_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_metrics_uq ON qijian.monthly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 307. `qijian.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,895 行 / 393,216 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,899 | 2025-02-01 | 2026-08-01 |
| `period_end` | 1,899 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 1,899 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON qijian.quarterly_customer_sales USING btree (id)` |
| `qijian.quarterly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_customer_sales_uq ON qijian.quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 308. `qijian.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：12,458 行 / 2,154,496 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 12,918 | 2025-02-01 | 2026-08-01 |
| `period_end` | 12,918 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 12,918 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON qijian.quarterly_product_sales USING btree (id)` |
| `qijian.quarterly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_sales_uq ON qijian.quarterly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 309. `qijian.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON qijian.quarterly_refunds USING btree (id)` |
| `qijian.quarterly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_uq ON qijian.quarterly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 310. `qijian.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON qijian.quarterly_sales USING btree (id)` |
| `qijian.quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_uq ON qijian.quarterly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 311. `qijian.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：510,026 行 / 247,808,000 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：订单或订单商品记录（以具体订单号/商品编码组合为准）。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `订单号` | `text` | 是 | — |
| `商品名称` | `text` | 是 | — |
| `销售渠道` | `text` | 是 | — |
| `订单类型` | `text` | 是 | — |
| `订单来源` | `text` | 是 | — |
| `交易成功时间` | `text` | 是 | — |
| `订单实付金额` | `text` | 是 | — |
| `分销推广补差` | `text` | 是 | — |
| `分销推广佣金` | `text` | 是 | — |
| `商品规格` | `text` | 是 | — |
| `商品规格ID` | `text` | 是 | — |
| `规格编码` | `text` | 是 | — |
| `商品编码` | `text` | 是 | — |
| `商品单价` | `text` | 是 | — |
| `商品数量` | `text` | 是 | — |
| `商品ID` | `text` | 是 | — |
| `分销商商品推广补差` | `text` | 是 | — |
| `商品推广佣金` | `text` | 是 | — |
| `商品退款状态` | `text` | 是 | — |
| `商品已退款金额` | `text` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `订单创建时间` | `text` | 是 | — |
| `买家昵称` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 511,028 | 2026-08-11 17:14:26.549674 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON qijian.raw_data USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 312. `qijian.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：5,080 行 / 901,120 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 5,090 | 2025-01-27 | 2026-08-03 |
| `period_end` | 5,090 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 5,090 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_customer_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON qijian.weekly_customer_sales USING btree (id)` |
| `qijian.weekly_customer_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_customer_sales_uq ON qijian.weekly_customer_sales USING btree (period_start, period_end, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 313. `qijian.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：64,257 行 / 10,199,040 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `numeric(18,4)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 64,660 | 2025-01-27 | 2026-08-03 |
| `period_end` | 64,660 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 64,660 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_product_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON qijian.weekly_product_sales USING btree (id)` |
| `qijian.weekly_product_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_product_sales_uq ON qijian.weekly_product_sales USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 314. `qijian.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON qijian.weekly_refunds USING btree (id)` |
| `qijian.weekly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_uq ON qijian.weekly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 315. `qijian.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON qijian.weekly_sales USING btree (id)` |
| `qijian.weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_uq ON qijian.weekly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 316. `qijian.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `qijian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 17:22:56.266954 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_metrics_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qijian.weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON qijian.weekly_sales_metrics USING btree (id)` |
| `qijian.weekly_sales_metrics_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_metrics_uq ON qijian.weekly_sales_metrics USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 317. `qudao.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：556 行 / 303,104 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `daren_transaction_amount` | `numeric(20,2)` | 是 | — |
| `siyu_transaction_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_transaction_amount` | `numeric(20,2)` | 是 | — |
| `transaction_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `year_over_year_rate` | `numeric(20,6)` | 是 | — |
| `rolling_7_day_transaction_amount` | `numeric(20,2)` | 是 | — |
| `rolling_30_day_transaction_amount` | `numeric(20,2)` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 556 | 2025-02-01 | 2026-08-11 |
| `updated_at` | 556 | 2026-08-14 16:06:06.758517+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_check` | 检查约束 | `CHECK (transaction_amount = (COALESCE(daren_transaction_amount, 0::numeric) + COALESCE(siyu_transaction_amount, 0::numeric) + COALESCE(fenxiao_transaction_amount, 0::numeric)))` |
| `daily_sales_check1` | 检查约束 | `CHECK (included_group_count = ((daren_transaction_amount IS NOT NULL)::integer + (siyu_transaction_amount IS NOT NULL)::integer + (fenxiao_transaction_amount IS NOT NULL)::integer))` |
| `daily_sales_check2` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `daily_sales_check3` | 检查约束 | `CHECK (is_complete OR year_over_year_rate IS NULL AND rolling_7_day_transaction_amount IS NULL AND rolling_30_day_transaction_amount IS NULL)` |
| `daily_sales_daren_transaction_amount_check` | 检查约束 | `CHECK (daren_transaction_amount >= 0::numeric)` |
| `daily_sales_fenxiao_transaction_amount_check` | 检查约束 | `CHECK (fenxiao_transaction_amount >= 0::numeric)` |
| `daily_sales_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `daily_sales_rolling_30_day_transaction_amount_check` | 检查约束 | `CHECK (rolling_30_day_transaction_amount >= 0::numeric)` |
| `daily_sales_rolling_7_day_transaction_amount_check` | 检查约束 | `CHECK (rolling_7_day_transaction_amount >= 0::numeric)` |
| `daily_sales_siyu_transaction_amount_check` | 检查约束 | `CHECK (siyu_transaction_amount >= 0::numeric)` |
| `daily_sales_transaction_amount_check` | 检查约束 | `CHECK (transaction_amount >= 0::numeric)` |
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_transaction_date_key` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON qudao.daily_sales USING btree (id)` |
| `qudao.daily_sales_transaction_date_key` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_transaction_date_key ON qudao.daily_sales USING btree (transaction_date)` |
| `qudao.qudao_daily_sales_complete_idx` | 否 | 否 | `CREATE INDEX qudao_daily_sales_complete_idx ON qudao.daily_sales USING btree (transaction_date DESC) WHERE is_complete` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 318. `qudao.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：4 行 / 90,112 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `daren_half_year_refund_amount` | `numeric(20,2)` | 是 | — |
| `siyu_half_year_refund_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_half_year_refund_amount` | `numeric(20,2)` | 是 | — |
| `half_year_refund_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_refunds_check1` | 检查约束 | `CHECK (half_year_refund_amount = (COALESCE(daren_half_year_refund_amount, 0::numeric) + COALESCE(siyu_half_year_refund_amount, 0::numeric) + COALESCE(fenxiao_half_year_refund_amount, 0::numeric)))` |
| `half_year_refunds_check2` | 检查约束 | `CHECK (included_group_count = ((daren_half_year_refund_amount IS NOT NULL)::integer + (siyu_half_year_refund_amount IS NOT NULL)::integer + (fenxiao_half_year_refund_amount IS NOT NULL)::integer))` |
| `half_year_refunds_check3` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `half_year_refunds_daren_half_year_refund_amount_check` | 检查约束 | `CHECK (daren_half_year_refund_amount >= 0::numeric)` |
| `half_year_refunds_fenxiao_half_year_refund_amount_check` | 检查约束 | `CHECK (fenxiao_half_year_refund_amount >= 0::numeric)` |
| `half_year_refunds_half_year_refund_amount_check` | 检查约束 | `CHECK (half_year_refund_amount >= 0::numeric)` |
| `half_year_refunds_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `half_year_refunds_siyu_half_year_refund_amount_check` | 检查约束 | `CHECK (siyu_half_year_refund_amount >= 0::numeric)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.half_year_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_period_start_period_end_key ON qudao.half_year_refunds USING btree (period_start, period_end)` |
| `qudao.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON qudao.half_year_refunds USING btree (id)` |
| `qudao.qudao_half_year_refunds_complete_idx` | 否 | 否 | `CREATE INDEX qudao_half_year_refunds_complete_idx ON qudao.half_year_refunds USING btree (period_start DESC) WHERE is_complete` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 319. `qudao.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：4 行 / 90,112 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `daren_half_year_transaction_amount` | `numeric(20,2)` | 是 | — |
| `siyu_half_year_transaction_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_half_year_transaction_amount` | `numeric(20,2)` | 是 | — |
| `half_year_transaction_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_sales_check1` | 检查约束 | `CHECK (half_year_transaction_amount = (COALESCE(daren_half_year_transaction_amount, 0::numeric) + COALESCE(siyu_half_year_transaction_amount, 0::numeric) + COALESCE(fenxiao_half_year_transaction_amount, 0::numeric)))` |
| `half_year_sales_check2` | 检查约束 | `CHECK (included_group_count = ((daren_half_year_transaction_amount IS NOT NULL)::integer + (siyu_half_year_transaction_amount IS NOT NULL)::integer + (fenxiao_half_year_transaction_amount IS NOT NULL)::integer))` |
| `half_year_sales_check3` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `half_year_sales_daren_half_year_transaction_amount_check` | 检查约束 | `CHECK (daren_half_year_transaction_amount >= 0::numeric)` |
| `half_year_sales_fenxiao_half_year_transaction_amount_check` | 检查约束 | `CHECK (fenxiao_half_year_transaction_amount >= 0::numeric)` |
| `half_year_sales_half_year_transaction_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_sales_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `half_year_sales_siyu_half_year_transaction_amount_check` | 检查约束 | `CHECK (siyu_half_year_transaction_amount >= 0::numeric)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.half_year_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_period_start_period_end_key ON qudao.half_year_sales USING btree (period_start, period_end)` |
| `qudao.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON qudao.half_year_sales USING btree (id)` |
| `qudao.qudao_half_year_sales_complete_idx` | 否 | 否 | `CREATE INDEX qudao_half_year_sales_complete_idx ON qudao.half_year_sales USING btree (period_start DESC) WHERE is_complete` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 320. `qudao.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：19 行 / 90,112 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `daren_monthly_refund_amount` | `numeric(20,2)` | 是 | — |
| `siyu_monthly_refund_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_monthly_refund_amount` | `numeric(20,2)` | 是 | — |
| `monthly_refund_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `monthly_refunds_check1` | 检查约束 | `CHECK (monthly_refund_amount = (COALESCE(daren_monthly_refund_amount, 0::numeric) + COALESCE(siyu_monthly_refund_amount, 0::numeric) + COALESCE(fenxiao_monthly_refund_amount, 0::numeric)))` |
| `monthly_refunds_check2` | 检查约束 | `CHECK (included_group_count = ((daren_monthly_refund_amount IS NOT NULL)::integer + (siyu_monthly_refund_amount IS NOT NULL)::integer + (fenxiao_monthly_refund_amount IS NOT NULL)::integer))` |
| `monthly_refunds_check3` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `monthly_refunds_daren_monthly_refund_amount_check` | 检查约束 | `CHECK (daren_monthly_refund_amount >= 0::numeric)` |
| `monthly_refunds_fenxiao_monthly_refund_amount_check` | 检查约束 | `CHECK (fenxiao_monthly_refund_amount >= 0::numeric)` |
| `monthly_refunds_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `monthly_refunds_monthly_refund_amount_check` | 检查约束 | `CHECK (monthly_refund_amount >= 0::numeric)` |
| `monthly_refunds_siyu_monthly_refund_amount_check` | 检查约束 | `CHECK (siyu_monthly_refund_amount >= 0::numeric)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.monthly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_period_start_period_end_key ON qudao.monthly_refunds USING btree (period_start, period_end)` |
| `qudao.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON qudao.monthly_refunds USING btree (id)` |
| `qudao.qudao_monthly_refunds_complete_idx` | 否 | 否 | `CREATE INDEX qudao_monthly_refunds_complete_idx ON qudao.monthly_refunds USING btree (period_start DESC) WHERE is_complete` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 321. `qudao.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：19 行 / 57,344 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `daren_monthly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `siyu_monthly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_monthly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `monthly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `month_over_month_rate` | `numeric(20,6)` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `monthly_sales_check1` | 检查约束 | `CHECK (monthly_transaction_amount = (COALESCE(daren_monthly_transaction_amount, 0::numeric) + COALESCE(siyu_monthly_transaction_amount, 0::numeric) + COALESCE(fenxiao_monthly_transaction_amount, 0::numeric)))` |
| `monthly_sales_check2` | 检查约束 | `CHECK (included_group_count = ((daren_monthly_transaction_amount IS NOT NULL)::integer + (siyu_monthly_transaction_amount IS NOT NULL)::integer + (fenxiao_monthly_transaction_amount IS NOT NULL)::integer))` |
| `monthly_sales_check3` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `monthly_sales_check4` | 检查约束 | `CHECK (is_complete OR month_over_month_rate IS NULL)` |
| `monthly_sales_daren_monthly_transaction_amount_check` | 检查约束 | `CHECK (daren_monthly_transaction_amount >= 0::numeric)` |
| `monthly_sales_fenxiao_monthly_transaction_amount_check` | 检查约束 | `CHECK (fenxiao_monthly_transaction_amount >= 0::numeric)` |
| `monthly_sales_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `monthly_sales_monthly_transaction_amount_check` | 检查约束 | `CHECK (monthly_transaction_amount >= 0::numeric)` |
| `monthly_sales_siyu_monthly_transaction_amount_check` | 检查约束 | `CHECK (siyu_monthly_transaction_amount >= 0::numeric)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.monthly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_period_start_period_end_key ON qudao.monthly_sales USING btree (period_start, period_end)` |
| `qudao.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON qudao.monthly_sales USING btree (id)` |
| `qudao.qudao_monthly_sales_complete_idx` | 否 | 否 | `CREATE INDEX qudao_monthly_sales_complete_idx ON qudao.monthly_sales USING btree (period_start DESC) WHERE is_complete` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 322. `qudao.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：7 行 / 90,112 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `daren_quarterly_refund_amount` | `numeric(20,2)` | 是 | — |
| `siyu_quarterly_refund_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_quarterly_refund_amount` | `numeric(20,2)` | 是 | — |
| `quarterly_refund_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `quarterly_refunds_check1` | 检查约束 | `CHECK (quarterly_refund_amount = (COALESCE(daren_quarterly_refund_amount, 0::numeric) + COALESCE(siyu_quarterly_refund_amount, 0::numeric) + COALESCE(fenxiao_quarterly_refund_amount, 0::numeric)))` |
| `quarterly_refunds_check2` | 检查约束 | `CHECK (included_group_count = ((daren_quarterly_refund_amount IS NOT NULL)::integer + (siyu_quarterly_refund_amount IS NOT NULL)::integer + (fenxiao_quarterly_refund_amount IS NOT NULL)::integer))` |
| `quarterly_refunds_check3` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `quarterly_refunds_daren_quarterly_refund_amount_check` | 检查约束 | `CHECK (daren_quarterly_refund_amount >= 0::numeric)` |
| `quarterly_refunds_fenxiao_quarterly_refund_amount_check` | 检查约束 | `CHECK (fenxiao_quarterly_refund_amount >= 0::numeric)` |
| `quarterly_refunds_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `quarterly_refunds_quarterly_refund_amount_check` | 检查约束 | `CHECK (quarterly_refund_amount >= 0::numeric)` |
| `quarterly_refunds_siyu_quarterly_refund_amount_check` | 检查约束 | `CHECK (siyu_quarterly_refund_amount >= 0::numeric)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.quarterly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_period_start_period_end_key ON qudao.quarterly_refunds USING btree (period_start, period_end)` |
| `qudao.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON qudao.quarterly_refunds USING btree (id)` |
| `qudao.qudao_quarterly_refunds_complete_idx` | 否 | 否 | `CREATE INDEX qudao_quarterly_refunds_complete_idx ON qudao.quarterly_refunds USING btree (period_start DESC) WHERE is_complete` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 323. `qudao.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：7 行 / 90,112 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `daren_quarterly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `siyu_quarterly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_quarterly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `quarterly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-14 20:47:45.277471+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `quarterly_sales_check1` | 检查约束 | `CHECK (quarterly_transaction_amount = (COALESCE(daren_quarterly_transaction_amount, 0::numeric) + COALESCE(siyu_quarterly_transaction_amount, 0::numeric) + COALESCE(fenxiao_quarterly_transaction_amount, 0::numeric)))` |
| `quarterly_sales_check2` | 检查约束 | `CHECK (included_group_count = ((daren_quarterly_transaction_amount IS NOT NULL)::integer + (siyu_quarterly_transaction_amount IS NOT NULL)::integer + (fenxiao_quarterly_transaction_amount IS NOT NULL)::integer))` |
| `quarterly_sales_check3` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `quarterly_sales_daren_quarterly_transaction_amount_check` | 检查约束 | `CHECK (daren_quarterly_transaction_amount >= 0::numeric)` |
| `quarterly_sales_fenxiao_quarterly_transaction_amount_check` | 检查约束 | `CHECK (fenxiao_quarterly_transaction_amount >= 0::numeric)` |
| `quarterly_sales_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `quarterly_sales_quarterly_transaction_amount_check` | 检查约束 | `CHECK (quarterly_transaction_amount >= 0::numeric)` |
| `quarterly_sales_siyu_quarterly_transaction_amount_check` | 检查约束 | `CHECK (siyu_quarterly_transaction_amount >= 0::numeric)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.quarterly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_period_start_period_end_key ON qudao.quarterly_sales USING btree (period_start, period_end)` |
| `qudao.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON qudao.quarterly_sales USING btree (id)` |
| `qudao.qudao_quarterly_sales_complete_idx` | 否 | 否 | `CREATE INDEX qudao_quarterly_sales_complete_idx ON qudao.quarterly_sales USING btree (period_start DESC) WHERE is_complete` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 324. `qudao.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：81 行 / 106,496 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `daren_weekly_refund_amount` | `numeric(20,2)` | 是 | — |
| `siyu_weekly_refund_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_weekly_refund_amount` | `numeric(20,2)` | 是 | — |
| `weekly_refund_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 81 | 2025-01-27 | 2026-08-10 |
| `period_end` | 81 | 2025-02-02 | 2026-08-16 |
| `updated_at` | 81 | 2026-08-14 16:06:06.758517+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `weekly_refunds_check1` | 检查约束 | `CHECK (weekly_refund_amount = (COALESCE(daren_weekly_refund_amount, 0::numeric) + COALESCE(siyu_weekly_refund_amount, 0::numeric) + COALESCE(fenxiao_weekly_refund_amount, 0::numeric)))` |
| `weekly_refunds_check2` | 检查约束 | `CHECK (included_group_count = ((daren_weekly_refund_amount IS NOT NULL)::integer + (siyu_weekly_refund_amount IS NOT NULL)::integer + (fenxiao_weekly_refund_amount IS NOT NULL)::integer))` |
| `weekly_refunds_check3` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `weekly_refunds_daren_weekly_refund_amount_check` | 检查约束 | `CHECK (daren_weekly_refund_amount >= 0::numeric)` |
| `weekly_refunds_fenxiao_weekly_refund_amount_check` | 检查约束 | `CHECK (fenxiao_weekly_refund_amount >= 0::numeric)` |
| `weekly_refunds_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `weekly_refunds_siyu_weekly_refund_amount_check` | 检查约束 | `CHECK (siyu_weekly_refund_amount >= 0::numeric)` |
| `weekly_refunds_weekly_refund_amount_check` | 检查约束 | `CHECK (weekly_refund_amount >= 0::numeric)` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.qudao_weekly_refunds_complete_idx` | 否 | 否 | `CREATE INDEX qudao_weekly_refunds_complete_idx ON qudao.weekly_refunds USING btree (period_start DESC) WHERE is_complete` |
| `qudao.weekly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_period_start_period_end_key ON qudao.weekly_refunds USING btree (period_start, period_end)` |
| `qudao.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON qudao.weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 325. `qudao.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：81 行 / 106,496 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `daren_weekly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `siyu_weekly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `fenxiao_weekly_transaction_amount` | `numeric(20,2)` | 是 | — |
| `weekly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `included_group_count` | `smallint` | 否 | — |
| `is_complete` | `boolean` | 否 | — |
| `week_over_week_rate` | `numeric(20,6)` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 81 | 2025-01-27 | 2026-08-10 |
| `period_end` | 81 | 2025-02-02 | 2026-08-16 |
| `updated_at` | 81 | 2026-08-14 16:06:06.758517+08 | 2026-08-14 20:47:45.277471+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_check1` | 检查约束 | `CHECK (weekly_transaction_amount = (COALESCE(daren_weekly_transaction_amount, 0::numeric) + COALESCE(siyu_weekly_transaction_amount, 0::numeric) + COALESCE(fenxiao_weekly_transaction_amount, 0::numeric)))` |
| `weekly_sales_check2` | 检查约束 | `CHECK (included_group_count = ((daren_weekly_transaction_amount IS NOT NULL)::integer + (siyu_weekly_transaction_amount IS NOT NULL)::integer + (fenxiao_weekly_transaction_amount IS NOT NULL)::integer))` |
| `weekly_sales_check3` | 检查约束 | `CHECK (is_complete = (included_group_count = 3))` |
| `weekly_sales_check4` | 检查约束 | `CHECK (is_complete OR week_over_week_rate IS NULL)` |
| `weekly_sales_daren_weekly_transaction_amount_check` | 检查约束 | `CHECK (daren_weekly_transaction_amount >= 0::numeric)` |
| `weekly_sales_fenxiao_weekly_transaction_amount_check` | 检查约束 | `CHECK (fenxiao_weekly_transaction_amount >= 0::numeric)` |
| `weekly_sales_included_group_count_check` | 检查约束 | `CHECK (included_group_count >= 1 AND included_group_count <= 3)` |
| `weekly_sales_siyu_weekly_transaction_amount_check` | 检查约束 | `CHECK (siyu_weekly_transaction_amount >= 0::numeric)` |
| `weekly_sales_weekly_transaction_amount_check` | 检查约束 | `CHECK (weekly_transaction_amount >= 0::numeric)` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `qudao.qudao_weekly_sales_complete_idx` | 否 | 否 | `CREATE INDEX qudao_weekly_sales_complete_idx ON qudao.weekly_sales USING btree (period_start DESC) WHERE is_complete` |
| `qudao.weekly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_period_start_period_end_key ON qudao.weekly_sales USING btree (period_start, period_end)` |
| `qudao.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON qudao.weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 326. `siyu.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：89,404 行 / 44,744,704 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `health_grain` | `text` | 否 | — |
| `source_platform` | `text` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `week_purchase_count` | `numeric(20,4)` | 是 | — |
| `month_purchase_count` | `numeric(20,4)` | 是 | — |
| `half_year_purchase_count` | `bigint` | 是 | — |
| `half_year_purchase_amount` | `numeric(20,2)` | 是 | — |
| `week_score` | `numeric(10,2)` | 是 | — |
| `month_score` | `numeric(10,2)` | 是 | — |
| `customer_score` | `numeric(10,2)` | 否 | — |
| `customer_health_status` | `text` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 75,502 | 2025-01-27 | 2026-08-03 |
| `period_end` | 75,502 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 75,502 | 2026-08-12 16:48:08.037873+08 | 2026-08-14 13:05:48.531742+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `customer_health_detail_customer_health_status_check` | 检查约束 | `CHECK (customer_health_status = ANY (ARRAY['高活跃'::text, '活跃'::text, '稳定'::text, '观察'::text, '风险'::text, '流失预警'::text, '流失'::text]))` |
| `customer_health_detail_customer_score_check` | 检查约束 | `CHECK (customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_health_grain_check` | 检查约束 | `CHECK (health_grain = ANY (ARRAY['natural_week'::text, 'business_half_year'::text]))` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `siyu_customer_health_business_key` | 唯一约束 | `UNIQUE (health_grain, source_platform, customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON siyu.customer_health_detail USING btree (id)` |
| `siyu.siyu_customer_health_business_key` | 是 | 否 | `CREATE UNIQUE INDEX siyu_customer_health_business_key ON siyu.customer_health_detail USING btree (health_grain, source_platform, customer_id, period_start, period_end)` |
| `siyu.siyu_customer_health_customer_idx` | 否 | 否 | `CREATE INDEX siyu_customer_health_customer_idx ON siyu.customer_health_detail USING btree (customer_id)` |
| `siyu.siyu_customer_health_period_status_idx` | 否 | 否 | `CREATE INDEX siyu_customer_health_period_status_idx ON siyu.customer_health_detail USING btree (period_start, period_end, customer_health_status)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 327. `siyu.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：549 行 / 163,840 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(20,2)` | 否 | — |
| `year_over_year_rate` | `numeric(20,6)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(20,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_rolling_30_day_transaction_amount_check` | 检查约束 | `CHECK (rolling_30_day_transaction_amount >= 0::numeric)` |
| `daily_sales_rolling_7_day_transaction_amount_check` | 检查约束 | `CHECK (rolling_7_day_transaction_amount >= 0::numeric)` |
| `daily_sales_transaction_amount_check` | 检查约束 | `CHECK (transaction_amount >= 0::numeric)` |
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_transaction_date_key` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON siyu.daily_sales USING btree (id)` |
| `siyu.daily_sales_transaction_date_key` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_transaction_date_key ON siyu.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 328. `siyu.half_year_high_frequency_products`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 49,152 bytes
- **推断功能**：半年商品频次分析表。
- **表的作用**：按商品周期保存购买数量、金额及频次排名/分位数等分析结果。
- **可以提供的信息**：可提供商品周期交易金额、商品数量、数量排名和频次分位数；排名范围和分位数算法需结合刷新逻辑确认。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_product_quantity` | `numeric(20,4)` | 否 | — |
| `half_year_transaction_amount` | `numeric(20,2)` | 否 | — |
| `quantity_rank` | `bigint` | 否 | — |
| `amount_rank` | `bigint` | 否 | — |
| `selection_type` | `text` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 28 | 2025-02-01 | 2026-08-01 |
| `period_end` | 28 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 28 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_high_frequency_pro_half_year_transaction_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_high_frequency_produ_half_year_product_quantity_check` | 检查约束 | `CHECK (half_year_product_quantity >= 0::numeric)` |
| `half_year_high_frequency_products_amount_rank_check` | 检查约束 | `CHECK (amount_rank > 0)` |
| `half_year_high_frequency_products_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_high_frequency_products_quantity_rank_check` | 检查约束 | `CHECK (quantity_rank > 0)` |
| `half_year_high_frequency_products_selection_type_check` | 检查约束 | `CHECK (selection_type = ANY (ARRAY['quantity_top5'::text, 'amount_top5'::text, 'both'::text]))` |
| `half_year_high_frequency_products_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_high_frequency_prod_period_start_period_end_produ_key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.half_year_high_frequency_prod_period_start_period_end_produ_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_high_frequency_prod_period_start_period_end_produ_key ON siyu.half_year_high_frequency_products USING btree (period_start, period_end, product_code)` |
| `siyu.half_year_high_frequency_products_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_high_frequency_products_pkey ON siyu.half_year_high_frequency_products USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 329. `siyu.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_refunds_half_year_refund_amount_check` | 检查约束 | `CHECK (half_year_refund_amount >= 0::numeric)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.half_year_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_period_start_period_end_key ON siyu.half_year_refunds USING btree (period_start, period_end)` |
| `siyu.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON siyu.half_year_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 330. `siyu.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `half_year_sales_half_year_transaction_amount_check` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.half_year_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_period_start_period_end_key ON siyu.half_year_sales USING btree (period_start, period_end)` |
| `siyu.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON siyu.half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 331. `siyu.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `monthly_refunds_monthly_refund_amount_check` | 检查约束 | `CHECK (monthly_refund_amount >= 0::numeric)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.monthly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_period_start_period_end_key ON siyu.monthly_refunds USING btree (period_start, period_end)` |
| `siyu.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON siyu.monthly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 332. `siyu.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：18 行 / 73,728 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `month_over_month_rate` | `numeric(20,6)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `monthly_sales_monthly_transaction_amount_check` | 检查约束 | `CHECK (monthly_transaction_amount >= 0::numeric)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.monthly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_period_start_period_end_key ON siyu.monthly_sales USING btree (period_start, period_end)` |
| `siyu.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON siyu.monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 333. `siyu.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `quarterly_refunds_quarterly_refund_amount_check` | 检查约束 | `CHECK (quarterly_refund_amount >= 0::numeric)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.quarterly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_period_start_period_end_key ON siyu.quarterly_refunds USING btree (period_start, period_end)` |
| `siyu.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON siyu.quarterly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 334. `siyu.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `quarterly_sales_quarterly_transaction_amount_check` | 检查约束 | `CHECK (quarterly_transaction_amount >= 0::numeric)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.quarterly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_period_start_period_end_key ON siyu.quarterly_sales USING btree (period_start, period_end)` |
| `siyu.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON siyu.quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 335. `siyu.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(20,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_check` | 检查约束 | `CHECK (period_end >= period_start)` |
| `weekly_refunds_weekly_refund_amount_check` | 检查约束 | `CHECK (weekly_refund_amount >= 0::numeric)` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.weekly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_period_start_period_end_key ON siyu.weekly_refunds USING btree (period_start, period_end)` |
| `siyu.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON siyu.weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 336. `siyu.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 73,728 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(20,2)` | 否 | — |
| `week_over_week_rate` | `numeric(20,6)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-12 12:56:09.99743+08 | 2026-08-14 13:06:14.282001+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_check` | 检查约束 | `CHECK (period_end = (period_start + 6))` |
| `weekly_sales_weekly_transaction_amount_check` | 检查约束 | `CHECK (weekly_transaction_amount >= 0::numeric)` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `siyu.weekly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_period_start_period_end_key ON siyu.weekly_sales USING btree (period_start, period_end)` |
| `siyu.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON siyu.weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 337. `weidian.customer_daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：4,940 行 / 1,056,768 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 5,052 | 2025-10-15 | 2026-08-11 |
| `updated_at` | 5,052 | 2026-08-09 22:09:28.502673+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_product_sales_customer_id_transaction_date_p_key` | 唯一约束 | `UNIQUE (customer_id, transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_daily_product_sales_customer_id_transaction_date_p_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_product_sales_customer_id_transaction_date_p_key ON weidian.customer_daily_product_sales USING btree (customer_id, transaction_date, product_code)` |
| `weidian.customer_daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_product_sales_pkey ON weidian.customer_daily_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 338. `weidian.customer_daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：907 行 / 221,184 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 949 | 2025-10-15 | 2026-08-11 |
| `updated_at` | 949 | 2026-08-09 21:30:04.417162+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_customer_id_transaction_date_key` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_daily_sales_customer_id_transaction_date_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_customer_id_transaction_date_key ON weidian.customer_daily_sales USING btree (customer_id, transaction_date)` |
| `weidian.customer_daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_pkey ON weidian.customer_daily_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 339. `weidian.customer_daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：907 行 / 237,568 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 949 | 2025-10-15 | 2026-08-11 |
| `updated_at` | 949 | 2026-08-09 21:30:04.417162+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_daily_sales_metrics_customer_id_transaction_date_key` | 唯一约束 | `UNIQUE (customer_id, transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_daily_sales_metrics_customer_id_transaction_date_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_customer_id_transaction_date_key ON weidian.customer_daily_sales_metrics USING btree (customer_id, transaction_date)` |
| `weidian.customer_daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_daily_sales_metrics_pkey ON weidian.customer_daily_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 340. `weidian.customer_half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,914 行 / 663,552 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2,949 | 2025-08-01 | 2026-08-01 |
| `period_end` | 2,949 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 2,949 | 2026-08-09 22:09:28.502673+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_product_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_product_sa_customer_id_period_start_peri_key` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_half_year_product_sa_customer_id_period_start_peri_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_product_sa_customer_id_period_start_peri_key ON weidian.customer_half_year_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `weidian.customer_half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_product_sales_pkey ON weidian.customer_half_year_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 341. `weidian.customer_half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：126 行 / 81,920 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 135 | 2025-08-01 | 2026-08-01 |
| `period_end` | 135 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 135 | 2026-08-09 21:30:04.417162+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_half_year_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_half_year_sales_customer_id_period_start_period_en_key` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_half_year_sales_customer_id_period_start_period_en_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_half_year_sales_customer_id_period_start_period_en_key ON weidian.customer_half_year_sales USING btree (customer_id, period_start, period_end)` |
| `weidian.customer_half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_half_year_sales_pkey ON weidian.customer_half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 342. `weidian.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：126 行 / 122,880 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_purchase_count` | `bigint` | 否 | — |
| `half_year_purchase_amount` | `numeric(18,2)` | 否 | — |
| `customer_health_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `text` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 135 | 2025-08-01 | 2026-08-01 |
| `period_end` | 135 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 135 | 2026-08-12 16:48:07.608057+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `customer_health_detail_customer_health_score_check` | 检查约束 | `CHECK (customer_health_score >= 0::numeric AND customer_health_score <= 100::numeric)` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_customer_id_period_start_period_end_key` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_health_detail_customer_id_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_customer_id_period_start_period_end_key ON weidian.customer_health_detail USING btree (customer_id, period_start, period_end)` |
| `weidian.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON weidian.customer_health_detail USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 343. `weidian.customer_id_mapping`

- **表类型**：普通表
- **估算行数 / 存储大小**：117 行 / 81,920 bytes
- **推断功能**：周期客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `affiliate_id` | `text` | 是 | — |
| `affiliate_nickname` | `text` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `updated_at` | 119 | 2026-08-09 19:16:40.169423+08 | 2026-08-13 15:01:53.001797+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_id_mapping_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_id_mapping_customer_id_key` | 唯一约束 | `UNIQUE (customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_id_mapping_customer_id_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_id_mapping_customer_id_key ON weidian.customer_id_mapping USING btree (customer_id)` |
| `weidian.customer_id_mapping_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_id_mapping_pkey ON weidian.customer_id_mapping USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 344. `weidian.customer_monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,375 行 / 794,624 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,434 | 2025-10-01 | 2026-08-01 |
| `period_end` | 3,434 | 2025-10-31 | 2026-08-31 |
| `updated_at` | 3,434 | 2026-08-09 22:09:28.502673+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_product_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `customer_monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_product_sale_customer_id_period_start_peri_key` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_monthly_product_sale_customer_id_period_start_peri_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_product_sale_customer_id_period_start_peri_key ON weidian.customer_monthly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `weidian.customer_monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_product_sales_pkey ON weidian.customer_monthly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 345. `weidian.customer_monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：231 行 / 114,688 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 241 | 2025-10-01 | 2026-08-01 |
| `period_end` | 241 | 2025-10-31 | 2026-08-31 |
| `updated_at` | 241 | 2026-08-09 21:30:04.417162+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_monthly_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `customer_monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_monthly_sales_customer_id_period_start_period_end_key` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_monthly_sales_customer_id_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_monthly_sales_customer_id_period_start_period_end_key ON weidian.customer_monthly_sales USING btree (customer_id, period_start, period_end)` |
| `weidian.customer_monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_monthly_sales_pkey ON weidian.customer_monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 346. `weidian.customer_quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,045 行 / 696,320 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `product_code` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,085 | 2025-08-01 | 2026-08-01 |
| `period_end` | 3,085 | 2025-10-31 | 2026-10-31 |
| `updated_at` | 3,085 | 2026-08-09 22:09:28.502673+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_product_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `customer_quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_product_sa_customer_id_period_start_peri_key` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_quarterly_product_sa_customer_id_period_start_peri_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_product_sa_customer_id_period_start_peri_key ON weidian.customer_quarterly_product_sales USING btree (customer_id, period_start, period_end, product_code)` |
| `weidian.customer_quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_product_sales_pkey ON weidian.customer_quarterly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 347. `weidian.customer_quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：149 行 / 90,112 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 158 | 2025-08-01 | 2026-08-01 |
| `period_end` | 158 | 2025-10-31 | 2026-10-31 |
| `updated_at` | 158 | 2026-08-09 21:30:04.417162+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_quarterly_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `customer_quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_quarterly_sales_customer_id_period_start_period_en_key` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_quarterly_sales_customer_id_period_start_period_en_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_quarterly_sales_customer_id_period_start_period_en_key ON weidian.customer_quarterly_sales USING btree (customer_id, period_start, period_end)` |
| `weidian.customer_quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_quarterly_sales_pkey ON weidian.customer_quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 348. `weidian.customer_weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：422 行 / 155,648 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_purchase_count` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 440 | 2025-10-13 | 2026-08-10 |
| `period_end` | 440 | 2025-10-19 | 2026-08-16 |
| `updated_at` | 440 | 2026-08-09 21:30:04.417162+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_weekly_sales_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `customer_weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_weekly_sales_customer_id_period_start_period_end_key` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.customer_weekly_sales_customer_id_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX customer_weekly_sales_customer_id_period_start_period_end_key ON weidian.customer_weekly_sales USING btree (customer_id, period_start, period_end)` |
| `weidian.customer_weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_weekly_sales_pkey ON weidian.customer_weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 349. `weidian.daily_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：907 行 / 221,184 bytes
- **推断功能**：日客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 949 | 2025-10-15 | 2026-08-11 |
| `updated_at` | 949 | 2026-08-09 19:35:04.288898+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_customer_sales_transaction_date_customer_id_key` | 唯一约束 | `UNIQUE (transaction_date, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.daily_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_customer_sales_pkey ON weidian.daily_customer_sales USING btree (id)` |
| `weidian.daily_customer_sales_transaction_date_customer_id_key` | 是 | 否 | `CREATE UNIQUE INDEX daily_customer_sales_transaction_date_customer_id_key ON weidian.daily_customer_sales USING btree (transaction_date, customer_id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 350. `weidian.daily_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：6,209 行 / 1,171,456 bytes
- **推断功能**：日商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 6,402 | 2025-08-05 | 2026-08-11 |
| `updated_at` | 6,402 | 2026-08-09 19:28:34.300488+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_product_sales_transaction_date_product_code_key` | 唯一约束 | `UNIQUE (transaction_date, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.daily_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_product_sales_pkey ON weidian.daily_product_sales USING btree (id)` |
| `weidian.daily_product_sales_transaction_date_product_code_key` | 是 | 否 | `CREATE UNIQUE INDEX daily_product_sales_transaction_date_product_code_key ON weidian.daily_product_sales USING btree (transaction_date, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 351. `weidian.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：262 行 / 81,920 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 288 | 2025-08-05 | 2026-08-11 |
| `updated_at` | 288 | 2026-08-09 19:21:40.020269+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_transaction_date_key` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON weidian.daily_sales USING btree (id)` |
| `weidian.daily_sales_transaction_date_key` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_transaction_date_key ON weidian.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 352. `weidian.daily_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：262 行 / 90,112 bytes
- **推断功能**：日销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `year_over_year_rate` | `numeric(12,6)` | 是 | — |
| `rolling_7_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `rolling_30_day_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 288 | 2025-08-05 | 2026-08-11 |
| `updated_at` | 288 | 2026-08-09 20:45:59.016232+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_metrics_transaction_date_key` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.daily_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_metrics_pkey ON weidian.daily_sales_metrics USING btree (id)` |
| `weidian.daily_sales_metrics_transaction_date_key` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_metrics_transaction_date_key ON weidian.daily_sales_metrics USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 353. `weidian.half_year_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：126 行 / 81,920 bytes
- **推断功能**：半年客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 135 | 2025-08-01 | 2026-08-01 |
| `period_end` | 135 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 135 | 2026-08-09 20:29:40.782273+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_customer_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_customer_sales_period_start_period_end_customer_i_key` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.half_year_customer_sales_period_start_period_end_customer_i_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_customer_sales_period_start_period_end_customer_i_key ON weidian.half_year_customer_sales USING btree (period_start, period_end, customer_id)` |
| `weidian.half_year_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_customer_sales_pkey ON weidian.half_year_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 354. `weidian.half_year_product_presales`

- **表类型**：普通表
- **估算行数 / 存储大小**：74 行 / 49,152 bytes
- **推断功能**：半年商品预售汇总表。
- **表的作用**：按商品和周期汇总预售标识、预售数量及预售交易金额。
- **可以提供的信息**：可提供商品在周期内的预售标识、预售数量和预售交易金额；是否与普通销售互斥、金额是否扣除退款需确认。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `is_presale` | `boolean` | 否 | — |
| `half_year_presale_quantity` | `bigint` | 否 | — |
| `half_year_presale_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 74 | 2025-08-01 | 2026-02-01 |
| `period_end` | 74 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 74 | 2026-08-09 20:29:40.782273+08 | 2026-08-09 20:29:40.782273+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_presales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_presales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_presales_period_start_period_end_product__key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code, is_presale)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.half_year_product_presales_period_start_period_end_product__key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_presales_period_start_period_end_product__key ON weidian.half_year_product_presales USING btree (period_start, period_end, product_code, is_presale)` |
| `weidian.half_year_product_presales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_presales_pkey ON weidian.half_year_product_presales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 355. `weidian.half_year_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：1,893 行 / 401,408 bytes
- **推断功能**：半年商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 1,942 | 2025-08-01 | 2026-08-01 |
| `period_end` | 1,942 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 1,942 | 2026-08-09 20:29:40.782273+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_sales_period_start_period_end_product_cod_key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.half_year_product_sales_period_start_period_end_product_cod_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_sales_period_start_period_end_product_cod_key ON weidian.half_year_product_sales USING btree (period_start, period_end, product_code)` |
| `weidian.half_year_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_sales_pkey ON weidian.half_year_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 356. `weidian.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-09 20:29:40.782273+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.half_year_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_period_start_period_end_key ON weidian.half_year_refunds USING btree (period_start, period_end)` |
| `weidian.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON weidian.half_year_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 357. `weidian.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3 | 2025-08-01 | 2026-08-01 |
| `period_end` | 3 | 2026-01-31 | 2027-01-31 |
| `updated_at` | 3 | 2026-08-09 20:29:40.782273+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 8::numeric])) AND period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.half_year_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_period_start_period_end_key ON weidian.half_year_sales USING btree (period_start, period_end)` |
| `weidian.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON weidian.half_year_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 358. `weidian.monthly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：231 行 / 114,688 bytes
- **推断功能**：月客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 241 | 2025-10-01 | 2026-08-01 |
| `period_end` | 241 | 2025-10-31 | 2026-08-31 |
| `updated_at` | 241 | 2026-08-09 20:12:27.442939+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_customer_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_customer_sales_period_start_period_end_customer_id_key` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.monthly_customer_sales_period_start_period_end_customer_id_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_customer_sales_period_start_period_end_customer_id_key ON weidian.monthly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `weidian.monthly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_customer_sales_pkey ON weidian.monthly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 359. `weidian.monthly_product_presales`

- **表类型**：普通表
- **估算行数 / 存储大小**：99 行 / 81,920 bytes
- **推断功能**：月商品预售汇总表。
- **表的作用**：按商品和周期汇总预售标识、预售数量及预售交易金额。
- **可以提供的信息**：可提供商品在周期内的预售标识、预售数量和预售交易金额；是否与普通销售互斥、金额是否扣除退款需确认。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `is_presale` | `boolean` | 否 | — |
| `monthly_presale_quantity` | `bigint` | 否 | — |
| `monthly_presale_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 99 | 2025-12-01 | 2026-07-01 |
| `period_end` | 99 | 2025-12-31 | 2026-07-31 |
| `updated_at` | 99 | 2026-08-09 20:19:30.732641+08 | 2026-08-09 20:19:30.732641+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_presales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_product_presales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_presales_period_start_period_end_product_co_key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code, is_presale)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.monthly_product_presales_period_start_period_end_product_co_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_presales_period_start_period_end_product_co_key ON weidian.monthly_product_presales USING btree (period_start, period_end, product_code, is_presale)` |
| `weidian.monthly_product_presales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_presales_pkey ON weidian.monthly_product_presales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 360. `weidian.monthly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：3,147 行 / 614,400 bytes
- **推断功能**：月商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `monthly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 3,239 | 2025-08-01 | 2026-08-01 |
| `period_end` | 3,239 | 2025-08-31 | 2026-08-31 |
| `updated_at` | 3,239 | 2026-08-09 20:08:19.500762+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_product_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_product_sales_period_start_period_end_product_code_key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.monthly_product_sales_period_start_period_end_product_code_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_product_sales_period_start_period_end_product_code_key ON weidian.monthly_product_sales USING btree (period_start, period_end, product_code)` |
| `weidian.monthly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_product_sales_pkey ON weidian.monthly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 361. `weidian.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 14 | 2025-07-01 | 2026-08-01 |
| `period_end` | 14 | 2025-07-31 | 2026-08-31 |
| `updated_at` | 14 | 2026-08-09 20:02:57.222486+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.monthly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_period_start_period_end_key ON weidian.monthly_refunds USING btree (period_start, period_end)` |
| `weidian.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON weidian.monthly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 362. `weidian.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 13 | 2025-08-01 | 2026-08-01 |
| `period_end` | 13 | 2025-08-31 | 2026-08-31 |
| `updated_at` | 13 | 2026-08-09 19:53:30.54206+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.monthly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_period_start_period_end_key ON weidian.monthly_sales USING btree (period_start, period_end)` |
| `weidian.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON weidian.monthly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 363. `weidian.monthly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `month_over_month_rate` | `numeric(12,6)` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 13 | 2025-08-01 | 2026-08-01 |
| `period_end` | 13 | 2025-08-31 | 2026-08-31 |
| `updated_at` | 13 | 2026-08-09 21:01:33.281654+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_metrics_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_metrics_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.monthly_sales_metrics_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_metrics_period_start_period_end_key ON weidian.monthly_sales_metrics USING btree (period_start, period_end)` |
| `weidian.monthly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_metrics_pkey ON weidian.monthly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 364. `weidian.quarterly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：149 行 / 81,920 bytes
- **推断功能**：季客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 158 | 2025-08-01 | 2026-08-01 |
| `period_end` | 158 | 2025-10-31 | 2026-10-31 |
| `updated_at` | 158 | 2026-08-09 20:29:40.782273+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_customer_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_customer_sales_period_start_period_end_customer_i_key` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.quarterly_customer_sales_period_start_period_end_customer_i_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_customer_sales_period_start_period_end_customer_i_key ON weidian.quarterly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `weidian.quarterly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_customer_sales_pkey ON weidian.quarterly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 365. `weidian.quarterly_product_presales`

- **表类型**：普通表
- **估算行数 / 存储大小**：80 行 / 49,152 bytes
- **推断功能**：季商品预售汇总表。
- **表的作用**：按商品和周期汇总预售标识、预售数量及预售交易金额。
- **可以提供的信息**：可提供商品在周期内的预售标识、预售数量和预售交易金额；是否与普通销售互斥、金额是否扣除退款需确认。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `is_presale` | `boolean` | 否 | — |
| `quarterly_presale_quantity` | `bigint` | 否 | — |
| `quarterly_presale_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-11-01 | 2026-05-01 |
| `period_end` | 80 | 2026-01-31 | 2026-07-31 |
| `updated_at` | 80 | 2026-08-09 20:29:40.782273+08 | 2026-08-09 20:29:40.782273+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_presales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_presales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_presales_period_start_period_end_product__key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code, is_presale)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.quarterly_product_presales_period_start_period_end_product__key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_presales_period_start_period_end_product__key ON weidian.quarterly_product_presales USING btree (period_start, period_end, product_code, is_presale)` |
| `weidian.quarterly_product_presales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_presales_pkey ON weidian.quarterly_product_presales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 366. `weidian.quarterly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：2,256 行 / 466,944 bytes
- **推断功能**：季商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `quarterly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 2,311 | 2025-08-01 | 2026-08-01 |
| `period_end` | 2,311 | 2025-10-31 | 2026-10-31 |
| `updated_at` | 2,311 | 2026-08-09 20:29:40.782273+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_product_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_product_sales_period_start_period_end_product_cod_key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.quarterly_product_sales_period_start_period_end_product_cod_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_product_sales_period_start_period_end_product_cod_key ON weidian.quarterly_product_sales USING btree (period_start, period_end, product_code)` |
| `weidian.quarterly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_product_sales_pkey ON weidian.quarterly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 367. `weidian.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 6 | 2025-05-01 | 2026-08-01 |
| `period_end` | 6 | 2025-07-31 | 2026-10-31 |
| `updated_at` | 6 | 2026-08-09 20:29:40.782273+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.quarterly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_period_start_period_end_key ON weidian.quarterly_refunds USING btree (period_start, period_end)` |
| `weidian.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON weidian.quarterly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 368. `weidian.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 5 | 2025-08-01 | 2026-08-01 |
| `period_end` | 5 | 2025-10-31 | 2026-10-31 |
| `updated_at` | 5 | 2026-08-09 20:29:40.782273+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_check` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND (EXTRACT(month FROM period_start) = ANY (ARRAY[2::numeric, 5::numeric, 8::numeric, 11::numeric])) AND period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.quarterly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_period_start_period_end_key ON weidian.quarterly_sales USING btree (period_start, period_end)` |
| `weidian.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON weidian.quarterly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 369. `weidian.raw_data`

- **表类型**：普通表
- **估算行数 / 存储大小**：36,293 行 / 28,377,088 bytes
- **推断功能**：原始数据或平台导入事实表。
- **表的作用**：保存平台导入的订单、商品、客户、金额、数量、退款及履约等原始或近原始记录，作为后续清洗和聚合的来源。
- **可以提供的信息**：可提供原始业务字段、订单/商品粒度的金额数量、客户和商品标识、履约/售后状态及导入时间；具体指标必须按字段定义和去重口径确认。
- **记录粒度**：订单或订单商品记录（以具体订单号/商品编码组合为准）。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `订单号` | `text` | 否 | — |
| `订单下单时间` | `timestamp with time zone` | 否 | — |
| `订单发货时间` | `timestamp with time zone` | 是 | — |
| `订单确认收货时间` | `timestamp with time zone` | 是 | — |
| `订单完成结算时间` | `timestamp with time zone` | 是 | — |
| `订单状态` | `text` | 是 | — |
| `发货方式` | `text` | 是 | — |
| `收件人姓名` | `text` | 是 | — |
| `收件人地址` | `text` | 是 | — |
| `省` | `text` | 是 | — |
| `市` | `text` | 是 | — |
| `区` | `text` | 是 | — |
| `收件人手机` | `text` | 是 | — |
| `买家备注` | `text` | 是 | — |
| `商家备注` | `text` | 是 | — |
| `打标颜色` | `text` | 是 | — |
| `商品总价` | `numeric(18,2)` | 是 | — |
| `订单实际支付金额` | `numeric(18,2)` | 是 | — |
| `订单实际收款金额` | `numeric(18,2)` | 是 | — |
| `订单运费` | `numeric(18,2)` | 是 | — |
| `商品优惠` | `numeric(18,2)` | 是 | — |
| `跨店优惠` | `numeric(18,2)` | 是 | — |
| `商品改价` | `numeric(18,2)` | 是 | — |
| `积分抵扣` | `numeric(18,2)` | 是 | — |
| `支付方式` | `text` | 是 | — |
| `支付时间` | `timestamp with time zone` | 是 | — |
| `交易单号` | `text` | 是 | — |
| `物流公司` | `text` | 是 | — |
| `快递单号` | `text` | 是 | — |
| `技术服务费` | `numeric(18,2)` | 是 | — |
| `技术服务费（将以人气卡形式返还）` | `numeric(18,2)` | 是 | — |
| `运费险预计投保费用` | `numeric(18,2)` | 是 | — |
| `带货方式` | `text` | 是 | — |
| `带货账号类型` | `text` | 是 | — |
| `带货账号昵称` | `text` | 是 | — |
| `带货ID` | `text` | 是 | — |
| `带货费用渠道` | `text` | 是 | — |
| `带货费用类型` | `text` | 是 | — |
| `带货费用` | `numeric(18,2)` | 是 | — |
| `带货佣金率` | `numeric(9,6)` | 是 | — |
| `企微企业ID` | `text` | 是 | — |
| `企微工号` | `text` | 是 | — |
| `导购最近分享时间` | `timestamp with time zone` | 是 | — |
| `企业微信加密id` | `text` | 是 | — |
| `企微备注` | `text` | 是 | — |
| `礼物单号` | `text` | 是 | — |
| `商品名称` | `text` | 是 | — |
| `商品编码(平台)` | `text` | 是 | — |
| `商品编码(自定义)` | `text` | 是 | — |
| `SKU编码(自定义)` | `text` | 是 | — |
| `商品属性` | `text` | 是 | — |
| `商品价格(单件)` | `numeric(18,2)` | 是 | — |
| `商品实际价格(单件)` | `numeric(18,2)` | 是 | — |
| `商品实际价格(总共)` | `numeric(18,2)` | 是 | — |
| `是否预售` | `text` | 是 | — |
| `商品数量` | `integer` | 是 | — |
| `商品平台券优惠` | `numeric(18,2)` | 是 | — |
| `商品平均运费` | `numeric(18,2)` | 是 | — |
| `活动商家补贴` | `numeric(18,2)` | 是 | — |
| `定制信息` | `text` | 是 | — |
| `定制预览图` | `text` | 是 | — |
| `商品发货` | `text` | 是 | — |
| `商品售后` | `text` | 是 | — |
| `商品已退款金额` | `numeric(18,2)` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `订单下单时间` | 36,639 | 2025-07-26 19:56:54+08 | 2026-08-11 14:17:21+08 |
| `订单发货时间` | 28,246 | 2025-08-15 10:16:31+08 | 2026-08-11 16:39:21+08 |
| `订单确认收货时间` | 23,575 | 2025-08-20 15:44:44+08 | 2026-08-13 08:46:53+08 |
| `订单完成结算时间` | 22,556 | 2025-08-27 15:45:18+08 | 2026-08-12 20:55:04+08 |
| `支付时间` | 33,762 | 2025-07-27 15:55:55+08 | 2026-08-11 14:17:28+08 |
| `导购最近分享时间` | 0 | — | — |
| `updated_at` | 36,639 | 2026-08-09 18:19:31.704499+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `raw_data_pkey` | 主键 | `PRIMARY KEY (id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.raw_data_affiliate_id_idx` | 否 | 否 | `CREATE INDEX raw_data_affiliate_id_idx ON weidian.raw_data USING btree ("带货ID")` |
| `weidian.raw_data_order_number_idx` | 否 | 否 | `CREATE INDEX raw_data_order_number_idx ON weidian.raw_data USING btree ("订单号")` |
| `weidian.raw_data_order_time_idx` | 否 | 否 | `CREATE INDEX raw_data_order_time_idx ON weidian.raw_data USING btree ("订单下单时间")` |
| `weidian.raw_data_pkey` | 是 | 是 | `CREATE UNIQUE INDEX raw_data_pkey ON weidian.raw_data USING btree (id)` |
| `weidian.raw_data_product_code_idx` | 否 | 否 | `CREATE INDEX raw_data_product_code_idx ON weidian.raw_data USING btree ("商品编码(自定义)")` |
| `weidian.raw_data_sales_filter_idx` | 否 | 否 | `CREATE INDEX raw_data_sales_filter_idx ON weidian.raw_data USING btree ("订单状态", "商品发货")` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 370. `weidian.weekly_customer_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：422 行 / 172,032 bytes
- **推断功能**：周客户销售汇总表。
- **表的作用**：按客户和周期汇总交易金额、购买次数或客户维度经营结果。
- **可以提供的信息**：可提供客户标识、周期交易金额以及表内明确存在的购买次数等客户经营指标；客户身份映射关系通常来自同 Schema 的映射表或原始表。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 440 | 2025-10-13 | 2026-08-10 |
| `period_end` | 440 | 2025-10-19 | 2026-08-16 |
| `updated_at` | 440 | 2026-08-09 19:50:27.293559+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_customer_sales_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_customer_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_customer_sales_period_start_period_end_customer_id_key` | 唯一约束 | `UNIQUE (period_start, period_end, customer_id)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.weekly_customer_sales_period_start_period_end_customer_id_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_customer_sales_period_start_period_end_customer_id_key ON weidian.weekly_customer_sales USING btree (period_start, period_end, customer_id)` |
| `weidian.weekly_customer_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_customer_sales_pkey ON weidian.weekly_customer_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 371. `weidian.weekly_product_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：4,372 行 / 860,160 bytes
- **推断功能**：周商品销售汇总表。
- **表的作用**：按商品和周期汇总交易金额与商品数量，支持商品结构、畅销品和商品趋势分析。
- **可以提供的信息**：可提供商品标识、周期交易金额和商品数量；若字段中存在退款或成本字段，还可支持相应的净额/毛利分析，但需按实际字段计算。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `text` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `weekly_product_quantity` | `bigint` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4,521 | 2025-08-04 | 2026-08-10 |
| `period_end` | 4,521 | 2025-08-10 | 2026-08-16 |
| `updated_at` | 4,521 | 2026-08-09 19:47:33.452054+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_product_sales_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_product_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_product_sales_period_start_period_end_product_code_key` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.weekly_product_sales_period_start_period_end_product_code_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_product_sales_period_start_period_end_product_code_key ON weidian.weekly_product_sales USING btree (period_start, period_end, product_code)` |
| `weidian.weekly_product_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_product_sales_pkey ON weidian.weekly_product_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 372. `weidian.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 52 | 2025-07-21 | 2026-08-10 |
| `period_end` | 52 | 2025-07-27 | 2026-08-16 |
| `updated_at` | 52 | 2026-08-09 19:42:25.26271+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.weekly_refunds_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_period_start_period_end_key ON weidian.weekly_refunds USING btree (period_start, period_end)` |
| `weidian.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON weidian.weekly_refunds USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 373. `weidian.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 53 | 2025-08-04 | 2026-08-10 |
| `period_end` | 53 | 2025-08-10 | 2026-08-16 |
| `updated_at` | 53 | 2026-08-09 19:38:34.028541+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.weekly_sales_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_period_start_period_end_key ON weidian.weekly_sales USING btree (period_start, period_end)` |
| `weidian.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON weidian.weekly_sales USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 374. `weidian.weekly_sales_metrics`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：周销售指标表。
- **表的作用**：保存销售汇总及同比、环比、滚动窗口等派生指标，减少看板查询时的重复计算。
- **可以提供的信息**：可提供周期交易金额及表内明确命名的同比、环比、7日/30日滚动金额等指标；增长率的分母和空值处理需结合刷新逻辑确认。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：通常由 `weidian.raw_data` 清洗/聚合而来；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `week_over_week_rate` | `numeric(12,6)` | 是 | — |
| `created_at` | `timestamp with time zone` | 否 | — |
| `updated_at` | `timestamp with time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 53 | 2025-08-04 | 2026-08-10 |
| `period_end` | 53 | 2025-08-10 | 2026-08-16 |
| `updated_at` | 53 | 2026-08-09 20:57:17.129694+08 | 2026-08-14 16:06:06.758517+08 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_metrics_check` | 检查约束 | `CHECK (EXTRACT(isodow FROM period_start) = 1::numeric AND period_end = (period_start + 6))` |
| `weekly_sales_metrics_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_metrics_period_start_period_end_key` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `weidian.weekly_sales_metrics_period_start_period_end_key` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_metrics_period_start_period_end_key ON weidian.weekly_sales_metrics USING btree (period_start, period_end)` |
| `weidian.weekly_sales_metrics_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_metrics_pkey ON weidian.weekly_sales_metrics USING btree (id)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 375. `youzan.customer_health_detail`

- **表类型**：普通表
- **估算行数 / 存储大小**：82,796 行 / 33,251,328 bytes
- **推断功能**：客户健康度结果表。
- **表的作用**：按客户和评估周期保存健康度评分、状态及可执行建议，供客户经营分析和跟进场景使用。
- **可以提供的信息**：可提供客户周期评分、周/月购买表现、健康状态、状态说明和跟进动作；不能仅凭表结构确定评分规则或建议生成方式。
- **记录粒度**：客户 × 日期/周期，可能再按商品拆分。
- **可能的上下游关联**：通过客户标识与客户维度或映射数据关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `customer_id` | `character varying(255)` | 否 | — |
| `week_period_start` | `date` | 否 | — |
| `week_period_end` | `date` | 否 | — |
| `month_period_start` | `date` | 否 | — |
| `month_period_end` | `date` | 否 | — |
| `week_purchase_count` | `integer` | 否 | — |
| `week_score` | `numeric(5,2)` | 否 | — |
| `month_purchase_count` | `numeric(10,2)` | 否 | — |
| `month_score` | `numeric(5,2)` | 否 | — |
| `customer_score` | `numeric(5,2)` | 否 | — |
| `customer_health_status` | `character varying(50)` | 否 | — |
| `state_instructions` | `text` | 是 | — |
| `follow_up_action` | `text` | 是 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 60,758 | 2025-01-27 | 2026-08-03 |
| `period_end` | 60,758 | 2025-02-02 | 2026-08-09 |
| `week_period_start` | 60,758 | 2025-01-27 | 2026-08-03 |
| `week_period_end` | 60,758 | 2025-02-02 | 2026-08-09 |
| `month_period_start` | 60,758 | 2025-01-01 | 2026-08-01 |
| `month_period_end` | 60,758 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 60,758 | 2026-08-12 16:48:08.037873 | 2026-08-14 13:05:48.531742 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `customer_health_detail_month_count_ck` | 检查约束 | `CHECK (month_purchase_count >= 0::numeric)` |
| `customer_health_detail_period_match_ck` | 检查约束 | `CHECK (period_start = week_period_start AND period_end = week_period_end AND period_end = (period_start + 6) AND EXTRACT(isodow FROM period_start) = 1::numeric AND EXTRACT(isodow FROM period_end) = 7::numeric)` |
| `customer_health_detail_score_ck` | 检查约束 | `CHECK (week_score >= 0::numeric AND week_score <= 100::numeric AND month_score >= 0::numeric AND month_score <= 100::numeric AND customer_score >= 0::numeric AND customer_score <= 100::numeric)` |
| `customer_health_detail_status_ck` | 检查约束 | `CHECK (customer_health_status::text = CASE     WHEN customer_score >= 90::numeric THEN '高活跃'::text     WHEN customer_score >= 80::numeric THEN '活跃'::text     WHEN customer_score >= 70::numeric THEN '稳定'::text     WHEN customer_score >= 60::numeric THEN '观察'::text     WHEN customer_score >= 50::numeric THEN '风险'::text     WHEN customer_score >= 40::numeric THEN '流失预警'::text     ELSE '流失'::text END)` |
| `customer_health_detail_week_count_ck` | 检查约束 | `CHECK (week_purchase_count >= 0)` |
| `customer_health_detail_pkey` | 主键 | `PRIMARY KEY (id)` |
| `customer_health_detail_uq` | 唯一约束 | `UNIQUE (customer_id, period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.customer_health_detail_pkey` | 是 | 是 | `CREATE UNIQUE INDEX customer_health_detail_pkey ON youzan.customer_health_detail USING btree (id)` |
| `youzan.customer_health_detail_uq` | 是 | 否 | `CREATE UNIQUE INDEX customer_health_detail_uq ON youzan.customer_health_detail USING btree (customer_id, period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 376. `youzan.daily_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：541 行 / 131,072 bytes
- **推断功能**：日销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `transaction_date` | `date` | 否 | — |
| `transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `transaction_date` | 549 | 2025-02-01 | 2026-08-03 |
| `updated_at` | 549 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `daily_sales_amount_ck` | 检查约束 | `CHECK (transaction_amount >= 0::numeric)` |
| `daily_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `daily_sales_uq` | 唯一约束 | `UNIQUE (transaction_date)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.daily_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX daily_sales_pkey ON youzan.daily_sales USING btree (id)` |
| `youzan.daily_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX daily_sales_uq ON youzan.daily_sales USING btree (transaction_date)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 377. `youzan.half_year_product_frequency`

- **表类型**：普通表
- **估算行数 / 存储大小**：10,353 行 / 3,096,576 bytes
- **推断功能**：半年商品频次分析表。
- **表的作用**：按商品周期保存购买数量、金额及频次排名/分位数等分析结果。
- **可以提供的信息**：可提供商品周期交易金额、商品数量、数量排名和频次分位数；排名范围和分位数算法需结合刷新逻辑确认。
- **记录粒度**：商品 × 日期/周期。
- **可能的上下游关联**：通过商品编码/SKU 与商品维度或原始商品字段关联；按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `product_code` | `character varying(255)` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `half_year_product_quantity` | `numeric(18,4)` | 否 | — |
| `quantity_rank` | `integer` | 否 | — |
| `frequency_percentile` | `numeric(6,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 10,353 | 2025-02-01 | 2026-08-01 |
| `period_end` | 10,353 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 10,353 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_product_frequency_amount_ck` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_product_frequency_percentile_ck` | 检查约束 | `CHECK (frequency_percentile >= 0::numeric AND frequency_percentile <= 100::numeric)` |
| `half_year_product_frequency_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_product_frequency_quantity_ck` | 检查约束 | `CHECK (half_year_product_quantity >= 0::numeric)` |
| `half_year_product_frequency_rank_ck` | 检查约束 | `CHECK (quantity_rank >= 1)` |
| `half_year_product_frequency_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_product_frequency_uq` | 唯一约束 | `UNIQUE (period_start, period_end, product_code)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.half_year_product_frequency_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_product_frequency_pkey ON youzan.half_year_product_frequency USING btree (id)` |
| `youzan.half_year_product_frequency_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_product_frequency_uq ON youzan.half_year_product_frequency USING btree (period_start, period_end, product_code)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 378. `youzan.half_year_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_refunds_amount_ck` | 检查约束 | `CHECK (half_year_refund_amount >= 0::numeric)` |
| `half_year_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.half_year_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_refunds_pkey ON youzan.half_year_refunds USING btree (id)` |
| `youzan.half_year_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_refunds_uq ON youzan.half_year_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 379. `youzan.half_year_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：半年销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `half_year_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 4 | 2025-02-01 | 2026-08-01 |
| `period_end` | 4 | 2025-07-31 | 2027-01-31 |
| `updated_at` | 4 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `half_year_sales_amount_ck` | 检查约束 | `CHECK (half_year_transaction_amount >= 0::numeric)` |
| `half_year_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '6 mons -1 days'::interval)::date)` |
| `half_year_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `half_year_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.half_year_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX half_year_sales_pkey ON youzan.half_year_sales USING btree (id)` |
| `youzan.half_year_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX half_year_sales_uq ON youzan.half_year_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 380. `youzan.monthly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_refunds_amount_ck` | 检查约束 | `CHECK (monthly_refund_amount >= 0::numeric)` |
| `monthly_refunds_period_ck` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.monthly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_refunds_pkey ON youzan.monthly_refunds USING btree (id)` |
| `youzan.monthly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_refunds_uq ON youzan.monthly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 381. `youzan.monthly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：月销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `monthly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 19 | 2025-02-01 | 2026-08-01 |
| `period_end` | 19 | 2025-02-28 | 2026-08-31 |
| `updated_at` | 19 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `monthly_sales_amount_ck` | 检查约束 | `CHECK (monthly_transaction_amount >= 0::numeric)` |
| `monthly_sales_period_ck` | 检查约束 | `CHECK (period_start = date_trunc('month'::text, period_start::timestamp with time zone)::date AND period_end = (period_start + '1 mon -1 days'::interval)::date)` |
| `monthly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `monthly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.monthly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX monthly_sales_pkey ON youzan.monthly_sales USING btree (id)` |
| `youzan.monthly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX monthly_sales_uq ON youzan.monthly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 382. `youzan.quarterly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_refunds_amount_ck` | 检查约束 | `CHECK (quarterly_refund_amount >= 0::numeric)` |
| `quarterly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.quarterly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_refunds_pkey ON youzan.quarterly_refunds USING btree (id)` |
| `youzan.quarterly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_refunds_uq ON youzan.quarterly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 383. `youzan.quarterly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：0 行 / 40,960 bytes
- **推断功能**：季销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `quarterly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 7 | 2025-02-01 | 2026-08-01 |
| `period_end` | 7 | 2025-04-30 | 2026-10-31 |
| `updated_at` | 7 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `quarterly_sales_amount_ck` | 检查约束 | `CHECK (quarterly_transaction_amount >= 0::numeric)` |
| `quarterly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + '3 mons -1 days'::interval)::date)` |
| `quarterly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `quarterly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.quarterly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX quarterly_sales_pkey ON youzan.quarterly_sales USING btree (id)` |
| `youzan.quarterly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX quarterly_sales_uq ON youzan.quarterly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 384. `youzan.weekly_refunds`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周退款汇总表。
- **表的作用**：按周期保存退款金额或退款相关聚合结果，用于退款趋势、净销售额和经营异常分析。
- **可以提供的信息**：可提供周期起止日期和退款金额等退款汇总信息；若无退款明细字段，不能进一步推导退款订单数、退款率或原因分布。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_refund_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_refunds_amount_ck` | 检查约束 | `CHECK (weekly_refund_amount >= 0::numeric)` |
| `weekly_refunds_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6) AND EXTRACT(isodow FROM period_start) = 1::numeric AND EXTRACT(isodow FROM period_end) = 7::numeric)` |
| `weekly_refunds_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_refunds_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.weekly_refunds_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_refunds_pkey ON youzan.weekly_refunds USING btree (id)` |
| `youzan.weekly_refunds_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_refunds_uq ON youzan.weekly_refunds USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---

### 385. `youzan.weekly_sales`

- **表类型**：普通表
- **估算行数 / 存储大小**：78 行 / 40,960 bytes
- **推断功能**：周销售汇总表。
- **表的作用**：按周期保存整体销售交易金额，作为销售趋势和经营看板的基础汇总层。
- **可以提供的信息**：可提供周期起止日期和交易金额；若同时存在指标字段，还可提供表内已计算的增长或滚动指标。
- **记录粒度**：日期/周期汇总。
- **可能的上下游关联**：按日期或周期字段与其他汇总层对齐。

| 字段 | 类型 | 可空 | 字段注释 |
|---|---|---:|---|
| `id` | `bigint` | 否 | — |
| `period_start` | `date` | 否 | — |
| `period_end` | `date` | 否 | — |
| `weekly_transaction_amount` | `numeric(18,2)` | 否 | — |
| `created_at` | `timestamp without time zone` | 否 | — |
| `updated_at` | `timestamp without time zone` | 否 | — |

**时间字段统计**

| 字段 | 非空值数 | 最小值 | 最大值 |
|---|---:|---|---|
| `period_start` | 80 | 2025-01-27 | 2026-08-03 |
| `period_end` | 80 | 2025-02-02 | 2026-08-09 |
| `updated_at` | 80 | 2026-08-11 17:44:54.382095 | 2026-08-14 13:06:14.282001 |

**主键、唯一约束与其他约束**

| 名称 | 类型 | 定义 |
|---|---|---|
| `weekly_sales_amount_ck` | 检查约束 | `CHECK (weekly_transaction_amount >= 0::numeric)` |
| `weekly_sales_period_ck` | 检查约束 | `CHECK (period_end = (period_start + 6) AND EXTRACT(isodow FROM period_start) = 1::numeric AND EXTRACT(isodow FROM period_end) = 7::numeric)` |
| `weekly_sales_pkey` | 主键 | `PRIMARY KEY (id)` |
| `weekly_sales_uq` | 唯一约束 | `UNIQUE (period_start, period_end)` |

**索引**

| 索引名 | 唯一 | 主键 | 定义 |
|---|---:|---:|---|
| `youzan.weekly_sales_pkey` | 是 | 是 | `CREATE UNIQUE INDEX weekly_sales_pkey ON youzan.weekly_sales USING btree (id)` |
| `youzan.weekly_sales_uq` | 是 | 否 | `CREATE UNIQUE INDEX weekly_sales_uq ON youzan.weekly_sales USING btree (period_start, period_end)` |

- **口径与业务定义置信度**：中：表名和字段组合能支持上述分类，但仍需核对刷新代码/数据字典。

---
