# 各店铺交易日期字段与上传注意事项

> 更新时间：2026-08-14  
> 口径：以 `8.11/backend/upload/` 当前代码为准。历史需求草稿仅供追溯，不作为上传规则的最终依据。

## 1. 每个店铺的交易日期字段

| 店铺 | `store_key` | PostgreSQL Schema | 交易日期来源字段 | 已有日期处理策略 |
|---|---|---|---|---|
| 微店 | `weidian` | `weidian` | `支付时间` | `skip`：已有日期整日跳过 |
| 儿童服饰旗舰店 | `doudian_children` | `doudianChildren` | `支付完成时间` | `replace`：已有日期整日覆盖 |
| Kocotree服饰配件店 | `doudian_kocotree` | `doudianKocotree` | `支付完成时间` | `replace`：已有日期整日覆盖 |
| 快手小店 | `kuaishou` | `kuaishouxiaodian` | `订单创建时间` | `replace`：已有日期整日覆盖 |
| 有赞旗舰店 | `youzan_qijian` | `qijian` | `订单创建时间` | `skip`：已有日期整日跳过 |
| 母婴旗舰店 | `youzan_muying` | `muyinqijian` | `订单创建时间` | `skip`：已有日期整日跳过 |
| 快团团 | `kuaituantuan` | `kuaituantuan` | `创单时间` | `upsert`：按业务键新增或更新 |
| 阿里巴巴 | `alibaba` | `alibaba` | `付款日期` | `skip`：已有日期整日跳过 |
| 聚水潭 | `jushuitan` | `jushuitan` | `付款日期` | `skip`：已有日期整日跳过 |

对应的核心配置结构为：

```python
# 8.11/backend/upload/models.py
@dataclass(frozen=True)
class StoreUploadConfig:
    store_key: str
    schema_name: str
    transaction_time_column: str
    customer_resolver: CustomerResolver
    customer_mapping_columns: tuple[str, ...]
    downstream_tables: tuple[str, ...]
    aggregate_path: tuple[str, ...]
    required_upload_columns: tuple[str, ...] = ()
    date_year_replacements: tuple[tuple[int, int], ...] = ()
    existing_date_policy: str = "skip"
    ignored_upload_columns: tuple[str, ...] = ()
    row_key_columns: tuple[str, ...] = ()
```

交易日期字段由每个店铺目录下的 `config.py` 直接配置，例如：

```python
# 微店
transaction_time_column="支付时间"

# 抖店两店
transaction_time_column="支付完成时间"
existing_date_policy="replace"

# 快手
transaction_time_column="订单创建时间"
existing_date_policy="replace"

# 快团团
transaction_time_column="创单时间"
existing_date_policy="upsert"
row_key_columns=("子订单号", "商品编码")

# 阿里巴巴、聚水潭
transaction_time_column="付款日期"
```

## 2. 三种日期处理策略

### 2.1 `replace`：按交易日期整日覆盖

适用店铺：抖店两店、快手小店。

```python
if config.existing_date_policy == "replace":
    accepted = prepared
    preview_replacement_dates = analysis.existing_dates
```

- 文件中的新日期全部新增。
- 文件中的已有日期会先删除数据库该日旧明细，再写入文件该日全部明细。
- 同一订单存在多条商品明细时不会按订单号合并。
- 正式写入在单一事务中执行，任一步失败会整体回滚。

注意：上传文件必须是所含日期的完整日快照。若只上传某日的部分订单，该日数据库旧数据仍会被整日替换，未包含的记录会消失。

### 2.2 `skip`：已有交易日期整日跳过

适用店铺：微店、有赞两店、阿里巴巴、聚水潭。

```python
elif config.existing_date_policy == "skip":
    accepted = [
        item for item in prepared
        if item.business_date not in analysis.existing_dates
    ]
```

- 文件中的新日期正常新增。
- 数据库已存在的日期整日跳过，不覆盖、不更新。

注意：重复上传无法修正已有日期。如果确实要改历史日期，需要先明确修数方案，不能把 `skip` 文件当成覆盖文件使用。

### 2.3 `upsert`：按商品明细业务键更新

适用店铺：快团团。

```python
existing_date_policy="upsert"
row_key_columns=("子订单号", "商品编码")
```

- `子订单号 + 商品编码` 不存在时新增。
- 业务键已存在且内容变化时更新。
- 内容一致时标记为不变。
- 不会因为 `创单时间` 所属日期已存在而跳过整日。

注意：两个业务键字段都必须上传；一行中不能两个键都为空。文件内同一业务键重复时，以最后一条不同内容为准。

## 3. 每个店铺的必需上传字段

交易日期字段会单独强制校验。下表是除交易日期外，代码新增的业务必需字段：

| 店铺 | 业务必需字段 | 允许忽略但不会入库的字段 |
|---|---|---|
| 微店 | `订单实际收款金额`、`商品已退款金额`、`订单状态`、`商品发货`、`商品数量`、`SKU编码(自定义)`、`带货ID`、`带货账号昵称`、`是否预售`、`订单下单时间` | 无 |
| 儿童服饰旗舰店 | `订单应付金额`、`订单状态`、`售后状态`、`商品数量`、`商家编码`、`达人ID`、`达人昵称`、`流量来源` | 无 |
| Kocotree服饰配件店 | `订单应付金额`、`订单状态`、`售后状态`、`商品数量`、`商家编码`、`达人ID`、`达人昵称`、`流量来源` | 无 |
| 快手小店 | `订单状态`、`实付款`、`成交数量`、`售后状态`、`订单备注`、`SKU编码`、`渠道`、`CPS达人ID`、`CPS达人昵称`、`团长ID`、`团长昵称`、`快赚客ID`、`快赚客昵称` | 无 |
| 有赞旗舰店 | `订单状态`、`商品单价`、`商品数量`、`商品已退款金额`、`规格编码`、`销售渠道`、`买家昵称` | 无 |
| 母婴旗舰店 | `订单状态`、`商品单价`、`商品数量`、`商品已退款金额`、`规格编码`、`销售渠道`、`买家昵称` | `分销商商品推广补差` |
| 快团团 | `子订单号`、`商品编码`、`数量`、`商品金额`、`已退款+退款中`、`团长` | 无 |
| 阿里巴巴 | `订单状态`、`实发金额`、`实退金额`、`实发数量`、`实退数量`、`商品编码`、`买家ID`、`买家账号` | `虚拟分类` |
| 聚水潭 | `订单状态`、`销售数量`、`销售金额`、`实退数量`、`实退金额`、`商品编码`、`分销商`、`店铺` | `虚拟分类` |

缺少上述字段时，预览阶段直接拒绝文件：

```python
# 8.11/backend/upload/pipeline.py
missing_required = [
    column
    for column in config.required_upload_columns
    if column not in effective_headers
]
if missing_required:
    raise ApiError(
        422,
        "UPLOAD_REQUIRED_COLUMN_MISSING",
        f"文件缺少业务必需字段：{', '.join(missing_required)}",
    )
```

## 4. 各店铺销售、退款与客户口径

### 4.1 微店

- 销售：`订单状态` 为 `已完成`或`已发货`，并且 `商品发货 == 已发货`。
- 销售额：`订单实际收款金额`，按毛销售额统计。
- 退款：`商品已退款金额 > 0`，单独进入退款指标，不直接冲减毛销售额。
- 预售：`是否预售` 不为空、不是 `-`、不是 `现货`。
- 客户：有效 `带货ID`；昵称取 `带货账号昵称`。
- 预售周期使用 `订单下单时间`，销售交易日期仍使用 `支付时间`。

### 4.2 抖店两店

- 有效销售状态：`已完成`、`已发货`、`待发货`。
- 销售额：`订单应付金额`，按毛销售额统计。
- `售后状态` 不属于空值、`-`、`换货成功`、`换货待收货`、`补寄成功`时，计入退款。
- 客户：仅 `流量来源 == 精选联盟` 时，使用 `达人ID` 和 `达人昵称`。

```python
DOUDIAN_MIXED_SALES_RULES = MixedSalesRules(
    amount_column="订单应付金额",
    order_status_column="订单状态",
    valid_sales_statuses=("已完成", "已发货", "待发货"),
    refund_status_column="售后状态",
    non_refund_statuses=("", "-", "换货成功", "换货待收货", "补寄成功"),
)
```

### 4.3 快手小店

- 有效销售状态：`交易成功`、`已发货`、`已收货`。
- 销售额：`实付款`。
- 退款：优先解析 `订单备注` 中的系统小额打款或明确部分退款；否则 `售后状态 == 退款成功` 时按 `实付款`计退款。
- 客户：仅 `渠道 == 分销` 时，依次取第一个有效的 `CPS达人ID`、`团长ID`、`快赚客ID`。
- 已修正：合法 `实付款 == 65` 现在会按 `65.00` 计入销售额，不再被当成空值。

### 4.4 有赞两店

- 有效销售状态：`已完成`、`已关闭`、`已发货`。
- 销售额：`商品单价 × 商品数量`，逐行四舍五入到 2 位。
- 退款：`商品已退款金额 > 0`，单独统计，不冲减毛销售额。
- 商品：使用 `规格编码`；空白或 `-` 不进入商品维度表。
- 客户：仅 `销售渠道 == 网店` 且 `买家昵称` 有效时，客户 ID 等于 `买家昵称`。

### 4.5 快团团

- 销售额：`数量 × 商品金额`，逐行四舍五入到 2 位。
- 退款：`已退款+退款中`。
- 商品：`商品编码`。
- 客户：`团长`。
- 明细更新业务键：`子订单号 + 商品编码`。

### 4.6 阿里巴巴

- 仅 `订单状态 == 已发货` 的记录进入销售统计。
- 销售额：`实发金额`，按毛销售额统计。
- 退款：`实退金额`。
- 商品数量：`实发数量 - 实退数量`。
- 客户：已发货且 `买家ID` 有效；昵称取 `买家账号`。

### 4.7 聚水潭

- 仅 `订单状态 == 已发货` 的记录进入销售统计。
- 销售额：`销售数量 × 销售金额`，逐行四舍五入到 2 位。
- 退款：`实退数量 × 实退金额`，逐行四舍五入到 2 位。
- 客户：优先使用 `分销商`；为空时根据 `店铺` 名称映射。
- 特殊日期修正：解析 `付款日期` 后，年份 `2525` 自动改为 `2025`，年份 `2024` 自动改为 `2026`。

```python
# 8.11/backend/upload/jushuitan/config.py
date_year_replacements=((2525, 2025), (2024, 2026))
```

重要：这条规则会修改所有上传记录中的对应年份。若文件中存在真实的 2024 年历史数据，也会被改成 2026 年；正式提交前必须在预览中核对日期范围。

## 5. 上传文件注意事项

### 5.1 文件格式和大小

- 只支持 `.csv` 和 `.xlsx`，不支持 `.xls`、压缩包或其他格式。
- 当前默认上限为 `314572800` 字节，即 300MB；来源为根目录 `.env.example` 的 `MAX_UPLOAD_BYTES`。
- CSV 支持 `UTF-8 BOM/UTF-8` 和 `GB18030`。
- XLSX 只读取工作簿的当前活动工作表 `workbook.active`。
- XLSX 第一行必须是表头；公式按缓存结果读取，上传前应确保文件已在表格软件中计算并保存。

### 5.2 表头

- 表头不能为空，也不能重复。
- 平台原始列名必须与对应 `raw_data` 列一致。
- 除配置在 `ignored_upload_columns` 中的字段外，文件出现数据库没有的列会报 `RAW_COLUMN_MISMATCH`。
- 非关键的 `raw_data` 列可以不出现在文件中；交易日期和本文件第 3 节列出的业务字段不能缺少。
- 应优先使用各平台原始导出文件，不要手工改列名或套用统一模板。

### 5.3 日期和数据行

- 交易日期值为空、`-` 或 `--` 的行会被排除，并计入 `excluded_undated_rows`。
- 只要存在无法解析的非空交易日期，整个文件会报 `UPLOAD_ROW_INVALID`，不会进入提交。
- 所有行都没有可用交易日期时，报 `SALES_DATE_MISSING`。
- 上传解析以北京时间 `Asia/Shanghai` 执行。

### 5.4 权限、预览和正式提交

- 主管 `manager` 可以看汇总数据，但代码明确禁止主管上传。
- 达人组、私域组、分销组账号只能上传自己组内店铺。
- 所有九个店铺都已启用 `mode=commit`。
- 正确流程必须先 `mode=preview`，核对日期、新增/覆盖/跳过数量、退款、客户和影响周期，再二次确认 `mode=commit`。
- 正式提交会在单一数据库事务中更新原始表、客户映射、店铺派生表和上级汇总表；任何一步失败都会回滚。

## 6. 本次已修正的风险

1. 增加 `required_upload_columns`：缺少金额、退款、数量、商品或客户关键列时，不再静默接受文件。
2. 修正微店预览策略说明：由错误的“覆盖已有日期”改为与代码一致的“跳过已有日期”。
3. 修正快手金额解析：删除把字符串 `65` 当作无效金额的特殊判断，并增加回归测试。
4. 更新上传模块和后端主 README：正式提交状态统一为九店已启用。
5. 保留聚水潭既有年份修正规则，但在本文档中明确其影响和正式提交前核对要求。

对应自动化验证：

```text
python -m pytest -q
58 passed
```

## 7. 代码来源索引

- 店铺清单和名称：`8.11/backend/app/catalog.py`
- 上传配置数据结构：`8.11/backend/upload/models.py`
- 文件解析规则：`8.11/backend/upload/parsing.py`
- 表头、必需字段、日期及三种策略：`8.11/backend/upload/pipeline.py`
- 权限、文件大小、预览和正式提交：`8.11/backend/upload/service.py`
- 九店配置：`8.11/backend/upload/<store_key>/config.py`
- 抖店通用销售/退款规则：`8.11/backend/upload/config_helpers.py`
- 快手退款解析：`8.11/backend/upload/kuaishou/refunds.py`
- 各平台金额口径：各店铺目录下的 `preview.py` 和 `incremental_refresh.py`
- 回归测试：`8.11/backend/tests/test_upload_core.py`

